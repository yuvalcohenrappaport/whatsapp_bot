// Stub landed by Plan 37-01. Plan 37-03 replaces the body with the
// side-by-side variant card (full post + collapsible image prompt + image).
export function VariantCard(_props: Record<string, unknown>) {
  return null;
}
