// Stub landed by Plan 37-01. Plan 37-04 replaces the body with the
// "Pick lesson" / "Pick variant" entry button rendered into the queue
// card's actionsSlot for PENDING_LESSON_SELECTION / PENDING_VARIANT posts.
export function PendingActionEntryButton(_props: Record<string, unknown>) {
  return null;
}
