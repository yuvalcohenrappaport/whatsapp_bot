// Stub landed by Plan 37-01. Plan 37-03 replaces the body with the image
// slot that handles idle / pending / ready / error states for the fal.ai
// generation flow (CONTEXT §Area 3 Scenario B).
export type VariantImageMode = 'idle' | 'pending' | 'ready' | 'error';

export function VariantImageSlot(_props: Record<string, unknown>) {
  return null;
}
