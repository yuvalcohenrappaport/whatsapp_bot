# v1.9 Consolidated UAT

Walk this end-to-end on the live PM2 dashboard. Covers Plans 46-05 + 47-04 + 48-03 Task 4 + Phase 49. ~30 min total.

**Dashboard URL:** <http://100.124.47.99:3000> (via Tailscale)

**PM2 state going in:**
- whatsapp-bot pid 2980203, uptime 21m+, online
- Serving bundle `index-CiSMOBQK.js` (Apr 21, contains all v1.9 UI)
- All 5 aggregator sources = `ok` (tasks:68, gcal:16, linkedin:4, event:1, gtasks:1)

**Report back:** `approved` if everything passes, or list the failing step numbers with specifics.

---

## A. Google Tasks (Phase 46 — GTASKS-01..05) — 6 steps

Navigate to **/calendar**.

**A1 (GTASKS-01, -02, -03):** Pills from Google Tasks appear on the calendar grid with colored stripes. You should see at least one gtasks pill from the **סופר** (Hebrew) list — that list uses `bg-violet-500`. Most pills you see will be `source='task'` (from WhatsApp-approved tasks) — those get dedup'd OUT of gtasks per GTASKS-05.

**A2 (GTASKS-04 — filter panel):** Open the left rail (desktop) or tap the filter icon in the header (mobile). Confirm the **Google Tasks** section lists 3 rows:
- WhatsApp Tasks
- סופר
- Claude_missions

Each with a checkbox, color dot, name, item count.

**A3 (GTASKS-04 — toggle):** Uncheck **סופר**. The violet gtasks pill disappears. Refresh the page. **סופר** is still unchecked (localStorage persistence). Re-check it.

**A4 (GTASKS-04 — color override):** Click the gear on **WhatsApp Tasks**. Change color. Confirm pills update. Reset to original.

**A5 (GTASKS-03 — mobile action sheet + Complete):** On phone, long-press any gtasks pill. Action sheet shows Reschedule / Edit / Complete / Delete (not just read-only). Tap **Complete** — pill disappears immediately. (Use a throwaway test task if you don't want to kill a real one.)

**A6 (GTASKS-05 — dedup proof):** Open DevTools Network, find the `/api/google-tasks/items` response. It should contain **only 1 item** (the violet סופר "חלב"). It should NOT contain id `SEU2MUtEWVpJbkJ0RTVEMg` (a WhatsApp-created task "לקנות כרטיסים מחר") even though that id exists in Google Tasks — because it's mirrored by an approved actionable and dedup'd. That task should appear on the grid as a `source='task'` pill, NOT a gtasks pill.

---

## B. Google Calendar (Phase 47 — GCAL-01..06) — 8 steps

Stay on **/calendar**.

**B1 (GCAL-01):** DevTools Network → `/api/google-calendar/calendars` returns your 3 owned calendars:
- Family (rose)
- yuvalc79@gmail.com primary (violet)
- Italy (sky)

**B2 (GCAL-02 — recurring):** Pick a recurring event you own (weekly standup, daily habit). Confirm separate pills on each occurrence in the current view, not one spanning pill.

**B3 (GCAL-02 — all-day):** Pick a known all-day event (birthday, holiday, vacation). Confirm it renders on the correct day only, does NOT bleed into the next day. (Plan 47-01 subtracted 1ms from Google's exclusive end; a bug here would show a 1-day event as 2 days.)

**B4 (GCAL-02 — multi-day):** Pick a known 3-day event. Confirm it spans exactly those 3 days inclusive.

**B5 (GCAL-04 — filter panel):** Filter panel now shows TWO sections: Google Tasks (from A2) AND **Google Calendar**. The Google Calendar section lists Family / yuvalc79 / Italy. Uncheck **Family** — rose gcal pills disappear. Refresh — still unchecked. DevTools Application → Local Storage → `calFilterPrefs_v1` has both `gtasksLists` AND `gcalCalendars` keys. Re-check.

**B6 (GCAL-06 — read-only):** Try to drag a gcal pill — nothing happens (cursor stays default). Click the body — no inline editor opens. Long-press on mobile → action sheet shows ONLY "Open in Google Calendar" + Cancel (no Reschedule/Edit/Delete/Complete).

**B7 (GCAL-06 — htmlLink):** Tap "Open in Google Calendar" → opens a new tab to the event on calendar.google.com.

**B8 (GCAL-05 — dedup — caveat):** Live dedup cannot be empirically verified right now — `personal_pending_events` has **zero rows** with `calendar_event_id` set (no WhatsApp-detected event has yet been auto-synced into Google Calendar). This is acceptance-by-code-coverage: Plan 47-01's vitest suite has a dedup test case (`personal_pending_events calendar_event_id set → drops from gcal response`). Confirm in your own words: "Accepting GCAL-05 via code coverage" → moves on.

---

## C. LinkedIn Composer (Phase 48 — LIN-NEW-01) — 6 steps

Navigate to **/linkedin**.

**C1:** Header shows **New Post** (primary, left) + **New Lesson Run** (secondary, right).

**C2 (SC#4 — validation):** Click **New Post**. Dialog opens. Click Submit with empty fields — inline errors appear under Title, Content, Project. No network request fires.

**C3 (SC#4 — language cross-field):** Fill title="Phase 48 smoke". Project: any (e.g. "ESP32 Coffee Machine Water Sensor"). Language: Hebrew. Leave Content (Hebrew) empty. Submit → inline "Hebrew content is required for this language".

**C4 (SC#2, SC#3 — happy path):** Reset language to English. Content: "v1.9 closeout smoke — manual composer." Perspective: yuval. Submit → dialog closes, toast "Post created — awaiting review" fires. Watch the queue — within ~3 seconds a new card with status "Pending Review" appears showing that content. Amber arrival flash fires briefly.

**C5 (SC#6 — persistence):** Close dialog. Reopen. Language/project/perspective should be the values you just used. Title/content/content_he empty.

**C6 (cleanup):** Reject or delete the smoke post so the queue isn't littered.

---

## D. Milestone Sign-off (Phase 49 — VER-01) — 2 steps

**D1:** Confirm PM2 is still green: no restart/crash happened mid-UAT. (If any UAT step required a restart, we'll revisit.)

**D2:** Confirm all v1.9 surfaces work as one coherent product — Calendar merges tasks + events + linkedin + gtasks + gcal into one view with filters, Pending-Tasks page approves/rejects, LinkedIn composer creates posts. v1.9 is coherent and shippable.

---

## Fixture reference

If UAT asks "which task/event" — use these for deterministic evidence:

**Dedup'd gtasks actionables (should NOT show in /api/google-tasks/items):**
- `366f69fa*` — "לקנות כרטיסים מחר" — todo_task_id `SEU2MUtEWVpJbkJ0RTVEMg`
- `5a773708*` — "קשר לא ידוע..." — todo_task_id `RlRsbGdUMTVmeXkwRGl3Vg`
- Plus 14 more (total 16 mirrored in actionables, 0 duplicated in gtasks response)

**Lone non-mirrored gtasks item (SHOULD show as violet gtasks pill):**
- id `Mm9QNnc1cHZSVU4tMFBHdQ`, title "חלב", list "סופר"

**JWT for DevTools/curl sanity checks:** stored at `/tmp/whatsapp-bot-jwt` (30-day expiry, minted this session)

---

## Known non-issues (do not flag as failures)

- `package.json` uncommitted diff: `better-sqlite3 12.8.0 → 12.9.0` — from a stray `npm update`. Module is working fine on Node 20. Unrelated to v1.9.
- 15 pre-existing vitest failures (Node-22 ABI mismatch on better-sqlite3) — logged in Phase 41 deferred-items.md.
- `cli/*.ts` rootDir noise under `tsc --noEmit` — pre-existing.
