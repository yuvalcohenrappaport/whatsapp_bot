---
phase: 24-smart-reminders
plan: 03
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/api/routes/reminders.ts
  - dashboard/src/hooks/useReminders.ts
  - dashboard/src/pages/Reminders.tsx
  - dashboard/src/components/reminders/ReminderCard.tsx
  - dashboard/src/App.tsx
  - dashboard/src/pages/Overview.tsx
autonomous: true
requirements: [REM-01, REM-06]

must_haves:
  truths:
    - "Dashboard shows a Reminders page accessible from navigation"
    - "Reminders page has tabs: Upcoming, Completed, Cancelled"
    - "Each reminder card shows task, scheduled time, and status"
    - "Upcoming tab has a Cancel button per reminder"
    - "Cancelling from dashboard updates DB and clears setTimeout"
    - "Overview page shows reminders count stat card"
  artifacts:
    - path: "src/api/routes/reminders.ts"
      provides: "GET /api/reminders and POST /api/reminders/:id/cancel endpoints"
      exports: ["default (reminderRoutes)"]
    - path: "dashboard/src/hooks/useReminders.ts"
      provides: "React Query hook for reminders API"
      exports: ["useReminders", "useCancelReminder"]
    - path: "dashboard/src/pages/Reminders.tsx"
      provides: "Dashboard page with tabs"
      exports: ["default (Reminders)"]
    - path: "dashboard/src/components/reminders/ReminderCard.tsx"
      provides: "Individual reminder display card"
      exports: ["ReminderCard"]
  key_links:
    - from: "dashboard/src/pages/Reminders.tsx"
      to: "dashboard/src/hooks/useReminders.ts"
      via: "useReminders hook call"
      pattern: "useReminders"
    - from: "dashboard/src/hooks/useReminders.ts"
      to: "src/api/routes/reminders.ts"
      via: "fetch /api/reminders"
      pattern: "api/reminders"
    - from: "src/api/routes/reminders.ts"
      to: "src/db/queries/reminders.ts"
      via: "getRemindersByStatus query"
      pattern: "getRemindersByStatus"
    - from: "dashboard/src/App.tsx"
      to: "dashboard/src/pages/Reminders.tsx"
      via: "React Router route"
      pattern: "Reminders"
---

<objective>
Build the dashboard API routes and Reminders page for viewing and managing reminders from the web UI.

Purpose: Users need a visual way to see upcoming reminders and cancel them without WhatsApp commands. Follows the Events page pattern exactly.
Output: Working /api/reminders endpoints, React Query hook, and Reminders dashboard page with tabs.
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-smart-reminders/24-RESEARCH.md
@.planning/phases/24-smart-reminders/24-01-SUMMARY.md

@src/api/routes/personalCalendar.ts
@dashboard/src/pages/Events.tsx
@dashboard/src/hooks/usePersonalEvents.ts
@dashboard/src/components/events/EventCard.tsx
@dashboard/src/App.tsx
@dashboard/src/pages/Overview.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: API routes for reminders</name>
  <files>src/api/routes/reminders.ts, src/api/server.ts</files>
  <action>
1. Create `src/api/routes/reminders.ts` following the pattern in personalCalendar.ts:
   - `GET /api/reminders?status=pending` — returns `{ reminders: [...] }` with reminders filtered by status. Default status = 'pending'. Support status values: pending, fired, cancelled, skipped. Each reminder object: { id, task, fireAt, status, calendarEventId, createdAt, updatedAt }.
   - `POST /api/reminders/:id/cancel` — cancels a reminder. Import `cancelScheduledReminder` from reminderScheduler.ts to clear the setTimeout. Call `updateReminderStatus(id, 'cancelled')`. Return `{ ok: true }`. Return 404 if reminder not found.
   - `GET /api/reminders/stats` — returns `{ pending: number, fired: number, cancelled: number }` for the overview card. Use COUNT queries or getRemindersByStatus and count in JS (simpler).
   - Both routes use `{ onRequest: [fastify.authenticate] }` for auth.
   - Export default async function reminderRoutes(fastify: FastifyInstance).

2. Register the routes in `src/api/server.ts`:
   - Import `reminderRoutes` from './routes/reminders.js'
   - Register with `fastify.register(reminderRoutes)` alongside existing route registrations
  </action>
  <verify>
    `npx tsc --noEmit` passes. Start bot, authenticate to dashboard, then `curl -H "Cookie: ..." http://localhost:3000/api/reminders?status=pending` returns JSON with reminders array.
  </verify>
  <done>
    API endpoints for listing reminders by status, cancelling reminders, and getting stats are working with authentication.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Reminders page with hook, card, and navigation</name>
  <files>dashboard/src/hooks/useReminders.ts, dashboard/src/components/reminders/ReminderCard.tsx, dashboard/src/pages/Reminders.tsx, dashboard/src/App.tsx, dashboard/src/pages/Overview.tsx</files>
  <action>
1. Create `dashboard/src/hooks/useReminders.ts` following usePersonalEvents.ts pattern:
   - Export interface `Reminder { id, task, fireAt, status, calendarEventId, createdAt, updatedAt }`
   - Export `useReminders(status: string)` — useQuery with key ['reminders', status], fetches `/api/reminders?status={status}`, returns `data.reminders` as Reminder[]
   - Export `useCancelReminder()` — useMutation that POSTs to `/api/reminders/{id}/cancel`, invalidates ['reminders'] queries on success
   - Export `useReminderStats()` — useQuery with key ['reminder-stats'], fetches `/api/reminders/stats`

2. Create `dashboard/src/components/reminders/ReminderCard.tsx`:
   - Follow EventCard.tsx pattern. Card component displaying:
     - Task text (bold, primary)
     - Scheduled time: formatted using Intl.DateTimeFormat with Asia/Jerusalem timezone, showing date + time
     - Status badge (color-coded: pending=blue, fired=green, cancelled=red, skipped=yellow)
     - Calendar icon if calendarEventId exists (indicating calendar event was created)
     - Cancel button (only visible for status='pending'): calls useCancelReminder mutation
   - Use shadcn Card, Badge components. Use lucide-react icons: Bell, Calendar, X, Clock.
   - Show relative time until fire ("in 2 hours", "in 3 days") for pending reminders.

3. Create `dashboard/src/pages/Reminders.tsx` following Events.tsx pattern exactly:
   - Tabs: "Upcoming" (status=pending), "Completed" (status=fired), "Cancelled" (status=cancelled)
   - Each tab renders a ReminderList component that uses useReminders(status)
   - Loading state: Skeleton cards (same pattern as Events.tsx)
   - Empty state: icon + message per tab ("No upcoming reminders", "No completed reminders", etc.)
   - Pending tab shows count badge on the tab trigger
   - Page title: "Reminders" with heading font

4. Add route to `dashboard/src/App.tsx`:
   - Import Reminders page (lazy load if existing pages use lazy loading, otherwise direct import)
   - Add route: `<Route path="/reminders" element={<Reminders />} />`
   - Add navigation link in the sidebar/nav component (find where Events, Contacts, etc. are listed)
   - Use Bell icon from lucide-react for the nav item

5. Update `dashboard/src/pages/Overview.tsx`:
   - Add a reminders stat card to the overview grid (follow the pattern of the existing events stat card added in Phase 23)
   - Use useReminderStats() hook to get counts
   - Show pending count as primary number, "Reminders" as label
   - Use Bell icon, amber/orange theme color (to differentiate from violet events card)
  </action>
  <verify>
    `npx tsc --noEmit` passes. `cd dashboard && npm run build` succeeds. Open dashboard in browser, navigate to Reminders page, verify tabs work and show correct empty states. Create a reminder via WhatsApp, verify it appears in the Upcoming tab. Cancel from dashboard, verify it moves to Cancelled tab.
  </verify>
  <done>
    Dashboard Reminders page works with tabs (Upcoming/Completed/Cancelled), reminder cards show task + time + status, cancel button works, navigation link and overview stat card added.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. `cd dashboard && npm run build` — builds successfully
3. Dashboard nav shows "Reminders" link with Bell icon
4. Reminders page loads with three tabs
5. Creating a reminder via WhatsApp -> appears in dashboard Upcoming tab
6. Cancel button on dashboard -> reminder status changes, moves to Cancelled tab
7. Overview page shows reminders count card
</verification>

<success_criteria>
- API returns reminders filtered by status with proper authentication
- Dashboard cancel triggers both DB update and setTimeout cleanup
- Reminders page follows Events.tsx pattern (tabs, loading states, empty states)
- ReminderCard shows task, formatted time, status badge, calendar indicator
- Navigation and overview integration complete
- Dashboard builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-smart-reminders/24-03-SUMMARY.md`
</output>
