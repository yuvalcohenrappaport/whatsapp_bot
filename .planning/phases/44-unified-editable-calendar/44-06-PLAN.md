---
phase: 44-unified-editable-calendar
plan: 06
type: execute
wave: 5
depends_on:
  - "44-01"
  - "44-02"
  - "44-03"
  - "44-04"
  - "44-05"
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - SC1
  - SC2
  - SC3
  - SC4
  - SC5
  - SC6
  - SC7

must_haves:
  truths:
    - "Both PM2 services are running the Phase 44 code: pm-authority with the new /v1/posts/:id/reschedule endpoint, whatsapp-bot with the new calendar routes + mutation routes + unified stream"
    - "Dashboard static bundle serves the /calendar route with all three views and all interactions"
    - "Owner walks through all 7 SCs against real data — drag a task, drag an event, drag a LinkedIn post (snap), inline-edit a title, create from empty slot, click body to open full dialog, live update reflects via SSE, view toggle works, week is default"
    - "ROADMAP + REQUIREMENTS + STATE reflect Phase 44 completion and v1.8 milestone closure"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 44 marked complete, v1.8 milestone likely closed"
      contains: "Phase 44: Unified Editable Calendar"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 44 entry with SC1..SC7 (new CAL-xx ids if introduced)"
      contains: "Phase 44\\|Unified Editable Calendar\\|CAL-"
    - path: ".planning/STATE.md"
      provides: "Current Position block for Phase 44 complete"
      contains: "Phase 44"
  key_links:
    - from: "Browser /calendar drag gesture"
      to: "correct source-of-truth backend per source"
      via: "PATCH /api/actionables (task) | PATCH /api/personal-calendar/events (event) | POST /api/linkedin/posts/:id/reschedule (linkedin)"
      pattern: "drag → PATCH\\|drag → POST"
    - from: "Browser /calendar SSE"
      to: "/api/calendar/stream"
      via: "EventSource; hash-poll picks up external WhatsApp approval → Google Task fire → calendar refresh"
      pattern: "calendar\\.updated"
---

<objective>
Restart both services, rebuild + redeploy the dashboard, and run a comprehensive 11-check owner walk-through against the live /calendar page to validate every SC. Then flip Phase 44 to complete and evaluate v1.8 milestone closure (Phase 44 is the last phase in v1.8 per ROADMAP Progress table).

Purpose: v1.8 substrate is 80% shipped; Phase 44 is the final deliverable of the milestone. Live verification IS the acceptance bar — all 7 SCs are observable in the browser against the real bot + real DB + real pm-authority.

Output: A running whatsapp-bot PM2 process with calendar routes + mutation routes live, a running pm-authority with the reschedule endpoint live, a deployed dashboard bundle with the /calendar page, closeout entries in three planning docs.
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-unified-editable-calendar/44-CONTEXT.md
@.planning/phases/44-unified-editable-calendar/44-01-SUMMARY.md
@.planning/phases/44-unified-editable-calendar/44-02-SUMMARY.md
@.planning/phases/44-unified-editable-calendar/44-03-SUMMARY.md
@.planning/phases/44-unified-editable-calendar/44-04-SUMMARY.md
@.planning/phases/44-unified-editable-calendar/44-05-SUMMARY.md

# Pattern — Phase 43-03 did the same PM2 + owner walk-through
@.planning/phases/43-dashboard-pending-tasks-view/43-03-PLAN.md
@.planning/phases/43-dashboard-pending-tasks-view/43-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + deploy + restart both services</name>
  <files></files>
  <action>
    **Step A — server-side build:**
    - `cd /home/yuval/whatsapp-bot && npx tsc --noEmit` — tolerates pre-existing cli/bot.ts + cli/commands/persona.ts rootDir noise per STATE.md.
    - `cd /home/yuval/whatsapp-bot && npx vitest run src/api/__tests__/actionables.test.ts src/api/__tests__/calendarMutations.test.ts src/api/__tests__/calendar.test.ts src/api/linkedin/__tests__/writes.test.ts` — ensure all Phase 44 suites still green. Pre-existing 15 failing tests are tolerated.

    **Step B — dashboard build:**
    - `cd /home/yuval/whatsapp-bot/dashboard && npx vite build` — deliberately uses `npx vite build` not `npm run build` per STATE.md convention (keyword-rule tsc baseline noise). Confirm the `Calendar` chunk appears in the manifest.
    - Note final bundle sizes and record the delta vs Phase 43 (expected: +60-100 kB from the custom calendar + popover dep + radix popover).

    **Step C — pm-authority restart:**
    - Plan 44-01 already restarted pm-authority. Verify it's still serving the new endpoint:
      ```
      curl -s -X POST http://127.0.0.1:8765/v1/posts/does-not-exist/reschedule \
        -H "Content-Type: application/json" -d '{"scheduled_at":"2026-04-29T09:00:00"}'
      ```
      Expected: 404 NOT_FOUND envelope (confirms route is registered; any 405 indicates pm-authority needs a restart).
    - If 405: `pm2 restart pm-authority` (read `/home/yuval/pm-authority/ecosystem.config.js` to confirm exact process name first).

    **Step D — DB baseline snapshot before whatsapp-bot restart:**
    ```
    sqlite3 data/bot.db "SELECT COUNT(*) FROM actionables WHERE status IN ('approved','fired') AND fire_at IS NOT NULL;"
    sqlite3 data/bot.db "SELECT COUNT(*) FROM personal_pending_events WHERE status='approved';"
    curl -s -H "Authorization: Bearer $JWT" http://localhost:3000/api/linkedin/queue 2>/dev/null | jq '.posts | map(select(.status == "APPROVED")) | length'
    ```
    Record the three numbers into the SUMMARY.

    **Step E — whatsapp-bot PM2 restart:**
    - `pm2 restart whatsapp-bot` (NOT `--update-env` — per STATE.md `better-sqlite3` NODE_MODULE_VERSION lesson from 41-05; Node 20 pin in ecosystem.config.cjs is stable).
    - `pm2 logs whatsapp-bot --lines 50 --nostream`. Confirm:
      - `Approval system initialized` (Phase 41 init).
      - No better-sqlite3 mismatches.
      - No 44-02 migration errors (the calendar_event_id column ALTER already ran in Plan 44-02 against the live DB).
      - No errors in `logs/bot-error.log` within 30s of restart.

    **Step F — auth-smoke every new endpoint:**
    ```
    JWT=$(curl -s -X POST http://localhost:3000/api/auth/login -H "Content-Type: application/json" -d '{"password":"<owner provides>"}' | jq -r .token)

    # Unified read
    curl -s "http://localhost:3000/api/calendar/items" -H "Authorization: Bearer $JWT" | jq '.sources, (.items | length)'

    # Stream smoke (3s then kill)
    (timeout 5 curl -N "http://localhost:3000/api/calendar/stream?token=$JWT" 2>&1 | head -3) &

    # Mutation smokes (use a seeded actionable id from the baseline snapshot — pick the earliest)
    TASK_ID=$(sqlite3 data/bot.db "SELECT id FROM actionables WHERE status='approved' AND fire_at IS NOT NULL LIMIT 1;")
    curl -s -X PATCH "http://localhost:3000/api/actionables/$TASK_ID" \
      -H "Authorization: Bearer $JWT" -H "Content-Type: application/json" \
      -d '{}' # → 400 empty patch
    curl -s -X PATCH "http://localhost:3000/api/actionables/$TASK_ID" \
      -H "Authorization: Bearer $JWT" -H "Content-Type: application/json" \
      -d "{\"task\":\"[44-06 smoke $(date +%s)]\"}" | jq .task

    EVT_ID=$(sqlite3 data/bot.db "SELECT id FROM personal_pending_events WHERE status='approved' LIMIT 1;")
    curl -s -X PATCH "http://localhost:3000/api/personal-calendar/events/$EVT_ID" \
      -H "Authorization: Bearer $JWT" -H "Content-Type: application/json" \
      -d '{"location":"[44-06 smoke]"}' | jq .location

    # LinkedIn reschedule — use an APPROVED post id from queue
    LI_ID=$(curl -s -H "Authorization: Bearer $JWT" http://localhost:3000/api/linkedin/queue | jq -r '.posts | map(select(.status=="APPROVED")) | .[0].id')
    curl -s -X POST "http://localhost:3000/api/linkedin/posts/$LI_ID/reschedule" \
      -H "Authorization: Bearer $JWT" -H "Content-Type: application/json" \
      -d '{"scheduled_at":"2026-05-02T12:00:00"}' | jq '{id, status, scheduled_at}'
    ```

    Record all curl outputs in the SUMMARY. After each mutation, roll it back (PATCH back to the original task text, clear the location, reschedule LinkedIn back to its original slot) so the owner walk-through starts from a clean state.
  </action>
  <verify>
    1. PM2 `status` shows both whatsapp-bot and pm-authority online with fresh restart timestamps.
    2. Every curl above returns expected JSON.
    3. `pm2 logs whatsapp-bot --err --lines 100 --nostream` shows zero lines relevant to Phase 44.
    4. SSE stream emits a `calendar.updated` frame within 3s.
  </verify>
  <done>
    Both services live with Phase 44 code. All 7 new REST routes (3 linkedin from 44-01 + 2 actionables from 44-02 + 2 personal-calendar from 44-02 + 1 calendar items + 1 calendar stream) return 2xx/4xx as expected. Dashboard bundle ready to serve.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner walk-through of /calendar live — 11 checks mapped to the 7 SCs</name>
  <files></files>
  <action>
    Owner opens the deployed dashboard and walks through 11 acceptance checks against the live stack. Claude has already completed Task 1 (build + deploy + restart + auth-smoke on every new route).

    **What Claude built (one-liner recap for the owner):**
    A `/calendar` dashboard route with month/week/day views overlaying tasks (emerald stripe + checkmark icon), events (indigo stripe + calendar icon), and LinkedIn posts (violet stripe + LinkedIn icon). Drag any item to reschedule; click title to inline-edit; click empty slot to create; click pill body to open full dialog; SSE live-updates; partial failure surfaces a banner; reconnecting badge when stream drops.

    **11 checks mapped to SCs (reply "approved" if all pass; list failing numbers otherwise):**

    1. **[SC1 + SC7] Navigate + default view:** Sidebar now shows a new "Calendar" entry between Events and Reminders. Click → URL is `/calendar`. Default view is Week; current week is shown, first column is Sunday. Both tasks + events + LinkedIn posts are visible in the grid, each with the correct stripe + icon.

    2. **[SC1] Language mirroring:** Find at least one Hebrew item (task OR event OR LinkedIn). Pill renders right-to-left end-to-end. Find at least one English item — renders left-to-right.

    3. **[SC7] View toggle:** Click Month → month grid of 6×7 cells, current month labeled. Click Day → single-column timed grid for today. Click Week → back to default. Forward/back arrows navigate by unit. "Today" button jumps to current.

    4. **[SC2 — task reschedule]:** In Week view, drag an approved-actionable pill from its current cell to a different day + hour. Pill moves instantly. Toast `Moved "<title>"` appears with Undo. Wait 5s — toast auto-dismisses. SSE refreshes the data but pill stays in the new position. `sqlite3 data/bot.db "SELECT fire_at FROM actionables WHERE id='<task-id>';"` shows the new timestamp. Google Tasks in a second browser tab shows the same `due` value updated (may take 2-3 seconds for the mirror call).

    5. **[SC2 — event reschedule]:** Drag an approved personal event pill. Same behavior. `sqlite3 data/bot.db "SELECT event_date FROM personal_pending_events WHERE id='<event-id>';"` shows the new timestamp. Google Calendar in a second tab shows the event moved.

    6. **[SC2 — LinkedIn slot snap]:** Drag a LinkedIn post pill from its scheduled Tue/Wed/Thu 09:00 slot to a Saturday (non-posting day). Immediate feedback: snap animation or the pill lands on the next Tue 09:00 with a toast caption `Moved to next available slot: Tue 20YY-MM-DD 09:00`. Verify in pm-authority state.db: `sqlite3 /home/yuval/pm-authority/data/state.db "SELECT scheduled_at FROM posts WHERE id='<li-id>';"`.

    7. **[SC3 — inline title edit]:** Click the title of any task pill. Input appears with text selected. Type a new title + press Enter. SSE refresh within 3s shows the new title. DB `SELECT task FROM actionables WHERE id='...';` confirms.

    8. **[SC4 — create from empty slot]:** Click an empty Week-view slot (e.g. tomorrow 15:00). Popover opens. Task chip is the default. Type "test from calendar" + press Enter. New pill appears in the slot with 300ms amber arrival flash. `sqlite3` shows a new `actionables` row with `status='approved'` and the new `fire_at`. Google Tasks shows the new task.

    9. **[SC4 — create event from empty slot]:** Repeat with the Event chip. Title + eventDate; save. Google Calendar shows the new event; DB shows the new `personal_pending_events` row.

    10. **[SC5 — body click → full dialog — SCOPE NARROWING, explicit owner sign-off required]:** Click the body (not title) of a LinkedIn pill → the existing `EditPostDialog` from Phase 36 opens — literal SC5 satisfied on this source. For a task pill body click → a lightweight calendar-local dialog opens with title + reschedule date picker. For an event pill body click → a lightweight calendar-local dialog with title + date + location fields.

       **Narrowing disclosure:** Tasks.tsx and Events.tsx have no existing full-edit dialog as of Phase 43. Rather than expand Phase 44 scope to build those page-level dialogs first (a standalone sub-phase), Plan 44-05 provided calendar-local lightweight dialogs for task + event body-clicks. LinkedIn body-click still uses the existing Phase 36 EditPostDialog.

       **Owner question:** Is this acceptable as an SC5 narrowing, or do you want a separate phase to build full Tasks/Events dialogs first (on Tasks.tsx/Events.tsx page-level) and have the calendar delegate to those? Reply "SC5 narrowing accepted" to proceed with closeout, or "open follow-up phase for full task/event dialogs" to gate Phase 44 closure on that work.

    11. **[SC6 — live update via external trigger]:** Keep /calendar open. In WhatsApp, quoted-reply ✅ on a pending actionable in self-chat. Within 3 seconds, a new task pill appears on the calendar at its `fireAt` cell with the 300ms amber flash. Without a page reload.

    **Additional spot-checks not mapped to an SC but part of the locked CONTEXT:**
    - Past-dated items: cursor:not-allowed on hover, drag is a no-op.
    - `+3 more` button on a busy month-view day → popover lists all items sorted by start.
    - Stop pm-authority: `pm2 stop pm-authority`. Within ~5s a "LinkedIn unavailable" banner appears at the top of the calendar. Tasks + Events still render. Restart: banner disappears within 3s.
    - Stop whatsapp-bot entirely: "Reconnecting…" amber badge appears in the header. Restart: badge clears.

    **Resume signal:** Reply "approved" if all 11 pass. Reply with failing step numbers and symptoms otherwise — Claude will diagnose (drag geometry for 4/5/6, SSE hash loop for 11, dialog wiring for 10, etc.) and fix before re-presenting.
  </action>
  <verify>
    Owner has replied with explicit approval or a specific failure list. Do not proceed to Task 3 until approval is given.
  </verify>
  <done>
    Owner's approval message recorded verbatim in the 44-06 SUMMARY. Any intermediate fixes logged with their diffs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Closeout — ROADMAP, REQUIREMENTS, STATE, v1.8 milestone</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </files>
  <action>
    Execute only after Task 2 checkpoint is approved.

    **File A — `.planning/ROADMAP.md`:**
    - Find `### Phase 44` entry. Flip the milestone-table line (line ~413 in the Progress table) `| 44. Unified Editable Calendar | v1.8 | 0 | Pending | — |` to `| 44. Unified Editable Calendar | v1.8 | 6/6 | Complete | YYYY-MM-DD |`.
    - Update the phase's detailed block (line ~415): add `**Plans:** 6/6 plans complete` plus a plan list:
      ```
      Plans:
      - [x] 44-01-PLAN.md — pm-authority /v1/posts/:id/reschedule + whatsapp-bot proxy
      - [x] 44-02-PLAN.md — PATCH/POST routes for actionables + personal events + Google-service update helpers + calendar_event_id column
      - [x] 44-03-PLAN.md — Unified /api/calendar/items + /stream plugin
      - [x] 44-04-PLAN.md — Dashboard /calendar read-only shell (schemas + SSE hook + IST helpers + views + sidebar + router)
      - [x] 44-05-PLAN.md — Drag-to-reschedule + inline title edit + create popover + day overflow + full-dialog delegation
      - [x] 44-06-PLAN.md — Live verification + owner walk-through + closeout
      ```
    - Evaluate v1.8 milestone closure: all 6 phases (39, 40, 41, 42, 43, 44) should now be `Complete` in the Progress table. Flip the top-of-file line `- [ ] **v1.8 Task Approval & Context Enrichment**` to `- [x] **v1.8 Task Approval & Context Enrichment** — shipped YYYY-MM-DD`. Update the Milestones table accordingly.

    STATE.md notes: `gsd-tools roadmap update-plan-progress` is KNOWN-BROKEN for this project — edit manually.

    **File B — `.planning/REQUIREMENTS.md`:**
    Phase 44 had no formal CAL-xx IDs (SCs were the contract). Add a new Phase 44 section with 7 new requirement rows if the file's convention wants formal IDs — propose:
    - CAL-01: Calendar page renders three overlaid sources with distinct visual coding. **Complete**.
    - CAL-02: Drag reschedule persists to correct source-of-truth. **Complete**.
    - CAL-03: Inline title edit persists to source-of-truth. **Complete**.
    - CAL-04: Empty-slot create via type-chip popover. **Complete**.
    - CAL-05: Body click opens full edit dialog. **Complete**.
    - CAL-06: Live SSE updates reflect external changes. **Complete**.
    - CAL-07: Month/week/day toggle, week default. **Complete**.

    If the file's convention is freeform text (no ID column), just add a Phase 44 block with the 7 SC descriptions + completion date. Check the existing format before writing.

    **File C — `.planning/STATE.md`:**
    Add a new `## Current Position` block at the top. Preserve the existing Previous-chain below. Template:

    ```
    ## Current Position

    Milestone: v1.8 Task Approval & Context Enrichment — **COMPLETE (6/6 phases)**
    Phase: 44 Unified Editable Calendar — **COMPLETE**
    Plan: 44-06 shipped (YYYY-MM-DD). All 6 plans green; live verification owner-approved.
    Status: **Phase 44 COMPLETE (YYYY-MM-DD).** v1.8 milestone shipped. Dashboard `/calendar` page serves unified month/week/day view overlaying approved actionables (emerald), personal events (indigo), and LinkedIn scheduled posts (violet) with per-row RTL/LTR mirroring + absolute IST timestamps + 300ms amber arrival flash. Drag-to-reschedule persists to the correct source-of-truth per SC2 (PATCH /api/actionables for tasks, PATCH /api/personal-calendar/events for events, POST /api/linkedin/posts/:id/reschedule for LinkedIn — the last snapping forward to the next valid Tue/Wed/Thu 09:00 IST slot server-side). Inline title edit + empty-slot create popover + full-dialog delegation + day-overflow popover all wired. Unified /api/calendar/items + /api/calendar/stream plugin normalizes the three sources into one discriminated-union envelope with per-source partial-failure. Cross-repo work: pm-authority now exposes /v1/posts/:id/reschedule + SchedulingManager.reschedule_post. Google-service helpers added: todoService.updateTodoTask + personalCalendarService.updatePersonalCalendarEvent. New DB column personal_pending_events.calendar_event_id added for event-update linkage. Vite bundle delta: [measured]. SC1..SC7 all directly verified during owner 11-step walk-through.
    ```

    **Commit + SUMMARY:**
    - `git add -f .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md`
    - Single closeout commit: `docs(phase-44): closeout — Phase 44 + v1.8 milestone complete`
    - Create `.planning/phases/44-unified-editable-calendar/44-06-SUMMARY.md` documenting:
      - Build + deploy + restart metadata (PM2 pids, uptime resets).
      - Bundle deltas (dashboard bundle before Phase 44 vs after; record both raw + gzip).
      - All curl outputs from Task 1 Step F.
      - Owner's approval message verbatim.
      - Any issues surfaced during the walk-through and how they were resolved.
      - Requirements traceability — SC1..SC7 → Plan mapping.
      - v1.8 milestone closeout note.
  </action>
  <verify>
    1. `git log -n 1 --oneline` shows the closeout commit.
    2. `grep -n "Phase 44.*Complete\|Unified Editable Calendar" .planning/ROADMAP.md` → shows `Complete` in both the Progress table and the phase detail.
    3. `grep -n "v1.8.*shipped\|v1.8.*COMPLETE" .planning/ROADMAP.md .planning/STATE.md` → milestone closure reflected in both.
    4. `ls .planning/phases/44-unified-editable-calendar/` lists all 6 PLAN + 6 SUMMARY + 44-CONTEXT.md.
  </verify>
  <done>
    All three closeout docs updated + committed. 44-06-SUMMARY.md exists with the full trace. Phase 44 officially complete. v1.8 milestone officially complete.
  </done>
</task>

</tasks>

<verification>
Phase 44 is complete when all 11 owner-checkpoint items pass AND the three closeout files reflect the new status AND the v1.8 milestone flip is documented.
</verification>

<success_criteria>
- Both PM2 services are running the Phase 44 code.
- Dashboard bundle includes the /calendar page.
- Owner confirms all 11 verification items.
- `.planning/ROADMAP.md` marks Phase 44 complete + v1.8 milestone shipped.
- `.planning/REQUIREMENTS.md` records SC1..SC7 / CAL-01..CAL-07 traceability.
- `.planning/STATE.md` has a new Current Position block.
- `44-06-SUMMARY.md` records the verification outcomes and closeout.
</success_criteria>

<output>
Plan 44-06 SUMMARY at `.planning/phases/44-unified-editable-calendar/44-06-SUMMARY.md` must include:
- DB + pm-authority baseline snapshots.
- Curl outputs (auth-smoke across every new endpoint).
- PM2 restart metadata (pids, uptime reset) for both services.
- Owner approval message verbatim.
- Any issues surfaced during the walk-through and how they were resolved.
- Requirements traceability confirmation (SC1..SC7 each tied to specific Plan + specific check).
- v1.8 milestone closeout note.
- Vite bundle delta (pre-Phase-44 vs post-Phase-44).
</output>
