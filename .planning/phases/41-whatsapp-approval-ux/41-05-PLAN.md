---
phase: 41-whatsapp-approval-ux
plan: 05
type: execute
wave: 5
depends_on: [41-01, 41-02, 41-03, 41-04]
files_modified: []
autonomous: false
requirements: [APPR-01, APPR-02, APPR-03, APPR-04, APPR-05, DETC-03]

must_haves:
  truths:
    - "PM2 restarted after Plans 41-01..04 land. On first boot, the digest fires (⏳ N items waiting…), pipeline setting flips to 'interactive', and the existing pending_approval backlog is flushed through per-chat debounce buckets"
    - "A new synthetic detection message produces a self-chat preview within ~2 min (debounce window)"
    - "Quoted-reply ✅ on a preview approves the actionable, creates a Google Tasks entry, and the bot sends ✅ Added: <task> confirmation"
    - "Quoted-reply edit: <new text> rewrites the task and then pushes it (same confirmation with new text)"
    - "Self-chat `remind me to X at Y` creates an actionable with source_type=user_command, status=approved and the legacy reminder still fires at the scheduled time"
    - "Owner walks through each of the 5 SCs and approves"
  artifacts: []
---

<objective>
Live verification of the Phase 41 approval UX on the production bot. Owner-driven — merge, restart, exercise each directive, verify.
</objective>

<context>
@.planning/phases/40-unified-detection-pipeline/40-CONTEXT.md
@.planning/phases/41-whatsapp-approval-ux/41-01-SUMMARY.md
@.planning/phases/41-whatsapp-approval-ux/41-02-SUMMARY.md
@.planning/phases/41-whatsapp-approval-ux/41-03-SUMMARY.md
@.planning/phases/41-whatsapp-approval-ux/41-04-SUMMARY.md
</context>

<tasks>

<task type="human-action">
  <name>Task 1: Merge branch + PM2 restart (owner)</name>
  <files></files>
  <action>
Owner-driven:
```bash
cd ~/whatsapp-bot
git checkout main
git merge --no-ff feat/v1.8-phase-41 # or whichever branch was used
pm2 restart whatsapp-bot
pm2 logs whatsapp-bot --lines 80 --nostream | grep -E "digest|Approval system|expiry|initialized"
```

Expected log: `Approval system initialized` + digest log line (`Approval digest sent; backlog enqueued` with the accumulated backlog count).
  </action>
  <verification>
- [ ] Owner confirms PM2 is online with new pid
- [ ] `Approval system initialized` present in logs
- [ ] Either `Approval digest sent` (with backlog count > 0) or `Approval digest skipped — no pending actionables`
  </verification>
</task>

<task type="human-verify">
  <name>Task 2: Digest fires + pipeline flips</name>
  <files></files>
  <action>
Within seconds of restart the owner's self-chat should receive:
- One `⏳ N items waiting for approval…` message (in the source-chat language of the most recent pending actionable)
- Within 2 min, one batched preview per source chat that had pending items (grouped, numbered grammar)

Verify the gate flipped:
```bash
sqlite3 ~/whatsapp-bot/data/bot.db "SELECT key, value FROM settings WHERE key IN ('v1_8_detection_pipeline', 'v1_8_approval_digest_posted')"
```
Expected: `v1_8_detection_pipeline=interactive`, `v1_8_approval_digest_posted=true`.
  </action>
  <verification>
- [ ] Owner received digest message
- [ ] Owner received one or more batched previews
- [ ] Settings table shows the flipped values
  </verification>
</task>

<task type="human-verify">
  <name>Task 3: Fresh detection + approve + reject + edit grammar</name>
  <files></files>
  <action>
Send a new synthetic commitment from a whitelisted contact. Wait ~2 min (debounce). A new preview arrives. Exercise each directive:

**(a) Approve with ✅:**
- Quoted-reply `✅` (for a single-item preview) or `1 ✅` for batched
- Expect: `✅ Added: <task>` confirmation arrives
- Check Google Tasks app: new entry appears in the "WhatsApp Tasks" list with the raw task text (NOT enriched — Phase 42 ships enrichment)
- Check DB: `SELECT status, todo_task_id FROM actionables WHERE id = <id>` → `approved` + non-null

**(b) Reject with ❌:**
- Trigger another detection
- Quoted-reply `❌`
- Expect: `❌ Dismissed` confirmation
- DB: status=rejected; Google Tasks: no new entry

**(c) Edit with edit:**
- Trigger another detection
- Quoted-reply `edit: Send the Q2 report to Lee by Friday`
- Expect: `✅ Added: Send the Q2 report to Lee by Friday`
- DB: `task` field updated, `original_detected_task` unchanged; Google Tasks entry has the new title

**(d) Unparseable reply:**
- Quoted-reply `banana`
- Expect: grammar hint message (`Reply ✅ / ❌ / edit: <text>` or HE equivalent)

**(e) Hebrew:**
- If possible, trigger a Hebrew detection (contact sends Hebrew text)
- Quoted-reply `אישור` or `1 אישור`
- Expect Hebrew confirmation `✅ נוסף: <משימה>`
  </action>
  <verification>
- [ ] All 5 directive paths work end-to-end
- [ ] Confirmations match language of the source chat
  </verification>
</task>

<task type="human-verify">
  <name>Task 4: Self-chat direct command bypass (DETC-03)</name>
  <files></files>
  <action>
In the owner's self-chat (not a contact chat), type: `remind me to call mom at 6pm`.

Expected:
- Legacy reminder confirmation fires (from existing reminderService): `⏰ call mom — Wed, Apr 19, 18:00` or similar
- NEW: a row appears in `actionables` with source_type='user_command', status='approved'
- The actionable has the same fireAt as the legacy reminder (for scheduled firing at 6pm)

Verify:
```bash
sqlite3 ~/whatsapp-bot/data/bot.db "SELECT id, source_type, status, task, fire_at FROM actionables WHERE source_type='user_command' AND detected_at > (strftime('%s','now') - 600)*1000"
```
  </action>
  <verification>
- [ ] Reminder was accepted as usual (existing reminderService behavior unchanged)
- [ ] New actionables row with source_type=user_command and status=approved
  </verification>
</task>

<task type="human-verify">
  <name>Task 5: 7-day expiry scan (optional — or verified via code inspection)</name>
  <files></files>
  <action>
The expiry scan runs hourly. Real-world verification requires waiting 7 days or backdating rows. Two options:

- **Backdate verification:** `sqlite3 ~/whatsapp-bot/data/bot.db "UPDATE actionables SET detected_at = (strftime('%s','now') - 8*86400)*1000 WHERE id = '<some-pending-id>'"`; wait for the next hourly scan; verify `SELECT status FROM actionables WHERE id = '<id>'` → `expired`
- **Skip live verification:** the expiry scan is covered by unit tests in Plan 41-04; owner can defer

If deferred, note in the phase-close commit.
  </action>
  <verification>
- [ ] Either live-verified via backdate OR explicitly deferred with a note
  </verification>
</task>

<task type="human-action">
  <name>Task 6: Owner sign-off and phase close</name>
  <files></files>
  <action>
Owner replies "approved" with any notes. Orchestrator then:
1. Flip ROADMAP.md Phase 41 checkbox + Progress table row
2. Flip APPR-01..05 + DETC-03 in REQUIREMENTS.md traceability
3. Update STATE.md for Phase 41 complete + advance to Phase 42
4. Commit `docs(phase-41): complete phase execution`

Any issues → gap closure via `/gsd:plan-phase 41 --gaps`.
  </action>
  <verification>
- [ ] Owner says "approved"
- [ ] Tracking docs + commit updated
  </verification>
</task>

</tasks>

<success_criteria>
- [ ] All 5 approval directive paths (approve, reject, edit, unparseable, bulk) work end-to-end on live traffic
- [ ] Digest flushed the Phase 40 backlog and the pipeline setting flipped to `interactive`
- [ ] Self-chat direct commands create approved user_command actionables
- [ ] Expiry scan either verified or explicitly deferred
- [ ] Owner signs off
</success_criteria>
