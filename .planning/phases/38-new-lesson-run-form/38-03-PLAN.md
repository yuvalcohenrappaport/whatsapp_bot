---
phase: 38-new-lesson-run-form
plan: 03
type: execute
wave: 3
depends_on: [38-02]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [LIN-14]

must_haves:
  truths:
    - "Owner can open the dashboard, click New Lesson Run, fill the form, submit, and see the new PENDING_LESSON_SELECTION post appear in the queue"
    - "Validation errors surface inline on the form, not as opaque 500s"
    - "The SSH + generate.py --mode lesson workflow is no longer required for the owner's normal lesson-mode usage"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 38 marked complete"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LIN-14 marked complete"
    - path: ".planning/STATE.md"
      provides: "Updated with Phase 38 completion"
  key_links:
    - from: "dashboard New Lesson Run button"
      to: "pm-authority PENDING_LESSON_SELECTION post"
      via: "form submit -> proxy -> pm-authority -> background worker -> SSE"
      pattern: "end-to-end"
---

<objective>
Live end-to-end verification of the New Lesson Run form in the owner's browser, then update project state artifacts.

Purpose: Confirm all 4 success criteria are met with real pm-authority data and real SSE updates. Mirror Phase 36-05 / 37-05 pattern.

Output: Verified working form + updated STATE.md, ROADMAP.md, REQUIREMENTS.md.
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-new-lesson-run-form/38-CONTEXT.md
@.planning/phases/38-new-lesson-run-form/38-01-SUMMARY.md
@.planning/phases/38-new-lesson-run-form/38-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restart services and build dashboard</name>
  <files>
    (no files -- runtime verification)
  </files>
  <action>
1. Restart pm-authority HTTP service: `npx pm2 restart pm-authority-http`
2. Rebuild dashboard: `cd /home/yuval/whatsapp-bot/dashboard && npx vite build`
3. Restart whatsapp-bot: `npx pm2 restart whatsapp-bot`
4. Verify both services are online:
   - `curl http://127.0.0.1:8765/v1/health` returns 200
   - `curl http://127.0.0.1:8765/v1/projects` returns project list
   - `curl http://127.0.0.1:3000/health` returns 200
   - `curl http://127.0.0.1:3000/api/linkedin/health` returns upstream ok (with JWT)
5. Run the full test suites to confirm no regressions:
   - `cd /home/yuval/pm-authority && python -m pytest tests/test_http*.py -v`
   - `cd /home/yuval/whatsapp-bot && npx vitest run src/api/linkedin/`
  </action>
  <verify>
Both PM2 services online. /v1/projects returns project names. All tests pass.
  </verify>
  <done>
Services running, endpoints responding, tests green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: LIVE browser walkthrough -- 4 Success Criteria</name>
  <files>
    (no files -- human interaction only)
  </files>
  <action>
HUMAN CHECKPOINT. Claude does NOT execute commands for this task -- the owner drives the browser. Claude presents the walkthrough script and waits for "approved".

**What was built (full Phase 38 New Lesson Run form):**
  - Plan 38-01: pm-authority GET /v1/projects + POST /v1/lesson-runs/generate (4 lesson candidates), proxy routes in whatsapp-bot
  - Plan 38-02: NewLessonRunSheet slide-out form (project dropdown + Custom, perspective radio, language radio, topic hint), useLinkedInProjects + useLinkedInStartLessonRun hooks, queue page header button, localStorage persistence, 409 retry countdown, 3-min timeout banner

**How to verify -- open http://127.0.0.1:3000/linkedin/queue in a browser:**

---

### SC#1 -- Form fields and submission

1. Confirm a "New Lesson Run" button is visible in the queue page header (primary action color, always visible).
2. Click the button. A sheet slides out from the right.
3. Confirm the sheet contains:
   - Project dropdown populated with real project names from pm-authority
   - "Custom..." option at the bottom of the dropdown (reveals free-text input when selected)
   - Perspective radio: "Yuval (first person)" / "Claude (collaborator)"
   - Language radio: English / Hebrew / Bilingual (he+en)
   - Topic hint textarea (optional, placeholder text)
   - Submit button at the bottom
4. Try submitting with no project selected -- confirm inline validation error appears.
5. Select a real project, leave perspective and language at defaults, submit.
6. Confirm: sheet closes, toast appears "Lesson run started for {project name}".

### SC#2 -- Post appears in queue

7. Wait up to 2-3 minutes on the queue page.
8. A new PENDING_LESSON_SELECTION post should appear in the queue with:
   - 300ms amber arrival flash (Phase 37-04 animation)
   - Purple "Lesson to pick" status pill
   - "Pick lesson" entry button
9. The "Lessons to pick" counter in the status strip should increment.

### SC#3 -- Validation errors inline

10. Reopen the form. Select "Custom..." in the project dropdown.
11. Type a nonsense project name (e.g., "zzz-nonexistent-project").
12. Submit. Confirm inline error appears under the project field (from the 404 response).
13. No opaque 500 error or browser error -- the error is user-friendly and inline.

### SC#4 -- localStorage persistence

14. Reopen the form after the successful submission in step 5-6.
15. Confirm project, perspective, and language are pre-filled from the last submission.
16. Confirm the topic hint field is empty (resets each time).

### Timeout banner (optional, only observable if generation takes >3min)

17. If a submission takes longer than 3 minutes, confirm an amber warning banner appears: "Lesson run for {project} may have failed -- check logs".

---

Report any issues or type "approved" to continue.
  </action>
  <verify>
Owner confirms all 4 SCs observed in the live browser. Form submits, post appears in queue, validation errors are inline, localStorage persistence works.
  </verify>
  <done>
All 4 Phase 38 success criteria verified by the owner in a live browser session.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md, ROADMAP.md, REQUIREMENTS.md</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
**Only run this task after the checkpoint is approved.**

1. Update `.planning/ROADMAP.md`:
   - Phase 38 checkbox: `[x]`
   - Plans: `3/3 plans complete`
   - Add completion date
   - Update the progress table: Phase 38 status = Complete
   - v1.7 milestone: if Phase 38 is the last phase, mark v1.7 as shipped

2. Update `.planning/REQUIREMENTS.md`:
   - LIN-14: mark `[x]` complete with traceability to Plans 38-01/02/03

3. Update `.planning/STATE.md`:
   - Move Phase 38 from "in progress" to "completed"
   - Note v1.7 milestone completion (Phase 38 is the last phase in v1.7)
   - Update current position
  </action>
  <verify>
`grep -n "LIN-14" .planning/REQUIREMENTS.md` shows `[x]`.
`grep -n "Phase 38" .planning/ROADMAP.md` shows completion.
  </verify>
  <done>
All project state artifacts updated. Phase 38 marked complete. LIN-14 requirement satisfied. v1.7 milestone complete (all phases 33-38 shipped).
  </done>
</task>

</tasks>

<verification>
- Services running and healthy
- Owner verified the form works end-to-end in their browser
- New PENDING_LESSON_SELECTION post appeared in the queue after form submission
- Project state artifacts updated
</verification>

<success_criteria>
1. All 4 Phase 38 success criteria verified by the owner in their browser
2. LIN-14 marked complete in REQUIREMENTS.md
3. Phase 38 marked complete in ROADMAP.md
4. v1.7 milestone finalized
</success_criteria>

<output>
After completion, create `.planning/phases/38-new-lesson-run-form/38-03-SUMMARY.md`
</output>
