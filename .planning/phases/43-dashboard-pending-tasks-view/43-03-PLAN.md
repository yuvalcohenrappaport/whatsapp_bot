---
phase: 43-dashboard-pending-tasks-view
plan: 03
type: execute
wave: 3
depends_on:
  - "43-01"
  - "43-02"
files_modified: []
autonomous: false
requirements:
  - DASH-ACT-01
  - DASH-ACT-02

must_haves:
  truths:
    - "Live PM2 bot serves the new /api/actionables/* routes and the dashboard build includes the /pending-tasks page"
    - "Owner visits /pending-tasks in a browser, sees every DB-pending actionable, audit list shows recent terminal rows"
    - "Approving a pending actionable in WhatsApp moves the row from Pending to Recent in ≤3 seconds without a page reload"
    - "Hebrew rows render RTL, English rows render LTR, timestamps are absolute IST, sidebar has a new 'Pending Tasks' entry"
    - "ROADMAP + REQUIREMENTS + STATE closeout reflects Phase 43 completion"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 43 marked complete with plan count + closeout note"
      contains: "Phase 43: Dashboard Pending Tasks View"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DASH-ACT-01 + DASH-ACT-02 flipped to Complete"
      contains: "DASH-ACT-01"
    - path: ".planning/STATE.md"
      provides: "Phase 43 closeout entry"
      contains: "Phase 43"
  key_links:
    - from: "Browser /pending-tasks"
      to: "Fastify /api/actionables/stream"
      via: "EventSource SSE with JWT query-string"
      pattern: "actionables\\.updated"
    - from: "WhatsApp approval ✅"
      to: "dashboard Recent list"
      via: "status flip in DB → hash change → SSE emit → client Zod parse → setRecent"
      pattern: "pending → approved"
---

<objective>
Restart PM2 on the live bot, rebuild + redeploy the dashboard, walk the owner through the /pending-tasks page end-to-end, then flip Phase 43 to complete in the planning docs.

Purpose: Phases 41 + 42 already shipped organic approval traffic live. This phase exists specifically to make that lifecycle browser-auditable. Live verification is the acceptance bar — the whole point of the page is that the owner uses it against real data on the real bot.

Output: A running PM2 process with the new routes, a deployed dashboard bundle with the new page, and closeout entries in the three planning docs.
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-dashboard-pending-tasks-view/43-CONTEXT.md
@.planning/phases/43-dashboard-pending-tasks-view/43-01-SUMMARY.md
@.planning/phases/43-dashboard-pending-tasks-view/43-02-SUMMARY.md

# Pattern — Phase 41-05 closeout did the same restart+walk-through
@.planning/phases/41-whatsapp-approval-ux/41-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + deploy + restart</name>
  <files></files>
  <action>
    **Step A — server build:**
    Run `npx tsc --noEmit` at the repo root. Must succeed with no NEW errors attributable to Plan 43-01 files. Pre-existing cli/bot.ts + cli/commands/persona.ts rootDir noise is tolerated (per STATE.md convention).

    **Step B — dashboard build:**
    `cd dashboard && npm run build`. Confirm the new `PendingTasks` chunk appears in the Vite build output. Build must succeed with no new warnings from Plan 43-02 files.

    **Step C — DB baseline snapshot:**
    Run and record BEFORE the restart:
    ```
    sqlite3 data/bot.db "SELECT status, COUNT(*) FROM actionables GROUP BY status ORDER BY status;"
    sqlite3 data/bot.db "SELECT COUNT(*) FROM actionables WHERE status='pending_approval';"
    sqlite3 data/bot.db "SELECT COUNT(*) FROM actionables WHERE status IN ('approved','rejected','expired','fired') AND updated_at > strftime('%s','now','-7 days')*1000;"
    ```
    Save the three numbers into the SUMMARY.

    **Step D — PM2 restart:**
    `pm2 restart whatsapp-bot --update-env`. Follow with `pm2 logs whatsapp-bot --lines 50 --nostream`. Confirm:
    - `Approval system initialized` appears in the boot log (Phase 41 init — must still run clean).
    - No `better-sqlite3` NODE_MODULE_VERSION mismatch (Phase 41-05 learned lesson — `ecosystem.config.cjs` pins Node 20; DO NOT touch it per STATE.md convention).
    - No errors in `logs/bot-error.log` within 30s of restart.

    **Step E — auth-smoke the new routes from the server shell:**
    ```
    JWT=$(curl -s -X POST http://localhost:3000/api/auth/login -H "Content-Type: application/json" -d '{"password":"<owner provides>"}' | jq -r .token)
    curl -s -H "Authorization: Bearer $JWT" http://localhost:3000/api/actionables/pending | jq '.actionables | length'
    curl -s -H "Authorization: Bearer $JWT" 'http://localhost:3000/api/actionables/recent?limit=10' | jq '.actionables | length'
    curl -s -H "Authorization: Bearer $JWT" http://localhost:3000/api/actionables/pending | jq '.actionables[0]'
    ```
    Confirm counts match Step C snapshot (pending count should equal), the first row has all the expected fields (task, sourceContactName, sourceMessageText, detectedLanguage, detectedAt), and recent-limit is honored.

    Record the curl outputs in the SUMMARY.
  </action>
  <verify>
    1. PM2 `status` shows the process online, uptime clock reset (fresh restart), no errors in tail.
    2. All three curl commands return valid JSON with non-empty arrays matching DB counts.
    3. `pm2 logs whatsapp-bot --err --lines 100 --nostream` shows zero lines relevant to Phase 43 work.
  </verify>
  <done>
    Live bot is running the new code. All three REST routes respond 200 with correct JSON shape. No new errors in the log.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner walk-through of /pending-tasks live</name>
  <files></files>
  <action>
    Owner opens the deployed dashboard and walks through 9 acceptance checks against the live bot + live DB. Claude has already completed Task 1 (build + deploy + restart + auth-smoke curl); this checkpoint is the human UAT gate before closeout in Task 3.

    **What Claude already built (for reference in the review):**
    - Fastify REST routes: GET /api/actionables/pending, GET /api/actionables/recent, GET /api/actionables/stream (JWT-gated).
    - Dashboard page at /pending-tasks with Pending list + Recent audit list + filter chips (All/Approved/Rejected/Expired).
    - Sidebar entry "Pending Tasks" with Inbox icon.
    - Live SSE updates: approving in WhatsApp moves the row from Pending to Recent within ~3 seconds without page reload.
    - Hebrew rows render RTL, English rows render LTR, timestamps are absolute IST.
    - Page is read-only — no approve/reject/edit affordances anywhere.

    **9 checks for the owner to run (reply "approved" when all pass, or list failing step numbers):**

    1. **Navigate:** Open the dashboard in a browser, click the new "Pending Tasks" item in the sidebar. URL becomes /pending-tasks.
    2. **Pending section:** Confirm the count in the header matches the baseline sqlite snapshot (Task 1 Step C). Scroll through — each card shows proposed task as the headline (big), contact name small below, source snippet multi-line (capped at ~6 lines), timestamp in `YYYY-MM-DD HH:mm` IST format.
    3. **Language direction:** Find at least one Hebrew pending card — the whole card (task headline + snippet) must read right-to-left. Find at least one English pending card — reads left-to-right.
    4. **Recent section:** Default filter "All" shows ≤50 rows. Click "Approved" — only approved/fired rows visible. Click "Rejected" — only rejected. Click "Expired" — only expired. Click "All" — everything returns.
    5. **Audit card shape:** Pick an approved row whose `enrichedTitle` differs from `originalDetectedTask` — confirm the enriched title is the headline and a subtle "Originally: …" line appears below. Badge is emerald-colored. A "Open in Google Tasks ↗" link appears.
    6. **Live update — the main event:** Keep the /pending-tasks tab open. Go to WhatsApp, quoted-reply ✅ on one of the pending previews in your self-chat. Watch the browser — within ~3 seconds the row must disappear from Pending and appear at the top of Recent under "Approved". NO manual refresh, NO page reload. If nothing happens, check the browser console for schema-drift logs and the PM2 tail for stream-poll warnings.
    7. **New-arrival flash (if detectable):** If organic detection fires a new pending actionable while the page is open (or trigger one by sending yourself a task-like message), the new row should slide in at the top of Pending with a ~300ms amber flash. May need a rehearsed trigger message — note in the reply if you skip this step.
    8. **Read-only confirmation:** Try to find any approve/reject/edit button on the page. There should be none. The footer tip should read "Approve, reject, or edit any pending actionable in WhatsApp."
    9. **Reconnecting behavior:** `pm2 stop whatsapp-bot`. Within ~5 seconds the page should show a "Reconnecting…" amber badge near the header. `pm2 start whatsapp-bot --update-env` — badge disappears once SSE reconnects.

    **Resume signal:** Reply "approved" if all 9 pass. Reply with failing step numbers and symptoms if any fail — Claude will diagnose (SSE hash loop for #6, Zod schema for #2/#3, amber-flash CSS for #7, etc.) and fix before re-presenting the checkpoint.
  </action>
  <verify>
    Owner has replied with explicit approval or a specific failure list. Do not proceed to Task 3 (closeout) until approval is given.
  </verify>
  <done>
    Owner's approval message recorded verbatim in the Plan 43-03 SUMMARY. Any intermediate fixes logged with their diffs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Closeout — ROADMAP, REQUIREMENTS, STATE</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </files>
  <action>
    Execute only after the checkpoint above is approved.

    **File A — `.planning/ROADMAP.md`:**
    - Find the `### Phase 43` entry. Flip the milestone bullet `- [ ] **Phase 43: Dashboard Pending Tasks View**` to `- [x]` with completion date `— completed YYYY-MM-DD`.
    - Under "Phase Details" → Phase 43, add a `**Plans:** 3/3 plans complete` line.
    - Add `Plans:` list with each plan's one-line outcome, e.g.:
      ```
      Plans:
      - [x] 43-01-PLAN.md — REST + SSE routes for actionables (...see 43-01-SUMMARY.md...)
      - [x] 43-02-PLAN.md — /pending-tasks dashboard page + sidebar entry (...see 43-02-SUMMARY.md...)
      - [x] 43-03-PLAN.md — Live verification + closeout (...see 43-03-SUMMARY.md...)
      ```
    - Check whether the `v1.8` milestone's phase range (`Phases 39-43`) now has all 5 phases checked. If yes, flip the milestone line `- [ ] **v1.8 Task Approval & Context Enrichment**` to `- [x]` with "shipped YYYY-MM-DD" and update the milestones table at the top of ROADMAP.md accordingly.

    STATE.md also notes: `gsd-tools roadmap update-plan-progress <phase>` and `gsd-tools phase complete <phase>` are KNOWN-BROKEN for this project's ROADMAP — update manually. Do NOT run the tool; edit by hand.

    **File B — `.planning/REQUIREMENTS.md`:**
    Flip DASH-ACT-01 and DASH-ACT-02 status cells to `Complete` with the completion date. Preserve the existing table formatting. If the requirements file uses a different column scheme (e.g. `Phase / Status / Completed`), match the existing rows' format.

    **File C — `.planning/STATE.md`:**
    Add a new `Current Position` block at the top (preserve the existing "Previous:" chain immediately below). Sample shape matching the existing convention:
    ```
    ## Current Position

    Milestone: v1.8 Task Approval & Context Enrichment — [COMPLETE (5/5 phases) | IN PROGRESS]
    Phase: 43 Dashboard Pending Tasks View — **COMPLETE**
    Plan: 43-03 shipped (YYYY-MM-DD). All 3 plans green; live verification owner-approved.
    Status: **Phase 43 COMPLETE (YYYY-MM-DD).** REST routes `/api/actionables/pending`, `/api/actionables/recent`, `/api/actionables/stream` shipped JWT-gated. Dashboard page `/pending-tasks` ships pending list + audit list with filter chips (All/Approved/Rejected/Expired), absolute IST timestamps, per-row RTL/LTR mirroring by `detectedLanguage`, enriched title as audit headline with `Originally: …` secondary, color-coded status badges, Google Tasks link on approved rows, 300ms amber arrival flash on new pending arrivals (matching Phase 37 pattern), Reconnecting… badge on SSE drop. Zero mutation affordances — approve/reject/edit remain WhatsApp-only per v1.8 milestone lock. DASH-ACT-01 + DASH-ACT-02 → Complete. [If v1.8 closed: milestone v1.8 complete, next up: ...]
    ```
    Write the three files via `Read + Edit` (never `Write` to avoid clobbering). Use `git add -f` on the planning files (they are gitignored). Single commit: `docs(phase-43): closeout — Phase 43 complete, DASH-ACT-01/02 → Complete`.

    Then create the Plan 43-03 SUMMARY at `.planning/phases/43-dashboard-pending-tasks-view/43-03-SUMMARY.md` documenting:
    - Baseline sqlite numbers (from Task 1 Step C).
    - Curl outputs from the auth-smoke.
    - PM2 restart metadata (pid, restart count delta).
    - Owner's approval message + any issues raised and how they were resolved.
    - Final requirements traceability table.
  </action>
  <verify>
    1. `git log -n 1 --oneline` shows the closeout commit.
    2. `grep -n "Phase 43" .planning/ROADMAP.md` shows `[x]` and completion date.
    3. `grep -n "DASH-ACT-0" .planning/REQUIREMENTS.md` shows both requirements marked Complete.
    4. `grep -n "Phase 43 COMPLETE" .planning/STATE.md` returns the new Current Position block.
    5. `ls .planning/phases/43-dashboard-pending-tasks-view/` lists 43-CONTEXT.md, 43-01-PLAN.md, 43-01-SUMMARY.md, 43-02-PLAN.md, 43-02-SUMMARY.md, 43-03-PLAN.md, 43-03-SUMMARY.md.
  </verify>
  <done>
    All three closeout docs updated and committed (with `-f` since planning is gitignored). 43-03-SUMMARY.md exists. Phase 43 is officially complete.
  </done>
</task>

</tasks>

<verification>
Phase 43 is complete when all 9 owner-checkpoint items pass AND the three closeout files reflect the new status. If the v1.8 milestone's 5 phases are now all shipped, the ROADMAP.md milestone line flips too.
</verification>

<success_criteria>
- PM2 is running the new routes and the dashboard bundle includes the new page.
- Owner has confirmed all 9 verification items (or issues are recorded + resolved).
- `.planning/ROADMAP.md` marks Phase 43 complete with plan list.
- `.planning/REQUIREMENTS.md` marks DASH-ACT-01 + DASH-ACT-02 complete.
- `.planning/STATE.md` has a new Current Position block.
- `43-03-SUMMARY.md` records the verification outcomes and closeout.
</success_criteria>

<output>
Plan 43-03 SUMMARY at `.planning/phases/43-dashboard-pending-tasks-view/43-03-SUMMARY.md` must include:
- Baseline sqlite snapshot (Task 1 Step C).
- Curl outputs (auth-smoke).
- PM2 restart metadata (pid, uptime reset).
- Owner approval message verbatim.
- Any issues surfaced during the walk-through and how they were resolved.
- Requirements traceability confirmation (DASH-ACT-01 + DASH-ACT-02 → Complete).
- If applicable: v1.8 milestone closeout note.
</output>
