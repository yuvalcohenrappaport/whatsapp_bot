---
phase: 42-context-enrichment-at-approval
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/42-context-enrichment-at-approval/42-02-SUMMARY.md
autonomous: false
requirements: [ENRI-01, ENRI-02, ENRI-03, ENRI-04]

must_haves:
  truths:
    - "Bot is running on prod (PM2) with the Phase 42 code from 42-01 — pm2 list shows a running whatsapp-bot process with a restart timestamp AFTER the 42-01 commits landed"
    - "At least one non-user_command actionable has been approved via the owner's self-chat quoted-reply (✅) and its Google Tasks entry exists with an enriched title that is observably richer than the raw detected task (contains contact name AND/OR resolves a pronoun AND/OR includes a deadline)"
    - "The same actionable row in the `actionables` table has non-null enriched_title + enriched_note + todo_task_id + todo_list_id, and updatedAt on the row is AFTER detectedAt (proves enrichment ran at approval time, not detection time — SC#5)"
    - "At least one user_command actionable (self-chat `remind me to X at Y`) landed with enriched_title NULL or equal to actionable.task — NOT a Gemini-enriched string — proving the user_command short-circuit held (cross-check: no `enrichActionable` / Gemini log entry for that id)"
    - "Enrichment failure path (ENRI-04) is accepted via vitest coverage from 42-01 Task 3 (8 enrichmentService.test.ts cases) + either a naturally-occurring live fallback captured in logs, or the test coverage alone (acceptance matches Phase 41-05's pattern of accepting SC4/SC5 on vitest evidence)"
    - ".planning/REQUIREMENTS.md traceability table marks ENRI-01/02/03/04 as Complete with a short note pointing at the live evidence (taskId + actionable row id)"
    - ".planning/STATE.md updated with Phase 42 completion narrative"
    - ".planning/ROADMAP.md Phase 42 entry flipped to [x] Complete with 2/2 plans"
  artifacts:
    - path: ".planning/phases/42-context-enrichment-at-approval/42-02-SUMMARY.md"
      provides: "Live verification record with SC-by-SC evidence"
      contains: "SC verification status"
    - path: ".planning/STATE.md"
      provides: "Phase 42 completion narrative"
      contains: "Phase 42"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ENRI-01..04 status → Complete in traceability table"
      contains: "ENRI-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 42 checkbox flipped"
      contains: "Phase 42: Context Enrichment at Approval"
  key_links:
    - from: ".planning/phases/42-context-enrichment-at-approval/42-02-SUMMARY.md"
      to: "live prod DB"
      via: "sqlite SELECT on actionables row with todo_task_id non-null"
      pattern: "todo_task_id"
---

<objective>
Verify Phase 42 end-to-end on prod. Trigger (or observe organically) at least one detected-commitment approval in a non-self-chat and confirm that the Google Tasks entry has an enriched, self-contained title + rich note, while a user_command approval bypasses enrichment as designed. Flip ENRI-01..04 to Complete and close Phase 42.

Purpose: ship the last mile. Unit tests prove the module works; prod verification proves the integration works against real Gemini + real Google Tasks + real Baileys.

Output: live evidence captured in 42-02-SUMMARY.md, three planning docs updated (STATE / REQUIREMENTS / ROADMAP).
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-whatsapp-approval-ux/41-05-SUMMARY.md
@.planning/phases/42-context-enrichment-at-approval/42-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy Phase 42 to prod (PM2) and stage the verification environment</name>
  <files></files>
  <action>
Phase 41-05 verified the bot is running under PM2 pinned to Node 20 (commit f045cf9). This task re-deploys with Phase 42 code and stages the verification environment.

Steps:

1. Confirm 42-01 commits landed on the main branch:
   ```bash
   cd /home/yuval/whatsapp-bot && git log --oneline -10
   ```
   Expect to see commits from 42-01 (enrichmentService.ts + approvalHandler.ts wiring + tests).

2. Ensure the build passes:
   ```bash
   cd /home/yuval/whatsapp-bot && npm run build 2>&1 | tail -5
   ```
   If the bot runs tsx in PM2 (check `ecosystem.config.cjs` — Phase 41-05 pinned the Node 20 interpreter there), the `build` step may be optional. Confirm either way.

3. Restart PM2:
   ```bash
   pm2 restart whatsapp-bot --update-env
   pm2 list
   ```
   Note the new PID and uptime.

4. Tail logs to confirm clean boot:
   ```bash
   pm2 logs whatsapp-bot --lines 40 --nostream
   ```
   Look for: no `ERR_DLOPEN_FAILED` (Node-version regressions from 41-05), no `Gemini` / `enrichment` ERROR lines, normal `initApprovalSystem` boot sequence.

5. Capture baseline state from the DB for later comparison:
   ```bash
   sqlite3 /home/yuval/whatsapp-bot/data/whatsapp-bot.db "SELECT COUNT(*) FROM actionables WHERE status='approved' AND todo_task_id IS NOT NULL;"
   sqlite3 /home/yuval/whatsapp-bot/data/whatsapp-bot.db "SELECT COUNT(*) FROM actionables WHERE enriched_title IS NOT NULL;"
   ```
   Record the baseline. (If the DB path differs from `data/whatsapp-bot.db`, check `src/config.ts` or `src/db/client.ts` for the actual path.)

6. Note: if any required env var is missing (`GEMINI_API_KEY`, Google Tasks OAuth), enrichment will fall back to the detected task + basic note. The Phase 41-05 verification proved Google Tasks + Baileys + Gemini are all wired. No new env vars in Phase 42.
  </action>
  <verify>
- `pm2 list` shows whatsapp-bot process with `online` status and restart timestamp within the last minute
- `pm2 logs whatsapp-bot --lines 40 --nostream` shows no `[error]` or `ERR_` lines tied to enrichment / approval subsystems
- Baseline counts captured for before/after comparison
  </verify>
  <done>
- Bot restarted under PM2 with latest code including Phase 42 changes
- No error lines in the first minute of logs
- Baseline row counts captured in a scratch note for Task 3's comparison
  </done>
</task>

<task type="human-verify">
  <name>Task 2: Live SC walkthrough — approve a detected commitment + verify user_command skip</name>
  <files></files>
  <action>
Owner-driven verification. Phase 42 code is deployed to prod; now exercise the approval flow against real Gemini + real Google Tasks + real Baileys and capture evidence.

**Verification A — Enriched detected commitment (ENRI-01, ENRI-02, ENRI-03, SC#5):**

1. Trigger a detection in a non-self-chat. Two options:
   - **Organic:** Wait for the owner to receive a detectable commitment from a contact (e.g., "I'll send you the report tomorrow"). The 2-min debounce bucket will flush → preview arrives in self-chat.
   - **Manual:** From a non-owner WhatsApp contact (second device or a friend), send a chat with short-context preamble + a commitment-shaped message. Example: 3-4 messages referring to a "Q2 report" or similar, then: "I'll check with you on it tomorrow evening". Wait ~2 min for the preview.

2. In self-chat, quote-reply the preview with ✅ (or `approve` / `ok` / `✓`).

3. Confirm the self-chat confirmation message appears: `✅ Added: <the detected task>` (confirmation shows the raw task per the Phase 41 decision — enriched title lives in Google Tasks).

4. Open Google Tasks UI (tasks.google.com or the Tasks side-panel in Gmail). Locate the new task in the "WhatsApp Tasks" list.

5. Inspect the task title and note:
   - **Title:** self-contained, references the contact's name, includes a deadline if one was detected. Example expected shape: `Follow up with Lee about Q2 report by Tuesday evening` rather than the raw `Check it`.
   - **Note:** contains (a) contact name, (b) a snippet of the chat context, (c) the original trigger message.

6. Cross-check the DB:
   ```bash
   sqlite3 /home/yuval/whatsapp-bot/data/whatsapp-bot.db "SELECT id, task, enriched_title, substr(enriched_note, 1, 200) AS note, todo_task_id, todo_list_id, detected_at, updated_at FROM actionables WHERE status='approved' AND todo_task_id IS NOT NULL ORDER BY updated_at DESC LIMIT 3;"
   ```
   Confirm: the most recent row has non-null enriched_title + enriched_note + todo_task_id + todo_list_id AND updated_at > detected_at (SC#5 — task created at approval time, not detection time).

**Verification B — user_command skip (Phase 41 Q9A-1 constraint):**

1. In self-chat, type: `remind me to buy milk tomorrow at 9am` (EN) or a Hebrew equivalent. The existing reminderService handles this and dual-writes an approved actionable with source_type='user_command'.

2. Cross-check the DB:
   ```bash
   sqlite3 /home/yuval/whatsapp-bot/data/whatsapp-bot.db "SELECT id, source_type, task, enriched_title, enriched_note FROM actionables WHERE source_type='user_command' ORDER BY updated_at DESC LIMIT 3;"
   ```
   The most recent user_command row's `enriched_title` should be either NULL (reminderService dual-write path doesn't push to Google Tasks — reminders fire-path stays the source of truth per Phase 41-04) or equal to `task`. Crucially: it should NOT be a Gemini-enriched string — that would prove enrichment leaked into user_command.

3. Tail logs for the time window and confirm NO `enrichActionable` Gemini call was made for the user_command id:
   ```bash
   pm2 logs whatsapp-bot --lines 200 --nostream | grep -iE "enrich|gemini" | tail -20
   ```

**Verification C — Fallback path (ENRI-04):**

Accept via vitest coverage from 42-01 Task 3 (8 enrichmentService.test.ts cases cover every fallback path: null, safeParse fail, throw, empty title). If a live fallback occurs naturally during verification (check logs for `enrichment Gemini call threw` or `enrichment Zod parse failed`), capture it. No requirement to force a failure in prod — acceptance pattern mirrors Phase 41-05 SC4/SC5 acceptance-via-coverage.

**Report back with:**
1. The actionable.id + todo_task_id from Verification A
2. The enriched_title string (verbatim) + a 1-line assessment of whether it's "self-contained" vs the raw task
3. The note body (verbatim or truncated)
4. Confirmation that Verification B's user_command row has enriched_title either NULL or equal to task
5. Any error lines from pm2 logs in the verification window

If any verification fails:
- Describe the failure mode (enriched title missing, Google Tasks entry missing, createTodoTask error, etc.)
- Capture the actionable.id and the relevant log lines
- Stop and escalate — the orchestrator may spawn a `--gaps` plan to fix before closing Phase 42
  </action>
  <verification>
- [ ] Owner approved a non-user_command actionable via quoted-reply ✅
- [ ] Google Tasks entry exists with a self-contained title (observably richer than raw task) + a multi-line note containing contact name + chat snippet + original trigger message
- [ ] DB row shows enriched_title + enriched_note + todo_task_id + todo_list_id all populated AND updated_at > detected_at
- [ ] A user_command actionable has enriched_title either NULL or equal to task (no Gemini enrichment leak)
- [ ] No `enrichActionable` log entry for the user_command actionable's id
- [ ] No unexpected errors in pm2 logs during the verification window
  </verification>
</task>

<task type="auto">
  <name>Task 3: Close Phase 42 — write SUMMARY + update STATE/REQUIREMENTS/ROADMAP</name>
  <files>.planning/phases/42-context-enrichment-at-approval/42-02-SUMMARY.md, .planning/STATE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Using the evidence captured in Task 2's walkthrough, close Phase 42:

1. **Write 42-02-SUMMARY.md** following `/home/yuval/.claude/get-shit-done/templates/summary.md`. Use 41-05-SUMMARY.md as the reference shape:
   - Frontmatter: `phase`, `plan: 02`, `subsystem: enrichment`, `tags: [live-verification, production, gemini, google-tasks]`, `requires` (list 42-01 with provides), `provides`, `affects: [43 dashboard audit view]`, `tech-stack` (no new libs), `key-files` (no source-code files modified in this plan; only planning docs), `key-decisions` (list any in-session decisions), `requirements-completed: [ENRI-01, ENRI-02, ENRI-03, ENRI-04]`, `duration`, `tests`, `completed: <date>`
   - SC verification table (SC#1-SC#5 from the phase goal), each row: Verification method (live / vitest coverage), Result (✓ Passed / ✓ Accepted)
   - Live evidence block: actionable.id, todo_task_id, enriched_title verbatim, enriched_note verbatim (or truncated), timestamps showing detected_at < updated_at
   - user_command verification block: user_command actionable.id, confirmation that enriched_title is either NULL or = task, confirmation no Gemini call in logs for that id
   - Any issues encountered (expected: none, per Phase 41-05 pattern)
   - Self-Check: PASSED

2. **Update STATE.md** — append a new section under the current position narrative (free-form prose, matching existing style — STATE.md is NOT gsd-tools standard format per known_project_quirks):
   ```
   ## Phase 42 complete (YYYY-MM-DD)
   Context enrichment at approval shipped. Gemini `enrichActionable` reads the last ~10 messages from the source chat and produces a self-contained Google Tasks title + rich audit note, falling back to the raw task + basic note on any failure (Gemini error, empty response, Zod validation fail). user_command actionables (`/remind me`) bypass enrichment per Phase 41 Q9A-1.
   Live evidence: <actionable.id> → enriched title `<string>` pushed to Google Tasks task `<todo_task_id>`.
   Test coverage: 8 enrichmentService.test.ts cases + 2 new approvalHandler.test.ts cases = all fallback paths covered.
   Next: Phase 43 Dashboard Pending Tasks View.
   ```

3. **Update REQUIREMENTS.md** — in the "v1.8 Traceability" table, flip ENRI-01, ENRI-02, ENRI-03, ENRI-04 status from "Pending" to "Complete (<date> — <1-line evidence>)". Model after the ACT-01 / DETC-01 rows. Also flip the checkboxes under `### Context Enrichment (at approval)` from `[ ]` to `[x]`.

4. **Update ROADMAP.md**:
   - Phase 42 entry line ~108 (`- [ ] **Phase 42: Context Enrichment at Approval**`): flip `[ ]` → `[x]` and add a short completion tail matching the Phase 41 style
   - Progress table row for Phase 42 (row ~402): `| 42. Context Enrichment at Approval | v1.8 | 0 | Pending | — |` → `| 42. Context Enrichment at Approval | v1.8 | 2/2 | Complete | <date> |`
   - Add a Plans block under the Phase 42 detail (mirror Phase 41 shape):
     ```
     **Plans:** 2/2 plans complete
     Plans:
     - [x] 42-01-PLAN.md — Enrichment service + approvalHandler wiring + vitest (see `.planning/phases/42-context-enrichment-at-approval/42-01-SUMMARY.md`)
     - [x] 42-02-PLAN.md — Live prod verification + STATE/REQUIREMENTS/ROADMAP updates (see `.planning/phases/42-context-enrichment-at-approval/42-02-SUMMARY.md`)
     ```

5. **Commit.** `.planning/` is gitignored per known_project_quirks — use `git add -f`:
   ```bash
   cd /home/yuval/whatsapp-bot && git add -f .planning/phases/42-context-enrichment-at-approval/42-02-SUMMARY.md .planning/STATE.md .planning/REQUIREMENTS.md .planning/ROADMAP.md
   node /home/yuval/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(42): close Phase 42 — live-verified context enrichment at approval"
   ```
   If gsd-tools' commit helper can't stage the force-added files, fall back to a plain `git commit` with the same message.
  </action>
  <verify>
- `.planning/phases/42-context-enrichment-at-approval/42-02-SUMMARY.md` exists with SC verification table + live evidence
- `grep -n "ENRI-0" .planning/REQUIREMENTS.md` shows ENRI-01..04 marked Complete
- `grep -n "Phase 42" .planning/ROADMAP.md` shows `[x]` + Complete row
- `git log --oneline -3` shows the closure commit
  </verify>
  <done>
- All 4 planning docs updated coherently
- Closure commit landed on main
- ENRI-01..04 marked Complete in REQUIREMENTS.md traceability table with short evidence notes
- Phase 42 flipped to [x] in ROADMAP.md with 2/2 plans complete
  </done>
</task>

</tasks>

<verification>
Phase 42 is closed when:
1. `git log --oneline -5` shows the closure commit
2. `.planning/ROADMAP.md` shows `[x] Phase 42` with completion date and 2/2 plans
3. `.planning/REQUIREMENTS.md` traceability table shows ENRI-01..04 as Complete with evidence
4. `.planning/phases/42-context-enrichment-at-approval/42-02-SUMMARY.md` exists with live SC evidence
</verification>

<success_criteria>
Phase 42 goal: "On approval, a Gemini call that uses the last ~10 messages from the source chat produces a self-contained Google Tasks title plus a rich note, and Google Tasks receives the task at approval time instead of detection time."

All 5 phase SCs verified:
- SC#1 (approval → Gemini call with ~10 messages + structured schema): verified live in Verification A + vitest case 1 from 42-01
- SC#2 (title self-contained, contact name, deadline, pronoun resolution): verified live in Verification A + system-prompt rules + vitest case 1 from 42-01
- SC#3 (note: contact name + snippet + trigger text): verified live in Verification A + buildBasicNote format preserved for fallback
- SC#4 (enrichment failure → fallback, approval never blocked): vitest cases 4/5/6/7 from 42-01 + approveAndSync status flip ordering (status flipped before enrichment starts)
- SC#5 (Google Tasks entry created at approval time, taskId/listId stored on actionable row before confirmation sent): verified live via actionables.updated_at > detected_at + approveAndSync ordering (updateActionableTodoIds before sock.sendMessage)

Phase 42 is COMPLETE.
</success_criteria>

<output>
Task 3 itself creates the 42-02-SUMMARY.md file as part of the plan's output.
</output>
