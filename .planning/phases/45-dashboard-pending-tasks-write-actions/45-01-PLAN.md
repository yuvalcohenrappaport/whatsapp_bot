---
phase: 45-dashboard-pending-tasks-write-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/approval/approvalHandler.ts
  - src/db/queries/actionables.ts
  - src/approval/__tests__/approvalHandler.test.ts
  - src/db/queries/__tests__/actionables.test.ts
autonomous: true
requirements:
  - DASH-APP-01
  - DASH-APP-02

must_haves:
  truths:
    - "approvalHandler exports approveActionable(sock, actionable) that runs the Phase 42 enrichment → Google Tasks push → status flip → self-chat ✅ echo pipeline, callable from BOTH the quoted-reply handler AND an HTTP handler (sock is passed in, no global state)"
    - "approvalHandler exports rejectActionable(sock, actionable) that flips status pending_approval → rejected AND sends the symmetric ❌ Dismissed / ❌ בוטל self-chat echo, matching the CONTEXT lock that dashboard-initiated Reject echoes to WhatsApp just like quoted-reply Reject"
    - "approvalHandler exports unrejectActionable(sock, actionable, graceMs) that flips rejected → pending_approval IFF the row's updatedAt is within graceMs, otherwise throws a typed GraceExpiredError — SILENT on WhatsApp per CONTEXT (no compensating self-chat message)"
    - "actionables query layer allows the rejected → pending_approval transition (undo) in ALLOWED_TRANSITIONS, so updateActionableStatus doesn't throw on a valid unreject inside the grace window"
    - "tryHandleApprovalReply still funnels to the same approveActionable / rejectActionable exports (no behavior drift for the WhatsApp surface; both surfaces now share one code path)"
    - "Existing self-chat echoes (✅ Added / ✅ נוסף on approve, ❌ Dismissed / ❌ בוטל on reject) remain byte-identical for the quoted-reply path"
  artifacts:
    - path: "src/approval/approvalHandler.ts"
      provides: "approveActionable, rejectActionable, unrejectActionable exports (shared between WhatsApp quoted-reply and dashboard HTTP write routes)"
      exports: ["tryHandleApprovalReply", "approveActionable", "rejectActionable", "unrejectActionable", "GraceExpiredError", "buildBasicNote"]
    - path: "src/db/queries/actionables.ts"
      provides: "ALLOWED_TRANSITIONS extended with rejected → pending_approval (undo grace window); optional helper unrejectActionableIfFresh(id, graceMs) returning boolean for server-enforced grace check"
      contains: "rejected: ['pending_approval']"
    - path: "src/approval/__tests__/approvalHandler.test.ts"
      provides: "vitest cases for approveActionable + rejectActionable callable standalone (without the tryHandleApprovalReply wrapper) and unrejectActionable grace window"
      contains: "approveActionable\\|rejectActionable\\|unrejectActionable"
    - path: "src/db/queries/__tests__/actionables.test.ts"
      provides: "vitest case proving updateActionableStatus now accepts rejected → pending_approval"
      contains: "rejected.*pending_approval"
  key_links:
    - from: "src/approval/approvalHandler.ts tryHandleApprovalReply"
      to: "src/approval/approvalHandler.ts approveActionable + rejectActionable"
      via: "the quoted-reply applyDirective() calls the exported functions (not an inline private)"
      pattern: "approveActionable\\(|rejectActionable\\("
    - from: "src/approval/approvalHandler.ts approveActionable"
      to: "src/approval/enrichmentService.ts enrichActionable"
      via: "await enrichActionable(actionable) between status flip and Google Tasks push — byte-identical to the existing private approveAndSync"
      pattern: "enrichActionable\\("
    - from: "src/approval/approvalHandler.ts unrejectActionable"
      to: "src/db/queries/actionables.ts updateActionableStatus"
      via: "transition rejected → pending_approval now valid per extended ALLOWED_TRANSITIONS; GraceExpiredError thrown before the DB call when age > graceMs"
      pattern: "updateActionableStatus\\(.*'pending_approval'"
---

<objective>
Refactor `approvalHandler.ts` so the approve + reject + unreject primitives are exported functions (not private helpers) callable from both the existing WhatsApp quoted-reply path AND a new HTTP handler — the foundation for Plans 45-02 (write routes) and 45-03 (dashboard UI). Extend the actionables lifecycle to allow a bounded rejected → pending_approval transition for the 5s-window Reject Undo affordance.

Purpose: Plan 45-02 needs to call approve + reject from Fastify handlers that already have the live baileys `sock` via `getState().sock`. The existing `approveAndSync` function in `approvalHandler.ts:179` is module-private — extracting it into an exported `approveActionable(sock, actionable)` is the "cleaner factoring" option the CONTEXT lock lets the planner pick. A symmetric `rejectActionable` is needed because CONTEXT locks the Dashboard Reject echoing the same `❌ Dismissed` / `❌ בוטל` self-chat message — today the quoted-reply path inlines that echo next to the status flip (`applyDirective` lines 166–174), so we split it out so both surfaces share one implementation. `unrejectActionable` is brand-new: the server-enforced 5s grace window for Reject Undo requires one place that atomically validates "still rejected + within window" before flipping — the DB-level transition guard is extended to allow it, and the grace-window check lives in this module (not leaked to the route layer).

Output: One source file refactored (approvalHandler.ts — private `approveAndSync` → exported `approveActionable`; new exported `rejectActionable` + `unrejectActionable`; `GraceExpiredError` named class); one source file extended (actionables.ts — ALLOWED_TRANSITIONS adds `rejected: ['pending_approval']`); vitest coverage proving the exports work standalone AND that tryHandleApprovalReply still drives the same pipeline. Zero behavior change visible to the WhatsApp surface.
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-dashboard-pending-tasks-write-actions/45-CONTEXT.md

# Source of truth for existing semantics (DO NOT drift)
@src/approval/approvalHandler.ts
@src/approval/enrichmentService.ts
@src/approval/replyParser.ts
@src/db/queries/actionables.ts

# Existing test infra to mirror
@src/approval/__tests__/approvalHandler.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ALLOWED_TRANSITIONS + export approve/reject/unreject primitives</name>
  <files>
    src/db/queries/actionables.ts
    src/approval/approvalHandler.ts
  </files>
  <action>
    **Step A — `src/db/queries/actionables.ts`:** Extend the `ALLOWED_TRANSITIONS` table at line 22 so unreject is a valid DB-level transition:

    ```ts
    const ALLOWED_TRANSITIONS: Record<ActionableStatus, ActionableStatus[]> = {
      pending_approval: ['approved', 'rejected', 'expired'],
      approved: ['fired'],
      rejected: ['pending_approval'], // undo within the server-enforced grace window (Phase 45)
      fired: [],
      expired: [],
    };
    ```

    `updateActionableStatus` already no-ops on same-status and throws on invalid transitions — no other change is needed. DO NOT add a new query helper; the grace-window check lives in `approvalHandler.ts` (Step B) so it can reach `actionable.updatedAt` without a second DB round-trip.

    **Step B — `src/approval/approvalHandler.ts`:** Three edits inside the existing file.

    **Edit B.1 — Rename private `approveAndSync` → exported `approveActionable` at line 179.**
    - Signature: `export async function approveActionable(sock: WASocket, actionable: Actionable): Promise<void>`.
    - Body is byte-identical to the current `approveAndSync` (lines 182–218): flip status first, `await enrichActionable(actionable)`, persist enrichment, push to Google Tasks gated on `isTasksConnected()`, swallow Google Tasks errors, send the `✅ Added` / `✅ נוסף` self-chat echo. No semantic drift.
    - The existing internal caller on line 162 (`await approveAndSync(sock, actionable)` inside `applyDirective`) is rewired to call `approveActionable(sock, actionable)` instead.

    **Edit B.2 — Extract the reject echo into an exported `rejectActionable`.**
    - Signature: `export async function rejectActionable(sock: WASocket, actionable: Actionable): Promise<void>`.
    - Body mirrors lines 167–173 (the existing inline reject branch in `applyDirective`):
      ```ts
      updateActionableStatus(actionable.id, 'rejected');
      await sock.sendMessage(config.USER_JID, {
        text: rejectedConfirmation(
          (actionable.detectedLanguage ?? 'en') as 'he' | 'en',
        ),
      });
      ```
    - Inside `applyDirective`, replace the inline block at lines 166–174 with `await rejectActionable(sock, actionable);`.
    - `rejectedConfirmation` is already a file-private helper — stays private. No need to export.

    **Edit B.3 — Add `unrejectActionable` + `GraceExpiredError`.**
    ```ts
    export class GraceExpiredError extends Error {
      constructor(msg: string) {
        super(msg);
        this.name = 'GraceExpiredError';
      }
    }

    /**
     * Flip a `rejected` actionable back to `pending_approval` IFF `updatedAt`
     * is within `graceMs`. Silent on WhatsApp per CONTEXT §Undo for Reject —
     * no compensating self-chat message is sent; the original ❌ Dismissed
     * echo (if already delivered) stays visible.
     *
     * Throws:
     *   - GraceExpiredError when the grace window has closed
     *   - Error from updateActionableStatus when the row is no longer in
     *     status='rejected' (e.g. a concurrent WhatsApp re-handle) — caller
     *     should surface as "already handled" UX
     */
    export async function unrejectActionable(
      _sock: WASocket,
      actionable: Actionable,
      graceMs: number,
    ): Promise<void> {
      const age = Date.now() - actionable.updatedAt;
      if (age > graceMs) {
        throw new GraceExpiredError(
          `unreject grace window expired (age ${age}ms > ${graceMs}ms)`,
        );
      }
      updateActionableStatus(actionable.id, 'pending_approval');
      // Deliberate: NO sock.sendMessage here. CONTEXT §Undo lock is "silent in WhatsApp".
    }
    ```
    The `_sock` parameter is kept for signature symmetry with approve/reject (callers shouldn't need to know which primitives speak to WhatsApp and which don't) — the `_` prefix signals "unused but intentional".

    Also export `buildBasicNote` (already exported at line 220 — verify no-op).

    **Do NOT change:**
    - `tryHandleApprovalReply` public signature or return value.
    - `parseApprovalReply`, `expandAllDirectives`, `dedupeLastWins` semantics.
    - `approvedConfirmation`, `rejectedConfirmation`, `alreadyHandledMessage`, `sendGrammarHint`, `pickHintLanguage` (all stay file-private).
    - The current ordering in `approveActionable`: `updateActionableStatus('approved')` FIRST, then enrich, then Google Tasks push, then echo. The comment on line 183 already explains why — enrichment failure must never block the status flip.

    **Verify:**
    - `npx tsc --noEmit` — zero new errors (pre-existing cli/bot.ts + cli/commands/persona.ts rootDir noise tolerated per STATE.md).
    - `grep -n "approveActionable\|rejectActionable\|unrejectActionable\|GraceExpiredError" src/approval/approvalHandler.ts` — four new exports visible.
    - `grep -n "rejected: \['pending_approval'\]" src/db/queries/actionables.ts` — transition entry present.
  </action>
  <verify>
    1. `npx tsc --noEmit` — zero new errors.
    2. Four new exports visible via grep in approvalHandler.ts.
    3. `rejected: ['pending_approval']` entry visible via grep in actionables.ts.
    4. `tryHandleApprovalReply` still exported with unchanged signature (grep to confirm).
  </verify>
  <done>
    approvalHandler.ts exports approveActionable, rejectActionable, unrejectActionable, GraceExpiredError (plus the existing tryHandleApprovalReply + buildBasicNote). actionables.ts ALLOWED_TRANSITIONS now permits rejected → pending_approval. Zero tsc regressions. No visible WhatsApp-surface behavior change (verified in Task 2).
  </done>
</task>

<task type="auto">
  <name>Task 2: vitest coverage for the new exports + transition</name>
  <files>
    src/approval/__tests__/approvalHandler.test.ts
    src/db/queries/__tests__/actionables.test.ts
  </files>
  <action>
    **Step A — extend `src/approval/__tests__/approvalHandler.test.ts`:** Add four new cases alongside the existing suite (keep all 15 current cases green):

    1. `approveActionable` called directly (not via `tryHandleApprovalReply`) on a `pending_approval` actionable → flips to `approved` in DB, calls `enrichActionable` once, calls `createTodoTask` with the enriched title + note, calls `updateActionableTodoIds` with the result, sends `✅ Added: <task>` to `config.USER_JID`.
    2. `rejectActionable` called directly on a `pending_approval` actionable → flips to `rejected`, sends `❌ Dismissed` to `config.USER_JID`. Mirror with a Hebrew row proving `❌ בוטל`.
    3. `unrejectActionable` called within the grace window (e.g. actionable.updatedAt = Date.now() - 2000, graceMs = 5000) → `updateActionableStatus` called with `'pending_approval'`, `sock.sendMessage` NOT called (silent per CONTEXT).
    4. `unrejectActionable` called outside the grace window (actionable.updatedAt = Date.now() - 10000, graceMs = 5000) → throws `GraceExpiredError`, `updateActionableStatus` NOT called, `sock.sendMessage` NOT called.

    Mock `enrichActionable`, `createTodoTask`, `isTasksConnected`, plus the DB query helpers — the existing suite already mocks these for `tryHandleApprovalReply`; reuse the same setup so the four new cases fit into the same describe block.

    **Step B — extend `src/db/queries/__tests__/actionables.test.ts`:** Add two new cases:

    1. `updateActionableStatus(id, 'pending_approval')` on a row in status `rejected` → succeeds (no throw); subsequent `getActionableById(id).status === 'pending_approval'`.
    2. `updateActionableStatus(id, 'approved')` on a row in status `rejected` → throws (only pending_approval is reachable from rejected).

    If `src/db/queries/__tests__/actionables.test.ts` does NOT exist yet (STATE.md notes the `better-sqlite3` ABI has historically blocked these tests from running locally in CI), check first via Read. If the file exists but the suite is skipped, ADD the cases behind the same skip guard — don't un-skip. If the file does not exist, create a minimal suite with only these two cases under the same skip guard convention used by other DB tests in the repo (e.g. `describe.skipIf(!process.env.RUN_DB_TESTS)` or the pattern already in `src/db/queries/__tests__/`). Scan existing tests via `ls src/db/queries/__tests__/` to match convention.

    **Do NOT run the full vitest suite** — STATE.md lists 15 pre-existing failures. Run only the two modified files:
    - `npx vitest run src/approval/__tests__/approvalHandler.test.ts` — expect 19/19 green (15 existing + 4 new).
    - `npx vitest run src/db/queries/__tests__/actionables.test.ts` — expect prior cases + 2 new green (or both new cases skipped-and-green if behind the skip guard).

    **Commit order (3 atomic commits expected):**
    - `feat(45-01): allow rejected → pending_approval transition for undo grace window`
    - `refactor(45-01): extract approve/reject/unreject primitives from approvalHandler`
    - `test(45-01): vitest for approveActionable + rejectActionable + unrejectActionable + unreject transition`

    Reminder: `.planning/` is gitignored — use `git add -f` when committing plan/summary docs (per STATE.md convention, same pattern used by Plans 43-01..43-03 and 44-01..44-06).
  </action>
  <verify>
    1. `npx tsc --noEmit` — zero new errors.
    2. `npx vitest run src/approval/__tests__/approvalHandler.test.ts` — 19/19 green (15 existing + 4 new).
    3. `npx vitest run src/db/queries/__tests__/actionables.test.ts` — prior cases pass + 2 new cases pass (or conditionally skipped with the same guard).
    4. `grep -n "approveActionable\|rejectActionable\|unrejectActionable\|GraceExpiredError" src/approval/__tests__/approvalHandler.test.ts` — four identifiers referenced.
  </verify>
  <done>
    19/19 approvalHandler tests green (4 new). Transition test green or conditionally skipped. Three atomic commits on the feature branch. Zero new tsc errors.
  </done>
</task>

</tasks>

<verification>
Phase-level check after both tasks:
- `grep -n "export async function approveActionable\|export async function rejectActionable\|export async function unrejectActionable\|export class GraceExpiredError" src/approval/approvalHandler.ts` — four match lines.
- `grep -n "tryHandleApprovalReply" src/approval/approvalHandler.ts` — signature unchanged.
- `grep -n "rejected: \['pending_approval'\]" src/db/queries/actionables.ts` — transition entry.
- Manual code read: `applyDirective` in approvalHandler.ts now delegates to `approveActionable` + `rejectActionable` (no inline duplicated status flip or send).
- Full approval suite still green (baseline was 91/91 after Phase 42 — expect 95/95 now).
</verification>

<success_criteria>
- approvalHandler.ts exports approveActionable, rejectActionable, unrejectActionable, GraceExpiredError.
- `applyDirective` delegates to the new exports — zero duplicated logic between the WhatsApp surface and the future HTTP surface.
- ALLOWED_TRANSITIONS allows rejected → pending_approval.
- tryHandleApprovalReply unchanged behavior for the WhatsApp surface (confirmed by all 15 existing tests still green).
- 4 new approvalHandler tests green (95/95 total approval suite).
- Feeds DASH-APP-01 (approve + reject primitives available to an HTTP route) + DASH-APP-02 (approveActionable IS the Phase 42 enrichment path — calling it from HTTP automatically inherits Gemini enrichment + Google Tasks sync + safe fallback).
</success_criteria>

<output>
After completion, create `.planning/phases/45-dashboard-pending-tasks-write-actions/45-01-SUMMARY.md` documenting:
- Files touched + line deltas.
- Commit hashes (3 atomic commits expected).
- vitest counts: approvalHandler 19/19, actionables query 2/2 new (or skip-guarded count).
- Any deviations (e.g. if rejectedConfirmation was surfaced as an export because a test needed it — document why).
- Confirmation the WhatsApp surface is unchanged (grep on applyDirective showing the delegated calls).
- Hand-off note for Plan 45-02: `approveActionable(sock, actionable)`, `rejectActionable(sock, actionable)`, `unrejectActionable(sock, actionable, graceMs)` are ready to call from Fastify handlers; sock comes from `getState().sock` in `src/api/state.ts`.
- Confirmation `.planning/` docs were added via `git add -f` per STATE.md convention.
</output>
</content>
</invoke>