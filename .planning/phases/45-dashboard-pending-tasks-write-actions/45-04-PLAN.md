---
phase: 45-dashboard-pending-tasks-write-actions
plan: 04
type: execute
wave: 4
depends_on:
  - "45-01"
  - "45-02"
  - "45-03"
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - DASH-APP-01
  - DASH-APP-02
  - DASH-APP-03

must_haves:
  truths:
    - "Live PM2 whatsapp-bot process serves the Phase 45 code: approvalHandler primitives exported, four new /api/actionables/:id/{approve|reject|edit|unreject} routes live, dashboard bundle serves the updated /pending-tasks page with the Approve/Reject/Edit buttons"
    - "SC#1 verified live: every pending_approval row on /pending-tasks renders ✅ Approve + ✏️ Edit + ❌ Reject buttons in a single horizontal row at card bottom, always visible"
    - "SC#2 verified live: clicking Approve on a pending row vanishes the card optimistically; within 3s the row re-materializes in the Recent section with an enriched title (Phase 42 Gemini enrichment) AND the self-chat has a new ✅ Added / ✅ נוסף echo AND Google Tasks shows the new task"
    - "SC#3 verified live: clicking Reject on a pending row vanishes the card + fires a 5s sonner toast with Undo; row appears in Recent as rejected within 3s; self-chat has ❌ Dismissed / ❌ בוטל echo; clicking Undo within 5s flips the row back to Pending silently (no WhatsApp message); clicking Undo past the 10s server grace yields a neutral 'Undo window closed' toast"
    - "SC#4 verified live: clicking Edit on a pending row morphs the card in place (textarea with correct dir=he/en, source snippet + contact + timestamp stay visible); typing new text and clicking Save & Approve OR Cmd/Ctrl+Enter flips the row to approved with the NEW task text as the source for enrichment, Google Tasks gets the enriched + edited title, single self-chat ✅ Added echo with the edited title (matches WhatsApp `edit:` grammar)"
    - "SC#5 verified live: concurrent WhatsApp quoted-reply on the same row while the dashboard has the row open — whichever request the server processes first wins; the other surface receives a 409 already_handled response, surfaces the 'Already handled in WhatsApp' toast, and does NOT rollback the row (end state is correct); Google Tasks has exactly ONE entry; self-chat has exactly ONE echo"
    - "ROADMAP.md line 112 Phase 45 checkbox flipped to [x]; ROADMAP.md Progress table row 'Phase 45' status → Complete; REQUIREMENTS.md v1.9 Traceability rows DASH-APP-01..03 → Complete with date + evidence note; STATE.md Current Position block advanced to Phase 46 as next"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 45 checkbox flipped + Progress table row updated + plan count finalized (N/N plans complete)"
      contains: "Phase 45: Dashboard Pending-Tasks Write Actions.*Complete"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DASH-APP-01, DASH-APP-02, DASH-APP-03 flipped to Complete in the v1.9 Traceability table"
      contains: "DASH-APP-01.*Complete\\|DASH-APP-02.*Complete\\|DASH-APP-03.*Complete"
    - path: ".planning/STATE.md"
      provides: "Current Position entry for Phase 45 complete + Phase 46 as next target"
      contains: "Phase 45.*complete\\|Phase 45 COMPLETE"
  key_links:
    - from: "Browser /pending-tasks Approve button"
      to: "POST /api/actionables/:id/approve → approveActionable → Gemini enrichment → Google Tasks API → WhatsApp self-chat echo"
      via: "CONTEXT-locked cross-surface parity: dashboard-initiated approve produces the same artifacts as WhatsApp quoted-reply approve"
      pattern: "approve → enrich → push → echo"
    - from: "Browser /pending-tasks Reject button + Undo toast"
      to: "POST /reject → rejectActionable (echo) + POST /unreject (silent, server-graced)"
      via: "5s client toast + 10s server grace window on unreject"
      pattern: "reject → echo; unreject → silent"
---

<objective>
Ship Phase 45 to production and verify all 5 success criteria against live data on the real bot. Then flip Phase 45 to complete across ROADMAP + REQUIREMENTS + STATE.

Purpose: Plans 45-01 through 45-03 delivered the code. This plan exercises the cross-surface-parity invariant: a dashboard-initiated Approve / Reject / Edit produces the SAME WhatsApp echo, the SAME enrichment path, the SAME Google Tasks entry as a WhatsApp quoted-reply on the same row would. Because that invariant is what DASH-APP-01..03 actually demand, live verification IS the acceptance bar — no amount of vitest coverage substitutes.

Output: A running whatsapp-bot PM2 process with the four new write routes + refactored approvalHandler primitives live, a deployed dashboard bundle with the updated PendingTasks page, three updated planning docs reflecting Phase 45 closure and Phase 46 as the next target.
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-dashboard-pending-tasks-write-actions/45-CONTEXT.md
@.planning/phases/45-dashboard-pending-tasks-write-actions/45-01-SUMMARY.md
@.planning/phases/45-dashboard-pending-tasks-write-actions/45-02-SUMMARY.md
@.planning/phases/45-dashboard-pending-tasks-write-actions/45-03-SUMMARY.md

# Pattern — prior phases that did the same PM2 restart + owner walk-through
@.planning/phases/43-dashboard-pending-tasks-view/43-03-PLAN.md
@.planning/phases/43-dashboard-pending-tasks-view/43-03-SUMMARY.md
@.planning/phases/44-unified-editable-calendar/44-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + deploy + auth-smoke every new route</name>
  <files>
    dashboard/dist
    logs/bot-error.log
  </files>
  <action>
    **Step A — server build:**
    - `cd /home/yuval/whatsapp-bot && npx tsc --noEmit` — tolerates pre-existing cli/bot.ts + cli/commands/persona.ts rootDir noise per STATE.md convention.
    - `npx vitest run src/approval/__tests__/approvalHandler.test.ts src/api/__tests__/actionables.test.ts src/api/__tests__/actionablesWriteActions.test.ts` — expect full green:
      - approvalHandler: 19/19 (15 existing + 4 new from Plan 45-01)
      - actionables: 10/10 (from Plan 43-01, unchanged)
      - actionablesWriteActions: 15/15 (from Plan 45-02)
      The 15 pre-existing failing tests (STATE.md) are tolerated; DO NOT run the full suite.

    **Step B — dashboard build:**
    - `cd /home/yuval/whatsapp-bot/dashboard && npx vite build` — deliberately `npx vite build` not `npm run build` per STATE.md convention (same tolerance used by Plans 43-02 / 44-04 / 44-06).
    - Record final bundle sizes. Expected delta vs Plan 44-06 baseline: +10-20 kB (one new hook + one extracted component + textarea primitive reuse; no new dep).

    **Step C — DB baseline snapshot BEFORE restart:**
    ```bash
    sqlite3 data/bot.db "SELECT status, COUNT(*) FROM actionables GROUP BY status;"
    sqlite3 data/bot.db "SELECT COUNT(*) FROM actionables WHERE status='pending_approval';"
    sqlite3 data/bot.db "SELECT id, task, detected_language FROM actionables WHERE status='pending_approval' ORDER BY detected_at DESC LIMIT 3;"
    ```
    Record the three values into the SUMMARY as the "pre-deploy baseline".

    **Step D — whatsapp-bot PM2 restart:**
    - `pm2 restart whatsapp-bot` (NOT `--update-env` — STATE.md Phase 41-05 lesson: `--update-env` has historically tripped a `better-sqlite3 NODE_MODULE_VERSION` mismatch. The Node 20 pin in `ecosystem.config.cjs` from commit `f045cf9` is stable).
    - `pm2 logs whatsapp-bot --lines 80 --nostream`. Confirm:
      - `Approval system initialized` (Phase 41 init still fires)
      - No better-sqlite3 mismatches
      - No new errors in `logs/bot-error.log` within 30s

    **Step E — auth-smoke every new endpoint:**
    ```bash
    JWT=$(curl -s -X POST http://localhost:3000/api/auth/login -H "Content-Type: application/json" -d '{"password":"<owner provides>"}' | jq -r .token)

    # Sanity — existing read still works
    curl -s "http://localhost:3000/api/actionables/pending" -H "Authorization: Bearer $JWT" | jq '.actionables | length'

    # 401 smokes — no JWT
    curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:3000/api/actionables/fake-id/approve   # expect 401
    curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:3000/api/actionables/fake-id/reject    # expect 401
    curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:3000/api/actionables/fake-id/edit      # expect 401
    curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:3000/api/actionables/fake-id/unreject  # expect 401

    # 404 smoke — JWT but nonexistent id
    curl -s -X POST http://localhost:3000/api/actionables/does-not-exist/approve \
      -H "Authorization: Bearer $JWT" | jq .  # expect {"error":"Actionable not found"} @ 404

    # 400 smoke — edit with empty body
    curl -s -X POST http://localhost:3000/api/actionables/some-id/edit \
      -H "Authorization: Bearer $JWT" -H "Content-Type: application/json" \
      -d '{"task":"   "}' | jq .  # expect {"error":"task is required"} @ 400

    # Optional: smoke a real approve (WARNING: this produces a self-chat echo +
    # Gemini enrichment + Google Tasks entry — only run if a DEV pending row is
    # around OR you're willing to own the side-effects on a real row).
    ```
    Record all HTTP status codes + response bodies into SUMMARY.

    **Step F — bundle serve verification:**
    Curl the dashboard SPA: `curl -s http://localhost:3000/ | grep -c '<script.*/assets/index-'` → 1 (bundle served; bot's static handler picks up `dashboard/dist/` automatically — no separate dashboard process to restart).
  </action>
  <verify>
    1. All three vitest files green (approvalHandler 19/19, actionables 10/10, actionablesWriteActions 15/15).
    2. vite build succeeds, bundle chunk `PendingTasks` present.
    3. PM2 log shows clean boot + `Approval system initialized`.
    4. All four auth-smoke 401 checks return 401.
    5. 404 smoke returns `{"error":"Actionable not found"}` @ 404.
    6. 400 smoke returns `{"error":"task is required"}` @ 400.
    7. Static handler serves the fresh bundle (grep matches on HTML root).
  </verify>
  <done>
    Phase 45 backend + frontend shipped to PM2 + dashboard/dist; auth-smoke proves every new route is reachable and returns the right typed envelopes; the bot boots clean with the refactored approvalHandler primitives. Owner-facing walkthrough can begin.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner walkthrough of all 5 success criteria against live data</name>
  <files></files>
  <action>
    This is a human-verify checkpoint. The executor pauses here and presents the following walkthrough script to the owner. Do NOT proceed to Task 3 until the owner types "approved" or describes gaps.

    **What was built (Plans 45-01 / 45-02 / 45-03 shipped to PM2):** approvalHandler exports `approveActionable` + `rejectActionable` + `unrejectActionable`; four new JWT-gated POST routes on `/api/actionables/:id/{approve|reject|edit|unreject}`; `/pending-tasks` page now has Approve / Reject / Edit buttons with optimistic removal, inline edit card-morph, 5s Reject Undo toast, and 409 already_handled → neutral toast with no rollback. PM2 restarted, dashboard bundle served, auth smoke all green.

    **Walkthrough script — SC#1 through SC#5:**

    **SC#1 — buttons render (visual)**
    1. Open the dashboard at `http://<host>:3000/pending-tasks` (or the owner's usual URL).
    2. Confirm every pending row shows three buttons in a row at card bottom: ✅ Approve / ✏️ Edit / ❌ Reject. Buttons always visible (not on hover, not in a kebab). English rows LTR, Hebrew rows RTL — but the button row itself is LTR in both cases (✅ first left-to-right).

    **SC#2 — dashboard Approve = WhatsApp quoted-reply Approve**
    3. Pick a pending_approval row, click ✅ Approve.
    4. Observe: card vanishes instantly.
    5. Within 3s observe: the row appears in the Recent section with the enriched title (a self-contained title, not the raw detected task).
    6. Open the owner's WhatsApp self-chat. Observe: a new ✅ Added / ✅ נוסף message with the task text (matches the language of the row).
    7. Open Google Tasks at `https://tasks.google.com/`. Observe: a new task with the enriched title + rich note (contact name, snippet, original message).
    → This is the Phase 42 enrichment + sync pipeline triggered from the dashboard, the whole point of DASH-APP-02.

    **SC#3 — Reject + Undo**
    8. Pick a different pending row. Click ❌ Reject.
    9. Observe: card vanishes; sonner toast appears at the bottom/top reading `Rejected: "<task>" — Undo` with a visible 5s countdown.
    10. Open self-chat. Observe: ❌ Dismissed / ❌ בוטל message appears.
    11. Before the 5s toast dismisses, click Undo. Observe: the row reappears in Pending (it flipped back to status='pending_approval' on the server).
    12. Open self-chat. Observe: NO new message was added from the Undo click (silent unreject per CONTEXT).
    13. Repeat Reject on the same row. Wait past 5s for the toast to dismiss. Wait another 5-10s. Attempt to click Undo (if still visible — depending on toast behavior it may already be gone). Expected: either the toast is already gone, OR clicking Undo yields a neutral toast "Undo window closed — it's already final" because the 10s server grace has expired.

    **SC#4 — Edit**
    14. Pick a different pending row (Hebrew if one exists, otherwise English). Click ✏️ Edit.
    15. Observe: the task headline turns into a textarea, still containing the current task text; dir matches the row language. The source snippet, contact name, and timestamp remain visible above. Buttons change to Cancel + Save & Approve.
    16. Press Esc. Observe: the card returns to read-only state with the original task text.
    17. Click Edit again. Type a short edit (e.g. append " - tomorrow"). Press Cmd+Enter (or Ctrl+Enter).
    18. Observe: card vanishes optimistically. Within 3s the row appears in Recent with the enriched title (NOTE: enrichment was run on the EDITED task text, so the enriched title should reflect the edit).
    19. Open self-chat. Observe: a single ✅ Added / ✅ נוסף echo with the EDITED task text (not the original). Matches the WhatsApp `edit:` grammar lock.
    20. Open Google Tasks. Observe: one new task with the edited+enriched title.

    **SC#5 — Concurrent WhatsApp race (optional but high-value)**
    21. Ideally: phone WhatsApp in one hand, dashboard in the other. Pick a pending row.
    22. On the dashboard, click ✅ Approve, and within ~1 second on WhatsApp quoted-reply ✅ on the preview for the same actionable.
    23. Observe: exactly ONE Google Tasks entry appears, exactly ONE ✅ Added echo in self-chat. One surface succeeds normally; the other gets a toast `Already handled in WhatsApp` (on the dashboard side) and the row stays gone on both surfaces. No rollback, no double-push.

    **If any SC fails:** describe the failure in detail. The planner will spawn a gap-closure plan via `/gsd:plan-phase 45 --gaps`. Task 3 does NOT fire on a failed UAT.

    **Resume signal:** Owner types "approved" (covering all 5 SCs) OR describes specific failures per SC. Partial approvals are accepted provided the checker annotates which SC was verified against live data vs code inspection (mirrors Phase 41-05 + Phase 43-03 hybrid-evidence precedent in STATE.md).
  </action>
  <verify>
    Owner explicitly approves (all 5 SCs covered directly or via hybrid code-inspection acceptance) OR provides a failure description per SC. In case of hybrid acceptance, the SUMMARY MUST annotate per-SC which evidence type (live vs inspection) was used.
  </verify>
  <done>
    Owner has walked through all 5 SCs and either approved or enumerated gaps. Task 3 fires only on "approved"; on gap enumeration, the orchestrator spawns `/gsd:plan-phase 45 --gaps` and Plan 45-04 pauses.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip ROADMAP + REQUIREMENTS + STATE to reflect Phase 45 closure</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </files>
  <action>
    **Step A — update `.planning/ROADMAP.md`:**
    1. Line 112 (`- [ ] **Phase 45: Dashboard Pending-Tasks Write Actions** ...`) → flip `[ ]` → `[x]` and append `(completed 2026-04-20)` or actual date.
    2. The "Phase Details" block for Phase 45 (around line 427-437): add a trailing line `**Plans:** N/N complete` and a `Plans:` subsection matching the pattern used by Phase 41 / 43 — one checkbox line per plan with plan name + commit-SUMMARY reference (e.g. `- [x] 45-01-PLAN.md — Refactor approvalHandler primitives + unreject transition (see 45-01-SUMMARY.md)`).
    3. Progress table (around line 421): update `| 45. Dashboard Pending-Tasks Write Actions | v1.9 | 0/? | Not started | — |` → `| 45. Dashboard Pending-Tasks Write Actions | v1.9 | 4/4 | Complete | 2026-04-20 |` (or whatever date is current per `date +%F`).
    4. Do NOT flip the v1.9 milestone line (line 14: `**v1.9 Dashboard Expansion**`) — that's Phase 49's job. Plans 45-49 are still pending even after Phase 45 ships.

    **Step B — update `.planning/REQUIREMENTS.md`:**
    In the "v1.9 Traceability" table (around lines 190-207):
    ```
    | DASH-APP-01 | Phase 45 | Not started |
    | DASH-APP-02 | Phase 45 | Not started |
    | DASH-APP-03 | Phase 45 | Not started |
    ```
    → flip to:
    ```
    | DASH-APP-01 | Phase 45 | Complete (2026-04-20 — /pending-tasks page renders Approve/Reject/Edit per pending row; four JWT-gated POST routes at /api/actionables/:id/{approve|reject|edit|unreject}; see 45-04-SUMMARY.md) |
    | DASH-APP-02 | Phase 45 | Complete (2026-04-20 — dashboard Approve invokes approveActionable (exported from Plan 45-01), which runs Phase 42 Gemini enrichment + Google Tasks push with the safe fallback inherited from Phase 42; owner walkthrough confirmed enriched title in Recent + Google Tasks UI) |
    | DASH-APP-03 | Phase 45 | Complete (2026-04-20 — POST /edit rewrites task text via updateActionableTask, then falls through to approveActionable — single ✅ echo matches the WhatsApp `edit:` grammar; SSE via the existing 3s hash-poll delivers the terminal-state row to all open sessions within ~3s) |
    ```

    Also flip each `- [ ] **DASH-APP-0X**:` line in the v1.9 Requirements section at the top (around lines 11-14) → `- [x]`.

    **Step C — update `.planning/STATE.md`:**
    Prepend a new Current Position entry at the top of the existing entries (keep the old ones for history), matching the verbose style of Phase 43-03 / 44-06 closeouts:

    ```
    ## Current Position

    Milestone: v1.9 Dashboard Expansion — **IN PROGRESS** (Phase 45 complete; Phases 46-49 pending)
    Phase: 45 Dashboard Pending-Tasks Write Actions — **COMPLETE** (Plans 45-01..45-04 shipped 2026-04-20)
    Plan: 45-04 shipped (2026-04-20). Live verification + closeout.
    Status: **Plan 45-04 COMPLETE — Phase 45 COMPLETE.** Owner UAT walked through all 5 SCs against live data — [describe outcome per actual walkthrough, cite which SCs were directly observed vs accepted on hybrid evidence per 41-05/43-03 precedent]. Bundle size delta vs 44-06: +<X> kB. PM2 restarted clean (pid <N>, zero errors in logs/bot-error.log within 30s of restart). DASH-APP-01/02/03 flipped to Complete in REQUIREMENTS.md traceability. ROADMAP Phase 45 row flipped to 4/4 Complete. v1.9 milestone NOT flipped (Phases 46-49 pending). Next target: Phase 46 Google Tasks Full-List Sync.
    ```

    Fill in the placeholder `[describe outcome ...]`, `<X>`, `<N>` with real values from the walkthrough in Task 2 + SUMMARY from 45-02 / 45-03.

    **Step D — commit:**
    ```bash
    git add -f .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md
    git commit -m "docs(45): flip Phase 45 to complete — DASH-APP-01..03 shipped + live verified"
    ```
    The `-f` is required because `.planning/` is gitignored (STATE.md convention).

    **Step E — write SUMMARY:**
    Create `.planning/phases/45-dashboard-pending-tasks-write-actions/45-04-SUMMARY.md` capturing:
    - PM2 pid + restart time + log excerpt showing clean boot
    - DB baseline snapshot (pre + post restart counts by status)
    - Auth smoke output for all four new routes (401/404/400)
    - SC-by-SC walkthrough notes (which were directly observed vs hybrid-accepted)
    - Any Rule-3 Blocking deviations (e.g. `npx vite build` vs `npm run build`, `pm2 restart` without `--update-env` — both follow STATE.md convention)
    - Commit hash of the closeout commit
    - Gap list (if any SC failed and needs `/gsd:plan-phase 45 --gaps`)
    - Next phase target: Phase 46 Google Tasks Full-List Sync
  </action>
  <verify>
    1. `grep -n "Phase 45.*Complete\|- \[x\] \*\*Phase 45" .planning/ROADMAP.md` — checkbox flipped + Progress table row updated.
    2. `grep -n "DASH-APP-01.*Complete\|DASH-APP-02.*Complete\|DASH-APP-03.*Complete" .planning/REQUIREMENTS.md` — all three traceability rows flipped.
    3. `grep -n "Phase 45.*COMPLETE\|Phase 46.*Next\|Phase 46 Google Tasks" .planning/STATE.md` — new Current Position entry present.
    4. `git log --oneline -5` — closeout commit visible.
    5. `.planning/phases/45-dashboard-pending-tasks-write-actions/45-04-SUMMARY.md` exists with the structured sections.
  </verify>
  <done>
    Planning-docs reflect Phase 45 completion. DASH-APP-01..03 flipped to Complete. v1.9 milestone stays open (Phases 46-49 pending). Phase 46 is the next target.
  </done>
</task>

</tasks>

<verification>
Post-checkpoint phase-level assertion (human-verified in Task 2):
- All 5 success criteria observable in the live browser against the real bot. Hybrid evidence is accepted where organic traffic doesn't trigger a specific SC during the UAT window, per the Phase 41-05 / 43-03 precedent (e.g. if no Hebrew pending row is available, SC#4 Hebrew-RTL edit mode is accepted on code inspection + coverage).

If any SC fails: the human pause ends with a failure description rather than "approved"; the orchestrator spawns `/gsd:plan-phase 45 --gaps` to close gaps; Task 3 does NOT fire on a failed UAT.
</verification>

<success_criteria>
- PM2 whatsapp-bot runs the Phase 45 code cleanly; four new write routes are reachable + returning the right 401/404/400/200/409/503 envelopes.
- Dashboard bundle serves the updated /pending-tasks page.
- Owner walks through all 5 SCs (with direct observation or hybrid acceptance per 41-05/43-03 convention).
- ROADMAP.md, REQUIREMENTS.md, STATE.md flipped to reflect Phase 45 completion.
- v1.9 milestone stays in progress; Phase 46 is clearly the next target.
- Phase 45 ships DASH-APP-01 (buttons + JWT-gated routes + optimistic UX) + DASH-APP-02 (Approve inherits Phase 42 enrichment + Google Tasks sync via approveActionable) + DASH-APP-03 (Edit fall-through + SSE re-hydration within 3s).
</success_criteria>

<output>
`.planning/phases/45-dashboard-pending-tasks-write-actions/45-04-SUMMARY.md` with the structured sections listed in Task 3 Step E. One closeout commit. If any SC failed: a gap description block that seeds the `--gaps` plan run.
</output>
</content>
</invoke>