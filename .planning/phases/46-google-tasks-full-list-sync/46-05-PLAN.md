---
phase: 46-google-tasks-full-list-sync
plan: "05"
type: execute
wave: 4
depends_on:
  - "46-04"
files_modified: []
autonomous: false
requirements:
  - GTASKS-01
  - GTASKS-02
  - GTASKS-03
  - GTASKS-04
  - GTASKS-05

must_haves:
  truths:
    - "PM2 whatsapp-bot service is running with Phase 46 code serving live traffic"
    - "GET /api/google-tasks/lists returns all owner task lists with real data"
    - "Calendar page shows gtasks pills from all lists with distinct per-list colors"
    - "Sidebar filter panel shows all gtasks lists; toggling a list hides/shows its pills"
    - "Filter preferences survive a page refresh"
    - "A gtasks item already mirrored as an approved actionable appears only once on the calendar (from actionables, not gtasks)"
    - "Completing a gtasks pill via long-press sheet makes it disappear immediately"
  artifacts:
    - path: "src/api/routes/googleTasks.ts"
      provides: "All four mutation routes + two read routes"
    - path: "src/api/routes/calendar.ts"
      provides: "Aggregator with gtasks source"
    - path: "dashboard/src/components/calendar/CalendarFilterPanel.tsx"
      provides: "Filter panel in production bundle"
  key_links:
    - from: "PM2 whatsapp-bot"
      to: "GET /api/google-tasks/lists"
      via: "live curl"
      pattern: "google-tasks/lists"
    - from: "Browser /calendar"
      to: "gtasks pills"
      via: "owner visual verification"
      pattern: ""
---

<objective>
Deploy Phase 46 code to the live PM2 server and run owner walkthrough verifying all five GTASKS requirements against real Google Tasks data.

Purpose: Phase closure — confirms the feature works end-to-end on real data, not just in tests.
Output: Phase 46 marked complete in ROADMAP.md + REQUIREMENTS.md + STATE.md.
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-google-tasks-full-list-sync/46-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dashboard bundle + restart PM2</name>
  <files></files>
  <action>
Run the full deploy sequence on the server:

```bash
cd /home/yuval/whatsapp-bot/dashboard
npx vite build

cd /home/yuval/whatsapp-bot
pm2 restart whatsapp-bot

# Confirm online
pm2 status whatsapp-bot
```

Sanity curls (replace $JWT with a valid token from `src/api/auth.ts` test-login or stored token):
```bash
# Lists endpoint
curl -s -H "Authorization: Bearer $JWT" http://localhost:3000/api/google-tasks/lists | head -c 500

# Items endpoint (default window)
curl -s -H "Authorization: Bearer $JWT" http://localhost:3000/api/google-tasks/items | head -c 500

# Unified aggregator includes gtasks
curl -s -H "Authorization: Bearer $JWT" http://localhost:3000/api/calendar/items | python3 -c "import sys,json; d=json.load(sys.stdin); print('gtasks source:', d['sources'].get('gtasks', 'MISSING')); print('gtasks items:', len([i for i in d['items'] if i['source']=='gtasks']))"
```

If any curl returns 500 or gtasks source is MISSING: read logs (`pm2 logs whatsapp-bot --lines 30`) and fix before proceeding to checkpoint.
  </action>
  <verify>
    pm2 status whatsapp-bot shows 'online'
    curl http://localhost:3000/api/google-tasks/lists returns 200 with lists array (may be empty if no auth, 401 is expected without JWT)
    curl with JWT to /api/calendar/items shows sources.gtasks key
  </verify>
  <done>
    - PM2 process is online
    - /api/google-tasks/lists returns 200 JSON with JWT
    - /api/calendar/items includes sources.gtasks='ok' or sources.gtasks='error' (never MISSING key)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Owner Walkthrough: Verify GTASKS-01..05 on Live Data</name>
  <what-built>
    Phase 46 complete feature set:
    - GET /api/google-tasks/lists and /items backend routes
    - Calendar aggregator extended with gtasks source (4th slot)
    - Dashboard CalendarFilterPanel with per-list toggles + color swatch + item count + gear override
    - Gtasks pills rendered with per-list hash color
    - Dedup: mirrored items appear only from actionables, not gtasks
    - Gtasks mutation routes: reschedule, edit, delete, complete
    - PillActionSheet Complete action for gtasks pills
    - Filter prefs persisted to localStorage
  </what-built>
  <action>Human walkthrough of GTASKS-01..05 requirements on live production data. See how-to-verify for detailed steps.</action>
  <how-to-verify>
    Open the dashboard in a browser. Navigate to /calendar.

    **GTASKS-01 — API routes:**
    1. In DevTools Network tab, confirm GET /api/google-tasks/lists returns your Google Tasks lists.
    2. Confirm GET /api/google-tasks/items?from=...&to=... returns CalendarItems with source='gtasks'.

    **GTASKS-02 — Aggregator:**
    3. Confirm the left-side source status indicators (if any) do not show gtasks error.
    4. Open Network tab, find the /api/calendar/items response, confirm sources.gtasks='ok'.

    **GTASKS-03 — Gtasks pills on calendar:**
    5. Verify that Google Tasks items with due dates in the current view window appear as colored pills on the calendar grid.
    6. Each list has a distinct color stripe on its pills.
    7. Drag a gtasks pill to a different date — confirm it moves and persists after refresh.
    8. Long-press a gtasks pill on mobile — action sheet shows Reschedule, Edit title, Complete, Delete.
    9. Tap "Complete" — pill disappears immediately.

    **GTASKS-04 — Filter panel:**
    10. The left rail shows a "Google Tasks" section listing each task list with a checkbox, color dot, name, and item count.
    11. Uncheck one list — its pills disappear from the calendar.
    12. Refresh the page — the list is still unchecked (localStorage persistence).
    13. Click the gear icon on a list — color override popover opens. Change the color — the dot and pill stripe update.

    **GTASKS-05 — Dedup:**
    14. Identify a Google Tasks item that was created via WhatsApp bot approval (has a matching row in actionables with status='approved').
    15. Confirm it appears ONCE on the calendar, rendered from the actionables source (source='task' pill, not source='gtasks' pill).
    16. In DevTools, verify the /api/google-tasks/items response does NOT include this item's task id.
  </how-to-verify>
  <verify>Owner confirms all 16 checks pass</verify>
  <done>All five GTASKS requirements verified against live production data</done>
  <resume-signal>Type "approved" if all checks pass, or describe any failures.</resume-signal>
</task>

</tasks>

<verification>
pm2 status whatsapp-bot
curl -s -H "Authorization: Bearer $JWT" http://localhost:3000/api/google-tasks/lists | python3 -m json.tool | head -30
curl -s -H "Authorization: Bearer $JWT" http://localhost:3000/api/calendar/items | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['sources'])"
</verification>

<success_criteria>
- PM2 online, fresh bundle served
- All five GTASKS requirements confirmed on live data:
  - GTASKS-01: /lists and /items endpoints live
  - GTASKS-02: aggregator includes gtasks, partial-failure isolated
  - GTASKS-03: gtasks pills render with per-list color, mutations work
  - GTASKS-04: sidebar filter panel with localStorage persistence
  - GTASKS-05: dedup confirmed — mirrored items appear once from actionables
- ROADMAP.md Phase 46 marked complete
- REQUIREMENTS.md GTASKS-01..05 marked complete
- STATE.md updated to Phase 47
</success_criteria>

<output>
After completion, create `.planning/phases/46-google-tasks-full-list-sync/46-05-SUMMARY.md`
</output>
