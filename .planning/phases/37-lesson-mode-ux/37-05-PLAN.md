---
phase: 37-lesson-mode-ux
plan: 05
type: execute
wave: 4
depends_on: [37-01, 37-02, 37-03, 37-04]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [LIN-11, LIN-12, LIN-13]
must_haves:
  truths:
    - "Live PM2-running dashboard serves the lesson-mode UX end-to-end against real pm-authority state on 127.0.0.1:8765"
    - "All 3 phase success criteria are observed in a live browser session against real posts in pm-authority's state.db"
    - "STATE.md reflects Phase 37 as complete with a commit pointer, ROADMAP marks the phase [x], REQUIREMENTS marks LIN-11/12/13 [x]"
    - "No regressions in the existing linkedin test suite (pm-authority pytest + whatsapp-bot vitest + dashboard tsc/vite)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated position: Phase 37 COMPLETE"
      contains: "Phase 37"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 37 marked [x]"
      contains: "Phase 37: Lesson Mode UX"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LIN-11 / LIN-12 / LIN-13 marked [x]"
      contains: "LIN-11"
  key_links:
    - from: "Live dashboard lesson selection + variant finalization flow"
      to: "pm-authority state.db"
      via: "whatsapp-bot proxy → pm-authority HTTP → ReviewManager → fal.ai"
      pattern: "state transition observed"
---

<objective>
Final wave plan for Phase 37. Does NOT write new application code — verifies every Plan 37-01 / 37-02 / 37-03 / 37-04 change works end-to-end on the live PM2 stack, then marks the phase complete in STATE / ROADMAP / REQUIREMENTS.

Purpose: Guarantee Phase 37's 3 success criteria are observable with a real user session before LIN-11/12/13 flip to [x].

Output:
  - Pre-verification test suite re-runs (pytest + vitest + tsc + vite build)
  - PM2 restart and sanity curls
  - Live browser walkthrough by the owner (checkpoint:human-verify)
  - STATE / ROADMAP / REQUIREMENTS doc updates
  - Final commit for the phase
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-lesson-mode-ux/37-CONTEXT.md
@.planning/phases/37-lesson-mode-ux/37-01-PLAN.md
@.planning/phases/37-lesson-mode-ux/37-02-PLAN.md
@.planning/phases/37-lesson-mode-ux/37-03-PLAN.md
@.planning/phases/37-lesson-mode-ux/37-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run all test suites to catch cross-plan regressions</name>
  <files>
    (no files — verification only)
  </files>
  <action>
    1. pm-authority HTTP test suite:
       `cd /home/yuval/pm-authority && ./.venv/bin/python -m pytest tests/test_http_*.py -v`
       Expect: all tests passing. Plan 37-01 added ~2-4 assertions to test_http_reads.py; baseline from Phase 36 was ~68+, so expect ~68-72 total.

    2. whatsapp-bot linkedin vitest:
       `cd /home/yuval/whatsapp-bot && npx vitest run src/api/linkedin/`
       Expect: all tests passing. Plan 37-01 Task 2 added ~1 new read test + updated existing mock payloads; baseline from Phase 36 was ~92+.

    3. Full whatsapp-bot vitest:
       `cd /home/yuval/whatsapp-bot && npx vitest run`
       Expect: all tests passing, no regressions in non-linkedin test files.

    4. Dashboard build chain:
       `cd /home/yuval/whatsapp-bot/dashboard && npx tsc -b && npx vite build`
       Both clean. No type errors in any of the new files (37-01/02/03/04).

    5. Optional: `cd /home/yuval/whatsapp-bot/dashboard && npx vitest run` if dashboard has any unit tests. Skip if none exist.

    If ANY of the above fails, STOP and raise an issue. The failure means a cross-plan integration bug slipped through individual plan verification gates — diagnose and fix before proceeding.
  </action>
  <verify>
    All four required commands exit 0. Record the exact test counts for the SUMMARY.
  </verify>
  <done>All test suites green against the full Phase 37 codebase.</done>
</task>

<task type="auto">
  <name>Task 2: PM2 restart both services + wire-level sanity curls + ensure a testable post exists</name>
  <files>
    (no files — runtime verification)
  </files>
  <action>
    1. `npx pm2 restart pm-authority-http` — wait 2s, then `npx pm2 logs pm-authority-http --lines 30 --nostream`. Look for clean FastAPI startup. No ImportError tracebacks from the Plan 37-01 schema additions.

    2. `npx pm2 restart whatsapp-bot` — wait 2s, then `npx pm2 logs whatsapp-bot --lines 30 --nostream`. Look for clean Fastify startup + no Zod-related drift errors.

    3. `npx pm2 status` — both services `online`.

    4. Wire-level sanity curls:

       ```bash
       # Upstream direct — verify the new DTO fields are present
       curl -sS http://127.0.0.1:8765/v1/posts | jq '.[0] | {id, status, project_name, source_snippet, variants: (.variants | map({id, kind, created_at})), lesson_candidates: (.lesson_candidates | map({id, created_at}))}'
       # → project_name should be a string; source_snippet can be null or string;
       #   variants[].created_at and lesson_candidates[].created_at should be ISO timestamps

       # Proxy health
       curl -sS -w '\n%{http_code}\n' http://127.0.0.1:3000/api/linkedin/health
       # → 401 (no token — expected)

       # SSE stream sanity (just connect + disconnect)
       curl -sS -N --max-time 5 'http://127.0.0.1:3000/api/linkedin/queue/stream?token=' | head -5
       # → 401 response body (no token — expected)
       ```

    5. Verify at least 1 testable post exists per SC:

       ```bash
       curl -sS http://127.0.0.1:8765/v1/posts | jq '[.[] | {id, status}] | group_by(.status) | map({status: .[0].status, count: length})'
       ```

       For a useful live walkthrough you want:
         - ≥1 post in PENDING_LESSON_SELECTION (for SC #1)
         - ≥1 post in PENDING_VARIANT (for SC #2 + SC #3)

       If NONE exist: STOP and coordinate with the owner. Options:
         (a) Run the existing `python -m pm_authority.cli` lesson-mode generate command on a real project to seed a real pair: `cd /home/yuval/pm-authority && ./.venv/bin/python -m generation.cli ...` (check the repo for the actual invocation — grep `generate.py\|cli.py\|generate_lesson_variants` in pm-authority).
         (b) Manually call `POST /v1/lesson-runs` against an existing sequence that has context_json populated:
             ```bash
             curl -sS -X POST http://127.0.0.1:8765/v1/lesson-runs \
                  -H 'Content-Type: application/json' \
                  -d '{"source_sequence_id": "<some-seq-id>", "chosen_lesson": "ship fast"}'
             ```
             (Phase 38 will build the dashboard form for this; for Phase 37 verification, a curl is fine.)
         (c) Degrade the walkthrough — Claude walks through with the owner using whatever states are available, noting which SCs are observed vs inferred.

    6. Record the post IDs you'll use in the walkthrough so Task 3's checkpoint has concrete targets.
  </action>
  <verify>
    Both PM2 services online; all curls return expected status codes; at least one post in PENDING_LESSON_SELECTION AND/OR PENDING_VARIANT identified.
  </verify>
  <done>Live stack is healthy and has real lesson-mode data to exercise in Task 3's walkthrough.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: LIVE browser walkthrough — 3 Success Criteria</name>
  <files>
    (no files — human interaction only; the owner walks through the live dashboard at http://127.0.0.1:3000/linkedin/queue)
  </files>
  <action>
    HUMAN CHECKPOINT. Claude does NOT execute any commands for this task — the owner drives the browser. Claude's job here is to present the walkthrough script clearly and wait for the "approved" signal.

    **What was built (full Phase 37 lesson-mode UX surface):**
      - Plan 37-01: pm-authority PostDTO extended (project_name, source_snippet, per-variant/candidate created_at), proxy Zod + dashboard Zod mirrored, GenerationMetadata + StickyConfirmBar shared components, 2 new router routes + stub pages
      - Plan 37-02: LinkedInLessonSelection page (vertical 4-card stack, focus-then-confirm, sticky confirm, modal wait, auto-nav on success), useLinkedInPickLesson hook, LessonCandidateCard + LessonGenerationModal
      - Plan 37-03: LinkedInVariantFinalization page (2-col grid, focus-then-confirm, sticky confirm, mixed 200/202 flow, SSE-driven image state), useLinkedInPickVariant hook, VariantCard + VariantImageSlot
      - Plan 37-04: Queue integration (purple/indigo pills + left stripes, PendingActionEntryButton, StatusStrip 2 new counters, useNewArrivalFlash 300ms amber animation)

    **How to verify — open http://127.0.0.1:3000/linkedin/queue in a browser:**

    ---

    ### Queue view pre-checks (Plan 37-04)

    1. The StatusStrip at the top shows 6 mini-counter cards on wide screens (or wraps to 3×2 / 2×3 depending on viewport). Confirm:
       - "Next publish" (from Phase 35)
       - "Pending" (from Phase 35)
       - "Lessons to pick: N" (NEW — purple accent)
       - "Variants to finalize: N" (NEW — indigo accent)
       - "Approved" (from Phase 35)
       - "Last published" (from Phase 35)

    2. In the queue card list, locate a PENDING_LESSON_SELECTION post (if any exist). Confirm:
       - Status pill reads "Lesson to pick" in purple
       - Card has a 4px purple left-edge accent stripe
       - Top-right actions area shows a "Pick lesson →" button

    3. Locate a PENDING_VARIANT post (if any). Confirm:
       - Status pill reads "Variant to finalize" in indigo
       - Card has a 4px indigo left-edge accent stripe
       - Top-right shows "Pick variant →" button

    ---

    ### SC#1 — Lesson selection flow (LIN-11)

    **Setup:** Click "Pick lesson →" on a PENDING_LESSON_SELECTION card.

    **Steps:**
      1. Browser navigates to /linkedin/queue/posts/:id/lesson. The full page renders:
         - "← Back to queue" link at top
         - Page title "Pick a lesson"
         - Metadata card: Project, Perspective, Language, Generated timestamp
         - "Source snippet" collapsible details (click to expand, confirm the first 500 chars of the source content are visible)
         - 4 lesson candidate cards in a vertical full-width stack, each with:
           * Letter tag A / B / C / D (round badge, top-left of card)
           * Lesson text (bold)
           * Rationale (muted, smaller)
           * "Generated <relative> · Claude" strip at bottom
      2. Click candidate A. Confirm:
         - A gets a blue ring + light-blue background
         - The sticky confirm bar at bottom of viewport shows "Confirm selection" button enabled with "Candidate A selected" helper text
      3. Click candidate C. Confirm:
         - A un-focuses (no ring), C gets the ring
         - Helper text updates to "Candidate C selected"
         - The switch is non-destructive
      4. Click "Confirm selection".
         - The modal opens: "Generating variants…" with a spinner, explanatory text about the lesson → variant gen.
         - Try to dismiss the modal via Escape key — it should NOT close (locked).
         - Try to click outside — should NOT close.
         - The browser back button IS the escape hatch (don't use it for this SC unless you want to abort).
      5. Wait 10-60s for pm-authority to generate the 2 variants.
      6. When generation completes:
         - Modal auto-dismisses
         - URL auto-navigates to /linkedin/queue/posts/:id/variant
         - The variant page loads for the same post id

    **Error-path observation (optional):** if generation fails during this test, confirm the modal flips to an error state with a "Back to queue" button. Otherwise skip.

    ---

    ### SC#2 — Variant finalization flow (LIN-12)

    **Setup:** You should be on /linkedin/queue/posts/:id/variant from SC#1, OR navigate there via "Pick variant →" on a PENDING_VARIANT post.

    **Steps:**
      1. Confirm the page renders:
         - "← Back to queue" + title "Finalize a variant"
         - Metadata card (project/perspective/language/generated/source snippet)
         - 2 variant cards side-by-side on desktop (stacked on mobile — resize browser to test)
         - Each variant card shows:
           * Kind badge ("lesson_story" or "lesson_claim")
           * VariantImageSlot showing either the generated image OR "Image will generate when you select this variant" placeholder
           * Hebrew content (if present, dir=rtl) above the English content
           * Collapsible "Show image prompt" details — click to expand, confirm the image_prompt text is visible
           * "Generated <relative> · Claude" strip
      2. Click variant 1 (left card). Confirm blue ring + selected state.
      3. Click variant 2 (right card). Confirm focus switches.
      4. Click "Finalize this variant" in the sticky bar.
      5. Observe the mutation:
         - Fast path (no image gen needed — hook variants or already-has-image): a sonner toast "Variant finalized" appears, the router auto-navigates back to /linkedin/queue. Confirm the post no longer shows in the PENDING_VARIANT state (either it moved to DRAFT/APPROVED or the image pipeline advanced it to PENDING_PII_REVIEW).
         - Slow path (lesson-mode variant with image_prompt and no pre-existing image): **proceed to SC#3 — the slow path IS SC#3**.

    ---

    ### SC#3 — Inline fal.ai image rendering (LIN-13)

    **Setup:** Continuing from SC#2's slow path. The variant page stays open; the focused variant's image slot flips from the idle placeholder to "Generating image…" with a spinner + elapsed-seconds counter.

    **Steps:**
      1. Confirm the focused variant's VariantImageSlot shows:
         - Loader2 spinner
         - "Generating image…" label
         - "Ns elapsed" counter that increments every second
      2. Content around the image slot stays interactive — you should still be able to click "Show image prompt" and read the Hebrew/English copy while waiting.
      3. The sticky confirm bar's helper text reads "fal.ai image is generating…" and the button is disabled.
      4. Wait 5-30 seconds for fal.ai to complete. No client-side timeout — the spinner waits indefinitely. (If fal.ai is down or takes ≥2 min, abort the test and note the degraded result.)
      5. When fal.ai completes, the SSE stream re-delivers the post with a populated image.url. The focused variant's slot flips from the spinner to the real image. **CRITICAL SC#3 PASS CRITERION:** the real image MUST be visible on the focused variant card for approximately 1.5 seconds before the router navigates back to /linkedin/queue. This is not a bug — Plan 37-03 deliberately delays the auto-nav via `setTimeout(..., 1500)` so the owner can visually confirm the image rendered on the variant card (otherwise the image would be visible for <1 frame before nav-away and SC#3 would be unobservable). A sonner toast "Variant finalized — image ready" appears during this 1.5s window. Then the router auto-navigates back to /linkedin/queue.

         **To mark SC#3 green, the owner must explicitly observe:**
           - [ ] The spinner disappears and is replaced by the actual generated image ON the focused variant card
           - [ ] The image stays visible on the card for ~1-2 seconds (NOT a flash)
           - [ ] Only AFTER that visible window does the router navigate back to /linkedin/queue
         If the nav happens instantly (image flashed for <1 frame), SC#3 FAILS — the 1500ms setTimeout was not implemented and Plan 37-03 must be revisited.
      6. Confirm via curl: `curl -s http://127.0.0.1:8765/v1/posts/{post_id} | jq '.image'` → image.url is populated and image.source is "ai".

    **Observation on the per-variant image model (documented in Plan 37-03):** pm-authority only generates ONE image at pick-variant time, attached to the post. So only the FOCUSED variant card ends up showing the image — the non-focused variant card keeps the idle placeholder throughout. This is the shipped interpretation of CONTEXT §Area 2's "generated fal.ai image on each variant card". Note in the SUMMARY whether the owner is satisfied with this interpretation or wants a future plan to pre-generate 2 images.

    ---

    ### SSE arrival flash (Plan 37-04 bonus check, optional)

    If you can trigger a new PENDING_LESSON_SELECTION or PENDING_VARIANT arrival while on /linkedin/queue (e.g. by running a lesson-run curl in another shell), confirm:
      - The new card slides into the list
      - The card flashes amber for ~300ms
      - NO toast appears

    ---

    **If ANY of the above fails:**
      - Capture the exact failure (screenshot / curl output / browser console logs).
      - Raise as a blocking issue. DO NOT proceed to Task 4 (STATE / ROADMAP update) until every SC is observed green or explicitly waived by the owner.

    **If all 3 SCs pass:** type "approved" to resume and proceed to Task 4.
  </action>
  <verify>
    All 3 success criteria observed live in the browser. Owner's typed confirmation ("approved") is the pass signal.
  </verify>
  <done>
    Owner typed "approved" after walking through SC#1-3 in the live browser against real pm-authority state. Phase 37 is cleared to progress to Task 4.
  </done>
  <resume-signal>
    Type "approved" once all 3 success criteria are observed in the live browser, OR describe any failures with screenshots / curl output.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update STATE.md / ROADMAP.md / REQUIREMENTS.md + commit + write SUMMARY</name>
  <files>
    /home/yuval/whatsapp-bot/.planning/STATE.md
    /home/yuval/whatsapp-bot/.planning/ROADMAP.md
    /home/yuval/whatsapp-bot/.planning/REQUIREMENTS.md
  </files>
  <action>
    Only run this task after Task 3's checkpoint resolved with "approved".

    1. `.planning/STATE.md`:
       - Update "Current Position":
         - `Phase: 37 — Lesson Mode UX — **COMPLETE** (5/5 plans, {today's date})`
         - `Plan: 37-05 complete; next = Phase 38 Plan 01 (New Lesson Run Form) or new phase work`
       - Update "Status:" line: prepend a 1-paragraph summary of Phase 37 — mention the lesson + variant pages, SSE-driven image state, queue integration, LIN-11/12/13 complete, live-verified SCs, final test counts, and commit pointers.
       - Update "Last activity: {date} — Phase 37 complete. Lesson-mode review fully in the dashboard (lesson selection + variant finalization + inline fal.ai image); all 3 SCs observed."
       - Update "Progress:" — v1.7 phases 33/34/35/36/37 shipped → 5/6 ≈ 83%.

    2. `.planning/ROADMAP.md`:
       - Find `### Phase 37: Lesson Mode UX`.
       - Flip the checkbox on the `- [ ] **Phase 37:** Lesson Mode UX …` summary line to `- [x]`.
       - Update the phase block's "**Plans:**" line from `TBD` to `5 plans shipped — foundation (37-01), lesson selection page (37-02), variant finalization page (37-03), queue integration (37-04), live verification (37-05)`.
       - Update the phase status table row at or near line 280+: change `| 37. Lesson Mode UX | v1.7 | 0/? | Not started | — |` to `| 37. Lesson Mode UX | v1.7 | 5/5 | Complete | {today} |`.

    3. `.planning/REQUIREMENTS.md`:
       - Flip LIN-11, LIN-12, LIN-13 from `- [ ]` to `- [x]`.
       - Update the requirements status table at the bottom: rows for LIN-11, LIN-12, LIN-13 from "Pending" to "Complete".

    4. Git commit in the whatsapp-bot tree:
       `git add .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md`
       `git commit -m "docs(phase-37): mark Phase 37 complete — LIN-11/12/13 shipped"`

    5. Do NOT push. Per global CLAUDE.md: "Never push without asking".

    6. Create `.planning/phases/37-lesson-mode-ux/37-05-SUMMARY.md` with:
       - Date, test counts, commit SHAs (pm-authority + whatsapp-bot)
       - Each SC checkbox with "observed live via {test}"
       - Documented architectural decisions: GenerationMetadata renders Claude + timestamp (no model/token_cost column); per-variant image reality (one image per post, shown on focused card only); SSE-driven image update via piggyback on /api/linkedin/queue/stream
       - Any deferred items or known bugs the owner waived during the checkpoint
       - Next suggested phase (38 New Lesson Run Form or a specific LIN-14 focus)
  </action>
  <verify>
    `grep -n "Phase 37.*Complete\|Phase 37.*COMPLETE" .planning/STATE.md .planning/ROADMAP.md` finds updates in both files.
    `grep -n "^- \[x\] \*\*LIN-11\|^- \[x\] \*\*LIN-12\|^- \[x\] \*\*LIN-13" .planning/REQUIREMENTS.md` shows all three flipped.
    `git log --oneline -1` shows the new commit.
    `.planning/phases/37-lesson-mode-ux/37-05-SUMMARY.md` exists with the expected content.
  </verify>
  <done>
    Phase 37 is formally complete in all planning docs, the commit is on the local branch (not pushed), and the phase summary is written.
  </done>
</task>

</tasks>

<verification>
  - Task 1 (test suites): all green
  - Task 2 (PM2 + curls): both services online, endpoints reachable, testable posts identified
  - Task 3 (human checkpoint): all 3 SCs observed in a live browser
  - Task 4 (doc updates + commit): STATE/ROADMAP/REQUIREMENTS flipped, commit made, summary written
</verification>

<success_criteria>
Phase 37 is done when:
  1. All 3 phase success criteria are observed live in a browser against real pm-authority data.
  2. All test suites are green at the final commit.
  3. STATE.md / ROADMAP.md / REQUIREMENTS.md reflect Phase 37 as complete.
  4. LIN-11, LIN-12, LIN-13 are flipped to [x] in REQUIREMENTS.md.
  5. A commit exists on the local branch with the planning-doc updates.
  6. `.planning/phases/37-lesson-mode-ux/37-05-SUMMARY.md` exists.
</success_criteria>

<output>
`.planning/phases/37-lesson-mode-ux/37-05-SUMMARY.md` — the phase's final summary. Include:
  - Live verification date + observed SC pass/fail checklist
  - Final test counts across pm-authority pytest + whatsapp-bot linkedin vitest + full whatsapp-bot vitest + dashboard tsc/vite
  - Commit SHAs from both repos
  - Documented assumptions: (a) GenerationMetadata omits model + token_cost columns that don't exist yet; (b) per-variant fal.ai image is a single post-level image shown on the focused card only; (c) SSE piggybacks the existing /api/linkedin/queue/stream
  - Any deferred items or bugs the owner waived during the checkpoint
  - Recommendation for next work (Phase 38 New Lesson Run Form, or a different direction)
</output>