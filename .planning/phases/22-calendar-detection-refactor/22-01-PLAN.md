---
phase: 22-calendar-detection-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/calendar/CalendarDetectionService.ts
  - src/groups/groupMessagePipeline.ts
  - src/groups/suggestionTracker.ts
  - src/groups/calendarHelpers.ts
  - src/groups/dateExtractor.ts
  - src/calendar/__tests__/CalendarDetectionService.test.ts
  - vitest.config.ts
  - package.json
autonomous: true
requirements:
  - CAL-05

must_haves:
  truths:
    - "CalendarDetectionService exists as a class in src/calendar/ with hasDateSignal() and extractDates() methods"
    - "Group chat date extraction behavior is identical after refactor (same pre-filter, same Gemini call, same confidence filtering)"
    - "The shared service accepts a DetectionContext with chatType field, callable from any pipeline"
    - "No circular dependency between groupMessagePipeline.ts and suggestionTracker.ts"
    - "Unit tests for CalendarDetectionService pass, covering hasDateSignal and extractDates with mocked Gemini"
  artifacts:
    - path: "src/calendar/CalendarDetectionService.ts"
      provides: "Shared date detection service class"
      exports: ["CalendarDetectionService", "ExtractedDate", "DetectionContext"]
    - path: "src/groups/calendarHelpers.ts"
      provides: "Shared helpers extracted from groupMessagePipeline to break circular deps"
      exports: ["buildConfirmationText", "detectGroupLanguage", "formatDateForDisplay", "getCalendarIdFromLink"]
    - path: "src/calendar/__tests__/CalendarDetectionService.test.ts"
      provides: "Unit tests for CalendarDetectionService"
      min_lines: 50
  key_links:
    - from: "src/groups/groupMessagePipeline.ts"
      to: "src/calendar/CalendarDetectionService.ts"
      via: "import and method calls"
      pattern: "calendarDetection\\.(hasDateSignal|extractDates)"
    - from: "src/groups/suggestionTracker.ts"
      to: "src/groups/calendarHelpers.ts"
      via: "import shared helpers"
      pattern: "import.*from.*calendarHelpers"
    - from: "src/groups/groupMessagePipeline.ts"
      to: "src/groups/calendarHelpers.ts"
      via: "import shared helpers"
      pattern: "import.*from.*calendarHelpers"
---

<objective>
Extract date detection logic into a shared CalendarDetectionService class and break the circular dependency between groupMessagePipeline and suggestionTracker. Unit test the extracted service.

Purpose: Makes date extraction reusable across private and group pipelines (CAL-05). Breaks circular import that blocks future refactoring. Tests ensure correctness per user locked decision.
Output: CalendarDetectionService class in src/calendar/, shared helpers in src/groups/calendarHelpers.ts, updated imports throughout, unit tests.
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-calendar-detection-refactor/22-CONTEXT.md
@.planning/phases/22-calendar-detection-refactor/22-RESEARCH.md
@src/groups/dateExtractor.ts
@src/groups/groupMessagePipeline.ts
@src/groups/suggestionTracker.ts
@src/calendar/calendarService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared helpers and create CalendarDetectionService</name>
  <files>
    src/calendar/CalendarDetectionService.ts
    src/groups/calendarHelpers.ts
  </files>
  <action>
**Step A: Create `src/groups/calendarHelpers.ts`** — Move these functions OUT of `groupMessagePipeline.ts`:
- `getCalendarIdFromLink(calendarLink: string): string | null`
- `formatDateForDisplay(date: Date): string`
- `buildConfirmationText(lang, title, date, calendarLink): string`
- `detectGroupLanguage(groupJid: string): Promise<'he' | 'en'>` (needs import of `getGroupMessagesSince`)

Export all four. This breaks the circular dependency where `suggestionTracker.ts` imports from `groupMessagePipeline.ts`.

**Step B: Create `src/calendar/CalendarDetectionService.ts`** as a class (per user locked decision — class-based, matching existing service naming convention):

```typescript
export interface ExtractedDate {
  title: string;
  date: Date;
  confidence: string;
  location?: string;
  description?: string;
  url?: string;
}

export interface DetectionContext {
  senderName: string | null;
  chatName: string | null;       // group name or contact name
  chatType: 'group' | 'private';
}

export class CalendarDetectionService {
  hasDateSignal(text: string): boolean { /* same logic as hasNumberPreFilter */ }
  async extractDates(text: string, context: DetectionContext): Promise<ExtractedDate[]> { /* same logic as current extractDates */ }
}
```

Move ALL logic from `src/groups/dateExtractor.ts` into the class:
- `hasNumberPreFilter` becomes `hasDateSignal` (method on class)
- `extractDates(text, senderName, groupName)` becomes `extractDates(text, context: DetectionContext)` — update the Gemini system prompt to use `context.chatName` instead of `groupName` (makes it pipeline-agnostic)
- Keep the Zod schema, DateExtractionSchema, and all validation/filtering logic identical
- Export a singleton instance: `export const calendarDetection = new CalendarDetectionService();`
- Export the types `ExtractedDate` and `DetectionContext`

The system prompt should mention "WhatsApp messages" (not "group messages") since it will be used for both pipelines.
  </action>
  <verify>
Run `npx tsc --noEmit` — should compile with zero errors. Verify `src/calendar/CalendarDetectionService.ts` exports the class, types, and singleton.
  </verify>
  <done>CalendarDetectionService class exists with hasDateSignal() and extractDates() methods. calendarHelpers.ts exports the four shared helper functions. Both files compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire imports in groupMessagePipeline and suggestionTracker, delete dateExtractor</name>
  <files>
    src/groups/groupMessagePipeline.ts
    src/groups/suggestionTracker.ts
    src/groups/dateExtractor.ts
  </files>
  <action>
**Step A: Update `src/groups/groupMessagePipeline.ts`:**
- Remove the four helper functions moved to calendarHelpers.ts (getCalendarIdFromLink, formatDateForDisplay, buildConfirmationText, detectGroupLanguage)
- Remove import of `hasNumberPreFilter, extractDates` from `./dateExtractor.js`
- Add import: `import { calendarDetection } from '../calendar/CalendarDetectionService.js';`
- Add import: `import { getCalendarIdFromLink, buildConfirmationText, detectGroupLanguage } from './calendarHelpers.js';`
- In `processGroupMessages()`, replace `hasNumberPreFilter(msg.body)` with `calendarDetection.hasDateSignal(msg.body)`
- Replace `extractDates(msg.body, msg.senderName, group.name ?? null)` with:
  ```typescript
  calendarDetection.extractDates(msg.body, {
    senderName: msg.senderName,
    chatName: group.name ?? null,
    chatType: 'group',
  })
  ```
- Keep `buildDeleteConfirmText` and `isDeleteTrigger` in groupMessagePipeline.ts (they are private helpers used only there)
- Keep `calendarIdCache`, debounce logic, `processGroupMessages`, `addToDebounce`, `handleReplyToDelete`, and `initGroupPipeline` exactly as they are
- Re-export `buildConfirmationText` and `detectGroupLanguage` from calendarHelpers for backward compatibility (if any other files import them from groupMessagePipeline — but suggestionTracker will be updated in Step B)

**Step B: Update `src/groups/suggestionTracker.ts`:**
- Change import from `'./groupMessagePipeline.js'` to `'./calendarHelpers.js'`:
  ```typescript
  import { buildConfirmationText, detectGroupLanguage } from './calendarHelpers.js';
  ```
- No other changes needed — this breaks the circular dependency.

**Step C: Delete `src/groups/dateExtractor.ts`** — all its logic now lives in CalendarDetectionService.ts. No other file should import from it (groupMessagePipeline was the only consumer).

**Step D: Verify no other files import from dateExtractor.ts:**
- Run a grep for `dateExtractor` across the codebase to confirm no remaining references.
  </action>
  <verify>
Run `npx tsc --noEmit` — zero errors. Run `grep -r "dateExtractor" src/` — no results. Run `grep -r "from.*groupMessagePipeline" src/` — should NOT show suggestionTracker.ts (circular dep broken). Start the bot with `npx tsx src/index.ts` and verify it initializes without import errors (check for "Group message pipeline initialized" log).
  </verify>
  <done>groupMessagePipeline uses CalendarDetectionService. suggestionTracker imports from calendarHelpers (no circular dep). dateExtractor.ts is deleted. Bot starts cleanly and group pipeline initializes.</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for CalendarDetectionService</name>
  <files>
    package.json
    vitest.config.ts
    src/calendar/__tests__/CalendarDetectionService.test.ts
  </files>
  <action>
**Step A: Install vitest and set up test infrastructure.**
Run `npm install -D vitest` to add vitest as a dev dependency. No jest -- vitest works natively with ESM TypeScript (this project uses `"type": "module"`).

Create `vitest.config.ts` at project root:
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
});
```

Update `package.json` scripts: change `"test"` from the placeholder echo to `"vitest run"`.

**Step B: Create `src/calendar/__tests__/CalendarDetectionService.test.ts`** with two test groups:

**Group 1: hasDateSignal (pure function, no mocking needed)**
- Returns true for text containing digits: "Meeting on 15/03", "call at 3pm", "2026"
- Returns false for text with no digits: "hello world", "no numbers here", ""
- Returns true for Hebrew text with digits: "פגישה ב-15 למרץ"
- Edge cases: single digit "5", mixed content "abc1def"

**Group 2: extractDates (mock the Gemini call)**
Mock `generateJson` from `../ai/provider.js` using `vi.mock`. The mock should be at the module level:
```typescript
import { vi, describe, it, expect, beforeEach } from 'vitest';

vi.mock('../../ai/provider.js', () => ({
  generateJson: vi.fn(),
}));

// Also mock config to avoid needing .env
vi.mock('../../config.js', () => ({
  config: { LOG_LEVEL: 'silent' },
}));

import { CalendarDetectionService } from '../CalendarDetectionService.js';
import { generateJson } from '../../ai/provider.js';
```

Test cases for extractDates:
- Returns extracted dates when Gemini returns high-confidence results (mock `generateJson` to return `{ dates: [{ title: "Flight to Barcelona", date: "2026-03-20T10:00:00", confidence: "high" }] }`) -- verify the returned array has one ExtractedDate with correct title and date
- Filters out low/medium confidence results (mock returns mixed confidence) -- verify only high-confidence items are returned
- Returns empty array when Gemini returns null (mock returns null)
- Returns empty array when Gemini throws an error (mock throws Error)
- Filters out invalid dates (mock returns `{ dates: [{ title: "Bad", date: "not-a-date", confidence: "high" }] }`) -- verify empty array
- Includes optional fields (location, description, url) when present in Gemini response

Create the service instance directly (`new CalendarDetectionService()`) rather than importing the singleton, to avoid side effects. Pass a DetectionContext with `chatType: 'group'` for all tests.

**Important:** The `generateJson` mock must be typed properly. Use `vi.mocked(generateJson)` to get proper typing for `.mockResolvedValue()`.
  </action>
  <verify>
Run `npx vitest run` — all tests pass. Run `npx tsc --noEmit` — test file compiles cleanly. Verify test output shows both test groups (hasDateSignal and extractDates) with all cases passing.
  </verify>
  <done>Vitest installed and configured. CalendarDetectionService has unit tests covering hasDateSignal (pure logic) and extractDates (with mocked Gemini). All tests pass. Per user locked decision: "Unit tests for the extracted CalendarDetectionService."</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compiles cleanly
2. No imports from `dateExtractor` remain in the codebase
3. No imports from `groupMessagePipeline` in `suggestionTracker.ts` (circular dep broken)
4. Bot starts and logs "Group message pipeline initialized"
5. CalendarDetectionService is importable from any module with `import { calendarDetection, ExtractedDate, DetectionContext } from '../calendar/CalendarDetectionService.js'`
6. `npx vitest run` passes all unit tests for CalendarDetectionService
</verification>

<success_criteria>
- CalendarDetectionService class exists at src/calendar/CalendarDetectionService.ts with hasDateSignal() and extractDates(text, DetectionContext) methods
- Group pipeline behavior is identical (same pre-filter, same Gemini extraction, same suggestion flow)
- Circular dependency between groupMessagePipeline and suggestionTracker is eliminated
- dateExtractor.ts is deleted, all imports updated
- The service can be called from any handler by importing the singleton
- Unit tests pass for hasDateSignal (pure logic) and extractDates (mocked Gemini) per user locked decision
</success_criteria>

<output>
After completion, create `.planning/phases/22-calendar-detection-refactor/22-01-SUMMARY.md`
</output>
