---
phase: 50-dashboard-mobile-ui-polish
plan: 06
type: execute
wave: 4
depends_on:
  - "50-01"
  - "50-02"
  - "50-03"
  - "50-04"
  - "50-05"
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - MOBILE-01
  - MOBILE-02
  - MOBILE-03
  - MOBILE-04
  - MOBILE-05
  - MOBILE-06

must_haves:
  truths:
    - "Live PM2 whatsapp-bot process serves a dashboard bundle that includes Plan 50-01..05 code: useViewport hook, StickyActionBar, MonthDotsView, useHorizontalSwipe, useLongPress, PillActionSheet, every component's isMobile branch"
    - "MOBILE-01 verified live on a real phone via Tailscale URL: tap-target floor (Buttons feel >=40px on phone), iOS auto-zoom kill (focusing an input/textarea on iOS Safari does NOT trigger viewport zoom), safe-area insets clear the iOS notch + home indicator on a notched device, StickyActionBar pins to bottom on phone (visible on Drafts page), useViewport responds to orientation rotation"
    - "MOBILE-02 verified live: on phone, /calendar defaults to Day view; horizontal swipe left advances to next day; horizontal swipe right goes back; vertical scroll on a busy DayView is NOT hijacked into day navigation (CONTEXT risk #1); tapping the view toggle exposes Day / 3-Day / Dots only (no Week, no Month); MonthDotsView renders 7-col dot grid; tapping a day in Dots view jumps to Day view for that date"
    - "MOBILE-03 verified live: CalendarHeader is a single compact row; CalendarPill is tappable with 28px min height + no hover tooltip; DayView has the + New FAB in the bottom-right that opens the create flow; DayOverflowPopover + CreateItemPopover open as bottom sheets; InlineTitleEdit promotes to a bottom sheet when triggered on a pill"
    - "MOBILE-04 verified live on a real phone: long-press on a calendar pill (~500ms) opens PillActionSheet; long-press during an in-progress vertical scroll does NOT open the sheet (CONTEXT risk #2 — useLongPress cancels on >8px movement); haptic fires (vibrate-capable device); Reschedule opens native datetime-local picker pre-set to current slot; Save dispatches useRescheduleMutation; the pill is NOT draggable via touch (drag attempt does nothing); rescheduled item appears at the new slot via SSE within 3s; **CONTEXT risk #4 verified: rescheduling to a wall-clock time on the phone results in DB row at that IST wall-clock time (correct UTC instant stored)**"
    - "MOBILE-05 verified live: Overview at 320px width has 1-col stacked metrics, no text wrap; PendingTasks 3-button action row fits at 320px; Drafts primary actions stay pinned at the bottom while scrolling a long draft list and clear the iOS home indicator"
    - "ROADMAP.md Phase 50 checkbox flipped to [x]; ROADMAP.md Progress table row 'Phase 50' status → Complete; REQUIREMENTS.md v2.0 Traceability rows MOBILE-01..06 → Complete with date + evidence note; STATE.md Current Position block advanced to Phase 51 / next-target as appropriate"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 50 checkbox flipped + Progress table row updated + plan count finalized (6/6 plans complete)"
      contains: "Phase 50: Dashboard Mobile UI Polish.*Complete"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MOBILE-01..06 added to v2.0 Requirements section AND v2.0 Traceability table; all six rows flipped to Complete with date + evidence cross-reference to 50-06-SUMMARY.md"
      contains: "MOBILE-01.*Complete\\|MOBILE-06.*Complete"
    - path: ".planning/STATE.md"
      provides: "Current Position entry for Phase 50 complete + next phase as target (next phase TBD — likely v1.9 backfill: Phase 46 Google Tasks Full-List Sync)"
      contains: "Phase 50.*complete\\|Phase 50 COMPLETE"
  key_links:
    - from: "Real phone (Yuval's device via Tailscale)"
      to: "Live PM2 whatsapp-bot serving the new dashboard bundle"
      via: "Tailscale URL (e.g., http://100.124.47.99:3000) opened in mobile Safari + Chrome"
      pattern: "Tailscale.*phone"
    - from: "Live walkthrough checklist (this plan's tasks)"
      to: "Each MOBILE-XX requirement"
      via: "Each checklist step maps 1:1 to a CONTEXT success criterion; failures route into a follow-up gap-closure plan via /gsd:plan-phase 50 --gaps"
      pattern: "MOBILE-0[1-6]"
---

<objective>
Ship Phase 50 to production and verify all six MOBILE-XX requirements against live data on a real phone (Yuval's device via Tailscale to the PM2 bot). Then flip Phase 50 to complete across ROADMAP + REQUIREMENTS + STATE.

Purpose: Plans 50-01 through 50-05 delivered the code. This plan exercises the cross-device parity invariant: every change is verified on the real surface (a phone, not just devtools emulation). Devtools emulation cannot test iOS auto-zoom on input focus, real haptic feedback, real safe-area-inset values on a notched device, or the touch event sequence that leads to the swipe vs scroll vs long-press disambiguation. CONTEXT risks 1, 2, 3, 4 are all "test on a real touch device" — that's what this plan is.

Output: A running whatsapp-bot PM2 process serving the Phase 50 dashboard bundle, three updated planning docs reflecting Phase 50 closure, and an SUMMARY documenting the live walkthrough log (each MOBILE-XX step PASS / FAIL with evidence).
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-dashboard-mobile-ui-polish/50-CONTEXT.md
@.planning/phases/50-dashboard-mobile-ui-polish/50-01-SUMMARY.md
@.planning/phases/50-dashboard-mobile-ui-polish/50-02-SUMMARY.md
@.planning/phases/50-dashboard-mobile-ui-polish/50-03-SUMMARY.md
@.planning/phases/50-dashboard-mobile-ui-polish/50-04-SUMMARY.md
@.planning/phases/50-dashboard-mobile-ui-polish/50-05-SUMMARY.md

# Pattern — prior phases that did the same PM2 restart + owner walk-through
@.planning/phases/45-dashboard-pending-tasks-write-actions/45-04-PLAN.md
@.planning/phases/45-dashboard-pending-tasks-write-actions/45-04-SUMMARY.md
@.planning/phases/44-unified-editable-calendar/44-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + deploy + run all dashboard vitest suites green</name>
  <files>
    dashboard/dist
    logs/bot-error.log
  </files>
  <action>
    **Step A — run the dashboard vitest suites Plans 50-01/02/04 added:**
    ```bash
    cd /home/yuval/whatsapp-bot/dashboard
    npx vitest run
    ```
    Expected: 18 cases green = useViewport (3) + useCalendarViewMode (5) + useHorizontalSwipe (5) + useLongPress (5). If any fail, that's a regression — fix in the relevant plan's source before continuing. The dashboard does NOT inherit the server's "15 pre-existing failing tests" tolerance — every dashboard test introduced in this phase MUST be green.

    **Step B — server build (no source changes expected):**
    - `cd /home/yuval/whatsapp-bot && npx tsc --noEmit` — tolerates pre-existing cli/bot.ts + cli/commands/persona.ts rootDir noise per STATE.md convention.
    - Server vitest: SKIP. Phase 50 is dashboard-only — no server-side change.

    **Step C — dashboard production build:**
    - `cd /home/yuval/whatsapp-bot/dashboard && npx vite build` — deliberately `npx vite build` not `npm run build` per STATE.md convention (same tolerance used by Plans 43-02 / 44-04 / 44-06 / 45-04).
    - Record final bundle sizes. Expected delta vs Phase 45 baseline: +20-40 kB (new MonthDotsView + PillActionSheet + StickyActionBar + 4 hooks; no new dependency, no font, no large primitive).

    **Step D — DB baseline snapshot BEFORE restart (so MOBILE-04 reschedule verification can compare against):**
    ```bash
    sqlite3 data/bot.db "SELECT id, datetime(due_at/1000, 'unixepoch', 'localtime') AS due_local, task FROM actionables WHERE status='approved' AND due_at IS NOT NULL ORDER BY due_at LIMIT 5;"
    sqlite3 data/bot.db "SELECT id, datetime(scheduled_at/1000, 'unixepoch', 'localtime') AS sched_local, status FROM personal_pending_events WHERE scheduled_at >= unixepoch()*1000 ORDER BY scheduled_at LIMIT 5;"
    sqlite3 data/bot.db "SELECT id, datetime(scheduled_at/1000, 'unixepoch', 'localtime') AS sched_local, status FROM linkedin_posts WHERE status='approved' ORDER BY scheduled_at DESC LIMIT 5;"
    ```
    Record all three query results in the SUMMARY as the "pre-deploy baseline" — these rows are the candidates the live walkthrough will reschedule. **Pick a TASK pill (from the actionables query above) as the test pill** so step 21 has a single, well-defined source to query against.

    **Step E — whatsapp-bot PM2 restart:**
    - `pm2 restart whatsapp-bot` (NOT `--update-env` — STATE.md Phase 41-05 lesson: `--update-env` has historically tripped a `better-sqlite3 NODE_MODULE_VERSION` mismatch. The Node 20 pin in `ecosystem.config.cjs` from commit `f045cf9` is stable).
    - `pm2 logs whatsapp-bot --lines 80 --nostream`. Confirm:
      - `Approval system initialized` (Phase 41 init still fires)
      - `Calendar SSE stream initialized` (Phase 44 init still fires)
      - No better-sqlite3 mismatches
      - No new errors in `logs/bot-error.log` within 30s
    - The dashboard is served from `dashboard/dist/` by the same PM2 process — no separate restart needed.

    **Step F — auth-smoke from a desktop browser (sanity, not the real verification):**
    Open the Tailscale dashboard URL in a desktop browser and confirm `/calendar`, `/pending-tasks`, `/drafts`, `/overview` all render at desktop width. This is a regression check that Phase 44/45 still works — NOT the phone verification (that's Task 2).

    **Verify:**
    - `npx vitest run` in `dashboard/` — 18/18 green.
    - `npx vite build` succeeds; bundle size recorded.
    - PM2 logs clean within 30s.
    - All four pages render in desktop browser without console errors.
  </action>
  <verify>
    1. `cd dashboard && npx vitest run` — 18/18 green.
    2. `cd dashboard && npx vite build` — succeeds; bundle size recorded.
    3. `pm2 status whatsapp-bot` — online; uptime fresh.
    4. `tail -50 logs/bot-error.log` — no new errors since restart.
    5. Desktop browser walkthrough: 4 pages render, no console errors.
  </verify>
  <done>
    All dashboard vitest green, dashboard built, PM2 restarted, server logs clean, desktop browser sanity check passed. Ready for the phone walkthrough.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner walkthrough of all 6 MOBILE-XX requirements on a real phone via Tailscale</name>
  <files>
    .planning/phases/50-dashboard-mobile-ui-polish/50-06-SUMMARY.md
  </files>
  <action>
    **Context:** Phase 50 dashboard bundle is live (Task 1). Six MOBILE-XX requirements need real-phone verification — devtools emulation cannot replicate iOS Safari auto-zoom, real haptic feedback, true safe-area-inset values, or the touch-event sequencing that distinguishes swipe / scroll / long-press.

    **How to run this checkpoint:** Open the Tailscale dashboard URL on a phone (Yuval's iPhone OR an Android device — iOS preferred for safe-area + zoom checks). Mobile Safari + Chrome. Walk through the 26-check protocol below; mark each PASS / FAIL with a short note in the SUMMARY. Any FAIL routes into a gap-closure plan via `/gsd:plan-phase 50 --gaps`.

    **Pick the test pill BEFORE starting** — use the TASK pill identified in Task 1 step D (from the `actionables` query). That pill's `id` is the one step 21's sqlite query will read.

    ## MOBILE-01 — Global primitives (5 checks)

    1. **Tap target floor**: On any page with buttons (e.g., /pending-tasks Approve/Reject buttons, /calendar view toggle), tap each — feels comfortable, no fat-finger misses on a 5-button row.
    2. **iOS auto-zoom kill** (iOS Safari only): Open /pending-tasks, click Edit on any pending row to open the inline-edit textarea. Tap to focus the textarea. Confirm: viewport does NOT zoom in. (If this fails on iOS, the input/textarea text-base check has regressed.)
    3. **Safe-area-inset top** (notched device only — iPhone X or later): Open /calendar in landscape. The header should clear the notch — no UI hidden behind it.
    4. **Safe-area-inset bottom**: Open /drafts. If primary actions are pinned at bottom (StickyActionBar), they should clear the home indicator — not be visually under it.
    5. **useViewport orientation**: Rotate the phone from portrait to landscape on /calendar. Layout adjusts (e.g., view toggle re-flows, FAB still visible) without a refresh.

    ## MOBILE-02 — Calendar mobile view (6 checks)

    6. **Default view on phone**: Visit /calendar fresh (clear localStorage first via browser settings, OR open in private mode). Default view is **Day** — NOT Week, NOT Month.
    7. **View toggle filtered**: The view toggle pill shows **Day / 3-Day / Dots** only. No Week. No Month.
    8. **Swipe prev/next**: On Day view, swipe right-to-left across the day column area. Calendar advances to the next day (date in header updates). Swipe left-to-right — goes back one day.
    9. **Vertical scroll preservation on DayView (CONTEXT risk #1)**: Open DayView on a day with many items (>= 8 pills). Scroll the day column vertically (up/down). Calendar does **NOT** advance to next/previous day during the scroll. The swipe gesture only fires on near-horizontal motion (>=60px horizontal with <30px vertical drift); pure vertical scroll falls through to the browser. **If the day advances on a vertical scroll → FAIL — useHorizontalSwipe drift threshold regressed.**
    10. **3-Day view scroll**: Tap **3-Day** in the view toggle. Three day columns visible, horizontally scrollable.
    11. **MonthDotsView**: Tap **Dots** in the view toggle. 7-col dot grid renders. Each day shows up to 3 colored dots; days with >3 items show a +N badge. Tap a day with items — view switches to Day view for that date.

    ## MOBILE-03 — Calendar components responsive (6 checks)

    12. **CalendarHeader compact**: Header is a single compact row (no wrapped two-row layout).
    13. **CalendarPill tap target**: Pills are >=28px tall, tappable with no fat-finger misses.
    14. **CalendarPill no tooltip**: Tap-and-hold a pill briefly (~200ms — under the long-press threshold). No tooltip appears (long-press at 500ms is a different flow — checked in MOBILE-04 below).
    15. **DayView FAB**: A circular + button is visible bottom-right of /calendar in Day view. Tap it — opens the create flow (CreateItemPopover) as a bottom sheet.
    16. **DayOverflowPopover bottom-sheet**: On a day with >5 items, find the "+N more" affordance. Tap it — opens as a bottom sheet (slides up from bottom; NOT a small popover floating mid-screen).
    17. **CreateItemPopover bottom-sheet**: From step 15's create flow, the form opens as a bottom sheet. Form fields are tappable; the date input is the native iOS/Android date picker.

    ## MOBILE-04 — Long-press → action sheet (6 checks, **CRITICAL**)

    18. **Touch drag dead**: Try to drag a calendar pill on phone (touch + drag). Nothing happens — the pill does NOT detach + drag (Phase 44 desktop behavior). [If this drags, the !isMobile gate failed.]
    19. **Long-press opens sheet**: Long-press a pill (~500ms hold). PillActionSheet slides up from bottom. Buttons visible: Reschedule / Edit title / Delete (when applicable) / Cancel.
    20. **Long-press during scroll (CONTEXT risk #2)**: Start a vertical scroll on a list of pills, then continue holding the finger down mid-scroll without releasing. PillActionSheet does **NOT** appear (>8px movement cancels the long-press timer). Release without lifting and re-press to confirm the next intentional long-press still works. **If the sheet opens during a mid-scroll hold → FAIL — useLongPress moveThreshold regressed.**
    21. **Haptic on open** (vibrate-capable device): Step 19 — feel a short vibration when the sheet appears. (Skip if device has no vibrator — silent no-op is the spec.)
    22. **Reschedule via datetime-local**: Tap **Reschedule**. Native iOS/Android datetime picker appears, pre-set to the test pill's current slot. Pick a new time (e.g., +2 hours from now). Tap Save. The pill disappears + reappears at the new slot within 3s (SSE refresh). **Note the new wall-clock time you picked.**
    23. **CRITICAL — IST timezone correctness** (CONTEXT risk #4): The test pill is a TASK (per Task 1 step D — chosen from `actionables`). On the bot's PM2 host (`ssh yuval@server`), run the matching query for the **actionables** source:
        ```bash
        sqlite3 /home/yuval/whatsapp-bot/data/bot.db \
          "SELECT id, datetime(due_at/1000, 'unixepoch', 'localtime') AS due_local FROM actionables WHERE id = '<TEST_PILL_ID>';"
        ```
        (If a different source was chosen as the test pill, run the matching query instead:
        - source `event` → `personal_pending_events` table, `scheduled_at` column
        - source `linkedin` → `linkedin_posts` table, `scheduled_at` column

        Pick the ONE matching source; running all three is wasteful.)

        The `due_local` (or `sched_local`) value MUST match the IST wall-clock time you set in step 22 (e.g., if you picked 14:30 and the device is in IST, this prints `2026-04-XX 14:30:00`). If it prints `2026-04-XX 11:30:00` or `17:30:00`, the device-TZ assumption is wrong (or the device wasn't in IST) — that's a blocker for closing Phase 50 if the phone IS in IST.

    ## MOBILE-05 — Daily-driver pages (3 checks)

    24. **Overview metric grid stacks**: At phone width, /overview metric tiles are 1-col stacked, no value text wrapping.
    25. **PendingTasks action row fits**: At phone width, the Approve / Edit / Reject row on a pending-task card fits in one row at 320px width (test in landscape on a small phone, or rotate iPhone SE to portrait if available).
    26. **Drafts sticky actions**: At phone width, /drafts pins primary actions to the bottom (if applicable per Plan 50-05 SUMMARY); scrolling a long draft list keeps them visible; they clear the home indicator.

    ## Total: 26 checks. Track PASS/FAIL count.

    Resume with one of:
    - `approved` — all 26 PASS, proceed to closeout (Task 3).
    - `partial: <list of failed numbers>` — proceed to closeout for the PASSes, then immediately route the failures via `/gsd:plan-phase 50 --gaps`.
    - `blocked: <reason>` — stop; do NOT close Phase 50; describe the blocker.
  </action>
  <verify>
    Walkthrough log captured in 50-06-SUMMARY.md with PASS/FAIL marker per check (26 total). For step 23 (IST correctness), the exact sqlite output is pasted in the SUMMARY proving the rescheduled item's stored timestamp matches the wall-clock time picked on the phone, and the source/table/column used is documented.
  </verify>
  <done>
    User has typed `approved` (26/26 PASS) OR `partial: <list>` (some PASSed) OR `blocked: <reason>` (none verified). The walkthrough log is in 50-06-SUMMARY.md. Task 3 (closeout) only proceeds for the PASSed subset.
  </done>
  <resume-signal>Type "approved" / "partial: 9, 20, 23" / "blocked: ..."</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Closeout — REQUIREMENTS.md + ROADMAP.md + STATE.md</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <action>
    Only run this task if Task 2 returned `approved` OR `partial: ...` (for the PASSed subset). DO NOT close any requirement that didn't PASS in Task 2.

    **Step A — `.planning/REQUIREMENTS.md`:**

    1. In the **v2.0 Requirements** section (currently absent — Phase 50 is the seed for v2.0; create a new `## v2.0 Requirements` section if it doesn't exist, mirroring the v1.9 structure):
       ```markdown
       ## v2.0 Requirements

       Requirements for the **Dashboard UX Polish** milestone. Mobile-first dashboard pass: global primitives, Calendar mobile strategy, daily-driver page polish, live verification on a real phone.

       ### Mobile UI Polish

       - [x] **MOBILE-01**: Global mobile primitives shipped — 44px tap-target floor on Buttons, ≥16px input font (iOS auto-zoom kill on Inputs + Textareas), safe-area insets on AppLayout, `<StickyActionBar>` primitive, `useViewport()` hook _(Plan 50-01 — verified live in 50-06)_
       - [x] **MOBILE-02**: Calendar phone view router — DayView default with horizontal swipe prev/next (60px / 30px-drift handwritten gesture), 3-Day scrollable, `MonthDotsView` replacing MonthView; WeekView desktop-only _(Plan 50-02 — verified live in 50-06)_
       - [x] **MOBILE-03**: Calendar components responsive on phone — CalendarHeader compact row, CalendarPill 28px min, DayView single-column with + New FAB, DayOverflowPopover + CreateItemPopover + InlineTitleEdit as bottom sheets _(Plan 50-03 — verified live in 50-06)_
       - [x] **MOBILE-04**: Long-press → PillActionSheet replaces touch drag-and-drop on phone; reschedule via native `<input type="datetime-local">` interpreted in device-local TZ (IST); reuses `useRescheduleMutation()`; delete forwarded to existing onDelete prop; desktop drag preserved with !past && !ghost predicates intact _(Plan 50-04 — verified live in 50-06)_
       - [x] **MOBILE-05**: Daily-driver pages mobile audit — Overview 1-col metric stack at <sm, PendingTasks 320px-safe action row, Drafts primary actions in StickyActionBar where applicable _(Plan 50-05 — verified live in 50-06)_
       - [x] **MOBILE-06**: Live walkthrough on a real phone passes against the live PM2 bot — 26-check protocol covers swipe nav, swipe-vs-vertical-scroll disambiguation, long-press-during-scroll, dot-month tap-to-day, long-press → reschedule with IST correctness, iOS zoom-on-focus test, safe-area on notched device _(Plan 50-06 — this requirement is 'verification passed')_
       ```

    2. In the **v2.0 Traceability** section (also new — create at the top alongside v1.9 Traceability):
       ```markdown
       ### v2.0 Traceability

       | Requirement | Phase | Status |
       |-------------|-------|--------|
       | MOBILE-01 | Phase 50 | Complete (2026-XX-XX — useViewport + StickyActionBar + safe-area insets + Button tap-target floor + Input/Textarea text-base, all live-verified on iPhone via Tailscale; see 50-06-SUMMARY.md MOBILE-01) |
       | MOBILE-02 | Phase 50 | Complete (2026-XX-XX — Day default + swipe prev/next + vertical-scroll preservation + 3-Day + MonthDotsView dot grid live-verified; WeekView never mounts on phone; see 50-06-SUMMARY.md MOBILE-02) |
       | MOBILE-03 | Phase 50 | Complete (2026-XX-XX — CalendarHeader / Pill / DayView / DayOverflowPopover / CreateItemPopover / InlineTitleEdit responsive live-verified; see 50-06-SUMMARY.md MOBILE-03) |
       | MOBILE-04 | Phase 50 | Complete (2026-XX-XX — long-press 500ms + scroll-cancel + PillActionSheet + datetime-local picker live-verified; touch drag dead; **CONTEXT risk #4 IST correctness verified by sqlite query against rescheduled item**; see 50-06-SUMMARY.md MOBILE-04) |
       | MOBILE-05 | Phase 50 | Complete (2026-XX-XX — Overview / PendingTasks / Drafts mobile audit live-verified at 320px width; see 50-06-SUMMARY.md MOBILE-05) |
       | MOBILE-06 | Phase 50 | Complete (2026-XX-XX — 26/26 walkthrough checks PASS on real phone via Tailscale to PM2 bot; see 50-06-SUMMARY.md walkthrough log) |
       ```
       Replace `2026-XX-XX` with today's date.

    3. **v2.0 Coverage** footer:
       ```markdown
       **v2.0 Coverage:**
       - v2.0 requirements: 6 total
       - Mapped to phases: 6 (Phase 50)
       - Unmapped: 0 ✓
       ```

    **Step B — `.planning/ROADMAP.md`:**

    1. Find the line `- [ ] **Phase 50: Dashboard Mobile UI Polish**` (currently in v2.0 Dashboard UX Polish milestone block) — flip checkbox to `[x]`.
    2. Find Phase 50's `**Requirements:**` line — currently `_TBD — define via /gsd:discuss-phase 50 before planning_` — replace with: `**Requirements:** MOBILE-01, MOBILE-02, MOBILE-03, MOBILE-04, MOBILE-05, MOBILE-06`.
    3. Find Phase 50 in the Progress table (last row) — `| 50. Dashboard Mobile UI Polish | v2.0 | 0/6 | Not started | — |` — update to `| 50. Dashboard Mobile UI Polish | v2.0 | 6/6 | Complete | 2026-XX-XX |`.
    4. Add the per-plan list under Phase 50's success criteria block (mirroring the Phase 45 / 44 pattern):
       ```markdown
       **Plans:** 6/6 plans complete

       Plans:
       - [x] 50-01-PLAN.md — Global mobile primitives: useViewport + StickyActionBar + safe-area insets + Button tap-target floor + Input/Textarea iOS zoom kill (see `.planning/phases/50-dashboard-mobile-ui-polish/50-01-SUMMARY.md`)
       - [x] 50-02-PLAN.md — Calendar view router + useCalendarViewMode + useHorizontalSwipe + MonthDotsView (see `.planning/phases/50-dashboard-mobile-ui-polish/50-02-SUMMARY.md`)
       - [x] 50-03-PLAN.md — Calendar components responsive pass: Header / Pill / DayView / DayOverflowPopover / CreateItemPopover / InlineTitleEdit (see `.planning/phases/50-dashboard-mobile-ui-polish/50-03-SUMMARY.md`)
       - [x] 50-04-PLAN.md — Long-press → PillActionSheet + useLongPress; touch DnD removed; reschedule via native datetime-local in IST (see `.planning/phases/50-dashboard-mobile-ui-polish/50-04-SUMMARY.md`)
       - [x] 50-05-PLAN.md — Daily-driver pages: Overview / PendingTasks / Drafts mobile audit (see `.planning/phases/50-dashboard-mobile-ui-polish/50-05-SUMMARY.md`)
       - [x] 50-06-PLAN.md — Live verification on real phone + ROADMAP / REQUIREMENTS / STATE closeout (see `.planning/phases/50-dashboard-mobile-ui-polish/50-06-SUMMARY.md`)
       ```

    **Step C — `.planning/STATE.md`:** Read the head of STATE.md to find the Current Position block. Add (or replace if already present) a Phase 50 entry: "Phase 50 COMPLETE 2026-XX-XX — Dashboard mobile UI shipped + live-verified on iPhone via Tailscale; v2.0 milestone seeded; next target: Phase 46 Google Tasks Full-List Sync (v1.9 backfill) OR Phase 51 (next v2.0 polish phase, TBD)." Match the formatting STATE.md already uses for prior phase closeouts (Phase 45 entry as the template).

    **Verify:**
    - `grep -n "Phase 50" .planning/ROADMAP.md` — checkbox is `[x]`, Progress table shows Complete + date.
    - `grep -n "MOBILE-0[1-6]" .planning/REQUIREMENTS.md` — six requirement IDs present in v2.0 section AND v2.0 Traceability table.
    - `grep -n "Phase 50.*COMPLETE\|Phase 50.*complete" .planning/STATE.md` — entry present.

    **Commit:**
    ```bash
    node /home/yuval/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(50-06): close Phase 50 — Dashboard Mobile UI Polish, MOBILE-01..06 complete via live phone walkthrough" --files .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/phases/50-dashboard-mobile-ui-polish/50-06-SUMMARY.md
    ```
    Note: `.planning/` is gitignored — gsd-tools commit handles the `git add -f` automatically OR add manually if needed (per STATE.md convention).

    **If Task 2 returned `partial: ...`:** Only flip MOBILE-XX rows to Complete for the requirements whose checks all PASSed. Leave failed requirements as `[ ]` and immediately call out the gap-closure path: "Run `/gsd:plan-phase 50 --gaps` to address: <list of failed checks>". The phase as a whole stays open until those are addressed.
  </action>
  <verify>
    1. `grep -n "Phase 50" .planning/ROADMAP.md` shows `[x]` checkbox + Complete in Progress table.
    2. `grep -n "MOBILE-0" .planning/REQUIREMENTS.md` shows six rows in v2.0 Traceability with Complete status (or fewer if partial).
    3. STATE.md has a Phase 50 closeout entry with today's date.
    4. Commit lands with the docs message; `git log --oneline -1` shows the closeout.
  </verify>
  <done>
    Phase 50 is closed (or explicitly partial-closed with the gap-closure path documented). REQUIREMENTS.md has the v2.0 section seeded. ROADMAP.md reflects 6/6 plans complete. STATE.md reflects Phase 50 closure + next target.
  </done>
</task>

</tasks>

<verification>
Phase-level check after all three tasks:
- All 18 dashboard vitest cases green.
- PM2 bot online with the new bundle deployed.
- Phone walkthrough: 26/26 PASS (or documented partial with gap-closure path).
- ROADMAP.md Phase 50 closed.
- REQUIREMENTS.md v2.0 section seeded with MOBILE-01..06 traceability.
- STATE.md updated.
- Git commit landed.
</verification>

<success_criteria>
- Live PM2 bot serves Phase 50 dashboard bundle without errors.
- All six MOBILE-XX requirements verified on a real phone (or partial-closed with explicit gap-closure path).
- ROADMAP.md, REQUIREMENTS.md, STATE.md updated to reflect Phase 50 closure.
- v2.0 Requirements section in REQUIREMENTS.md is seeded with MOBILE-01..06.
- Git commit lands with the closeout message.
- CONTEXT risk #1 (swipe vs vertical scroll) verified by walkthrough step 9.
- CONTEXT risk #2 (long-press during scroll) verified by walkthrough step 20.
- CONTEXT risk #4 (IST timezone correctness on datetime-local round-trip) verified by direct sqlite query against the matching source's table for the rescheduled test pill.
- Feeds MOBILE-06 (live walkthrough on real phone passes).
</success_criteria>

<output>
After completion, create `.planning/phases/50-dashboard-mobile-ui-polish/50-06-SUMMARY.md` documenting:
- Bundle size delta vs Phase 45 baseline.
- vitest counts: 18/18 dashboard tests green.
- DB baseline snapshot (pre-deploy) and post-walkthrough delta (proves the reschedule actually persisted) — including which source/table the test pill came from.
- Walkthrough log: each of the 26 checks marked PASS / FAIL with a one-line note + (where applicable) screenshot link or sqlite output capture.
- Specifically for MOBILE-04 step 23: paste the exact sqlite query output proving IST correctness, plus which table/column was queried (matching the test pill's source).
- Specifically for MOBILE-02 step 9 and MOBILE-04 step 20: explicit PASS notes covering the two CONTEXT risks (swipe-vs-scroll and long-press-during-scroll).
- Closeout commit hash.
- Confirmation REQUIREMENTS.md v2.0 section is correctly seeded (didn't stomp on v1.9 / v1.8 / v1.7 sections).
- Hand-off note for whoever picks up next: Phase 50 closes the seed of the v2.0 Dashboard UX Polish milestone. Next phase candidates are (a) Phase 46 v1.9 backfill (Google Tasks Full-List Sync), (b) the next v2.0 polish phase (TBD — see deferred items in 50-CONTEXT.md: LinkedIn workflow mobile pass, theme refresh, perf).
- Confirmation `.planning/` docs were added via `git add -f` (or via gsd-tools commit which handles it automatically).
</output>
