---
phase: 50-dashboard-mobile-ui-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "50-01"
files_modified:
  - dashboard/src/components/calendar/CalendarHeader.tsx
  - dashboard/src/components/calendar/CalendarPill.tsx
  - dashboard/src/components/calendar/DayView.tsx
  - dashboard/src/components/calendar/DayOverflowPopover.tsx
  - dashboard/src/components/calendar/CreateItemPopover.tsx
  - dashboard/src/components/calendar/InlineTitleEdit.tsx
  - dashboard/src/components/calendar/WeekView.tsx
autonomous: true
requirements:
  - MOBILE-03

must_haves:
  truths:
    - "CalendarHeader collapses on phone into a single compact row: title (truncated with ellipsis if narrow), date picker, and view toggle as a 3-segment pill (Day / 3-Day / Dots) with each segment >=44px tall; on desktop the existing layout is unchanged"
    - "CalendarPill on phone has min-height 28px, no hover tooltip (touch devices treat tap as click — tooltip would block the action), source color stays as the left accent bar; pills stack vertically on dense days; tap target is the entire pill, not just the title text"
    - "DayView on phone is a full-width single column (md:grid-cols-N → grid-cols-1 below md), hour rows grow to ~64px (up from desktop ~48px) so the touch target inside each hour is comfortable, and a floating + New item FAB (size-14 bottom-right with safe-area-inset awareness) replaces the desktop empty-slot click affordance which is hard to discover on touch"
    - "DayOverflowPopover renders as a Radix Dialog in bottom-sheet style on phone (full width, slides up from bottom, dismissable via backdrop tap or close button); on desktop renders as the existing Radix Popover anchored to the +N pill"
    - "CreateItemPopover renders as a Radix Dialog in bottom-sheet style on phone with the same form contents (recipient/type/title/datetime); on desktop renders as the existing Radix Popover anchored to the slot"
    - "InlineTitleEdit on phone promotes from inline-positioned edit to a bottom-sheet dialog when the inline editor would overlap the viewport edge OR pill — the heuristic is simply isMobile (any overlap risk on phone is enough to switch); on desktop stays inline as today"
    - "WeekView is desktop-only (Plan 50-02 already filters it out of the mobile view toggle); this plan applies a small responsive cleanup so any accidental mounting at narrow widths still doesn't crash, but does NOT add a phone-friendly week layout"
  artifacts:
    - path: "dashboard/src/components/calendar/CalendarHeader.tsx"
      provides: "Phone layout: single compact row, title truncate, view toggle as 3-segment pill driven by availableViews prop from Plan 50-02; desktop layout unchanged"
      contains: "isMobile\\|useViewport"
    - path: "dashboard/src/components/calendar/CalendarPill.tsx"
      provides: "Phone tweaks: min-h-7 (28px) + tap-target full-pill, no tooltip when isMobile, vertical stack class branch; desktop pill unchanged; source color left bar unchanged"
      contains: "isMobile\\|useViewport"
    - path: "dashboard/src/components/calendar/DayView.tsx"
      provides: "Phone tweaks: grid-cols-1, hour-row taller (h-16), floating + New FAB in the bottom-right with mb-[calc(env(safe-area-inset-bottom)+1rem)] mr-4 — opens the same CreateItemPopover that desktop slot-click opens"
      contains: "isMobile\\|FAB\\|useViewport"
    - path: "dashboard/src/components/calendar/DayOverflowPopover.tsx"
      provides: "Phone branch: returns a Radix Dialog (bottom-sheet positioned) instead of Popover; desktop branch returns existing Popover; both render the same overflow item list"
      contains: "isMobile.*Dialog\\|Dialog.*isMobile"
    - path: "dashboard/src/components/calendar/CreateItemPopover.tsx"
      provides: "Phone branch: returns a Radix Dialog (bottom-sheet positioned) wrapping the same form; desktop branch returns existing Popover; form internals unchanged"
      contains: "isMobile.*Dialog\\|Dialog.*isMobile"
    - path: "dashboard/src/components/calendar/InlineTitleEdit.tsx"
      provides: "Phone branch: when isMobile, promotes the editor into a Dialog instead of inline; desktop stays inline; save/cancel handlers unchanged"
      contains: "isMobile.*Dialog\\|Dialog.*isMobile"
    - path: "dashboard/src/components/calendar/WeekView.tsx"
      provides: "Minor responsive cleanup — guarded against narrow-width crash; no phone layout (filtered out by view router on phone per Plan 50-02)"
      contains: "WeekView"
  key_links:
    - from: "dashboard/src/components/calendar/CalendarHeader.tsx"
      to: "dashboard/src/hooks/useViewport.ts"
      via: "useViewport().isMobile drives the layout switch + view-toggle 3-segment pill style"
      pattern: "useViewport\\(\\)"
    - from: "dashboard/src/components/calendar/DayView.tsx FAB"
      to: "dashboard/src/components/calendar/CreateItemPopover.tsx (or whatever opens it)"
      via: "FAB onClick triggers the same create-item flow that desktop slot-click triggers — single source of truth for create logic"
      pattern: "FAB.*onClick\\|onClick.*createItem"
    - from: "dashboard/src/components/calendar/DayOverflowPopover.tsx"
      to: "@radix-ui dialog primitives"
      via: "isMobile branch wraps content in <Dialog.Root><Dialog.Content className='fixed bottom-0 ...'> bottom-sheet pattern (NOT importing react-bottom-sheet — handwritten with Radix Dialog + Tailwind classes, per CONTEXT lock 'no date-picker / no extra libs')"
      pattern: "DialogContent\\|@radix-ui/react-dialog"
    - from: "dashboard/src/components/calendar/CreateItemPopover.tsx"
      to: "@radix-ui dialog primitives"
      via: "Same isMobile-Dialog vs desktop-Popover branch as DayOverflowPopover"
      pattern: "DialogContent\\|@radix-ui/react-dialog"
---

<objective>
Make every calendar component (header, pill, DayView, two popovers, inline edit, weekview cleanup) usable on a phone. The view router from Plan 50-02 routes between views; this plan polishes each view so the things INSIDE the view don't crowd, clip, or under-target a touch finger.

Purpose: Plan 50-02 substituted MonthDotsView for MonthView and added swipe — the calendar is reachable on phone but the components themselves still assume mouse + 1024px+ width. Six components need a viewport-aware branch each. The change pattern is uniform: `useViewport()` → `isMobile ? mobile-layout : desktop-layout`. No new dependencies (CONTEXT lock).

Output: Six modified calendar components + one minor cleanup on WeekView. No new files. No backend change. Desktop behavior unchanged everywhere — the existing Phase 44 surface continues to work byte-identically at >=768px wide.
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-dashboard-mobile-ui-polish/50-CONTEXT.md
@docs/superpowers/specs/2026-04-20-dashboard-mobile-ui-design.md

# Plan 50-01 hand-off — useViewport, StickyActionBar, safe-area utilities ready
@.planning/phases/50-dashboard-mobile-ui-polish/50-01-SUMMARY.md
@dashboard/src/hooks/useViewport.ts
@dashboard/src/components/ui/StickyActionBar.tsx

# Components being modified — read FIRST to map current desktop layout before adding the isMobile branch
@dashboard/src/components/calendar/CalendarHeader.tsx
@dashboard/src/components/calendar/CalendarPill.tsx
@dashboard/src/components/calendar/DayView.tsx
@dashboard/src/components/calendar/DayOverflowPopover.tsx
@dashboard/src/components/calendar/CreateItemPopover.tsx
@dashboard/src/components/calendar/InlineTitleEdit.tsx
@dashboard/src/components/calendar/WeekView.tsx

# Existing Dialog + Popover primitives — both already shipped via shadcn
@dashboard/src/components/ui/dialog.tsx
@dashboard/src/components/ui/popover.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CalendarHeader + CalendarPill + DayView mobile branches</name>
  <files>
    dashboard/src/components/calendar/CalendarHeader.tsx
    dashboard/src/components/calendar/CalendarPill.tsx
    dashboard/src/components/calendar/DayView.tsx
  </files>
  <action>
    Read each file first; the changes below are the patterns, not the literal diffs (the files have layout-specific class strings the planner can't predict without reading them).

    **Step A — `CalendarHeader.tsx`:**
    1. Add `import { useViewport } from '@/hooks/useViewport';` and `const { isMobile } = useViewport();`.
    2. The current desktop layout is likely a 3-section grid (left: title + date picker, center: nav, right: view toggle) with `md:flex` plus `gap-4` styling. Add a phone branch:
       ```tsx
       if (isMobile) {
         return (
           <div className="flex items-center gap-2 px-3 py-2">
             <button onClick={onPrev} className="h-10 w-10 ...">‹</button>
             <button onClick={onNext} className="h-10 w-10 ...">›</button>
             <h2 className="flex-1 truncate text-sm font-medium">{title}</h2>
             <ViewTogglePillMobile views={availableViews} value={view} onChange={setView} />
           </div>
         );
       }
       ```
       Where `ViewTogglePillMobile` is a small inline subcomponent (or rendered inline) that renders the filtered `availableViews` from Plan 50-02 as a 3-segment pill — each segment is a `<button>` with `h-10 px-3 text-xs` and selected state uses `bg-background shadow-sm` inside a `bg-muted rounded-full` parent.
    3. Date-picker on phone: skip the calendar-icon mini-popover; tapping the title opens the same date picker the desktop title uses. Keep it simple — one tap target.
    4. Desktop layout untouched.

    **Step B — `CalendarPill.tsx`:**
    1. Add `useViewport()`. The pill probably has a `variant`-driven class string already; add a `mobile-tweaks` block at the top:
       ```tsx
       const { isMobile } = useViewport();
       const mobileClasses = isMobile ? 'min-h-7 text-xs px-2 py-1' : '';
       ```
    2. Tooltip suppression: if there's a `<Tooltip>` wrapper, gate it on `!isMobile`:
       ```tsx
       const inner = <PillInner ... />;
       return isMobile ? inner : <Tooltip>...{inner}...</Tooltip>;
       ```
    3. The source-color left accent bar is already there in desktop — keep it on phone (same class). The CONTEXT lock requires color discrimination on phone too.
    4. Vertical stacking on dense days is mostly a parent-grid concern (DayView decides the column layout); if CalendarPill has a horizontal flex layout that breaks at narrow widths, swap to `flex-col items-start` when isMobile.

    **Step C — `DayView.tsx`:**
    1. Add `useViewport()`.
    2. The current desktop layout is likely a multi-column grid (e.g., `grid-cols-[60px_1fr]` for time + content, OR `grid-cols-3` for week-style). On phone:
       - Force grid-cols-1 (single column, full-width).
       - Hour rows: bump from existing height to `h-16` (64px) on phone — `h-12 md:h-12` → `h-16 md:h-12`.
    3. Add the floating + New FAB:
       ```tsx
       {isMobile && (
         <button
           type="button"
           onClick={openCreateAtFocusedSlot}
           className={cn(
             'fixed z-30 right-4 size-14 rounded-full shadow-lg',
             'bg-primary text-primary-foreground hover:bg-primary/90',
             'flex items-center justify-center',
             'bottom-[calc(env(safe-area-inset-bottom)+1rem)]',
           )}
           aria-label="New item"
         >
           <PlusIcon className="size-6" />
         </button>
       )}
       ```
       `openCreateAtFocusedSlot` should call WHATEVER existing handler the desktop empty-slot click invokes (read the file to find it; likely `handleCreate(focusDate)` or similar). Default the new item's slot to `focusDate` at the next round hour. DO NOT introduce a new mutation; reuse the existing create flow.
    4. Z-30 keeps the FAB above content but under StickyActionBar (z-40) and toasts (z-50).

    **Verify:**
    - `npx tsc -b` — zero new errors.
    - `npx vite build` — succeeds.
    - `grep -n "useViewport" src/components/calendar/CalendarHeader.tsx src/components/calendar/CalendarPill.tsx src/components/calendar/DayView.tsx` — all three present.
    - `grep -n "size-14.*rounded-full\|FAB" src/components/calendar/DayView.tsx` — FAB present.
    - Manual desktop check: open `/calendar` at >=768px wide — Header / Pill / DayView render exactly as before (no FAB visible, full toggle visible, tooltip on pill works).
  </action>
  <verify>
    1. `npx tsc -b` — zero new errors.
    2. `npx vite build` — succeeds.
    3. `grep -c "useViewport" src/components/calendar/CalendarHeader.tsx src/components/calendar/CalendarPill.tsx src/components/calendar/DayView.tsx` — 1 each.
    4. `grep -n "env(safe-area-inset-bottom)" src/components/calendar/DayView.tsx` — FAB respects safe area.
    5. Manual phone-emulation in devtools (375x667): Header is one row + pill toggle, DayView is single column with FAB, pills are stacked vertically with 28px min height.
  </verify>
  <done>
    Three components have isMobile branches; desktop layouts unchanged; FAB on DayView opens the same create flow as desktop slot-click.
  </done>
</task>

<task type="auto">
  <name>Task 2: DayOverflowPopover + CreateItemPopover + InlineTitleEdit + WeekView cleanup</name>
  <files>
    dashboard/src/components/calendar/DayOverflowPopover.tsx
    dashboard/src/components/calendar/CreateItemPopover.tsx
    dashboard/src/components/calendar/InlineTitleEdit.tsx
    dashboard/src/components/calendar/WeekView.tsx
  </files>
  <action>
    All three of the popover/inline files use the same pattern: `isMobile ? <Dialog ... bottom-sheet style> : <Popover ... />` wrapping the same content body. Read each file first to extract the inner content as a function or fragment, then conditionally wrap.

    **Step A — `DayOverflowPopover.tsx`:**
    1. Import: `useViewport`, `Dialog, DialogContent, DialogHeader, DialogTitle, DialogClose` from `@/components/ui/dialog`.
    2. Extract whatever JSX is inside the existing `<PopoverContent>` into a local `body` variable / function.
    3. Pattern:
       ```tsx
       const { isMobile } = useViewport();
       const body = (<div className="space-y-2">{items.map(...)}</div>);

       if (isMobile) {
         return (
           <Dialog open={open} onOpenChange={onOpenChange}>
             <DialogContent className="fixed bottom-0 left-0 right-0 top-auto translate-x-0 translate-y-0 max-w-none rounded-b-none rounded-t-2xl pb-[max(env(safe-area-inset-bottom),1rem)] data-[state=open]:slide-in-from-bottom data-[state=closed]:slide-out-to-bottom">
               <DialogHeader>
                 <DialogTitle>More items — {dateLabel}</DialogTitle>
               </DialogHeader>
               {body}
             </DialogContent>
           </Dialog>
         );
       }
       return (
         <Popover open={open} onOpenChange={onOpenChange}>
           <PopoverTrigger>{trigger}</PopoverTrigger>
           <PopoverContent>{body}</PopoverContent>
         </Popover>
       );
       ```
       The Tailwind class string on `DialogContent` overrides shadcn's default centered-dialog positioning so the dialog snaps to the bottom edge — this is the "Dialog in bottom-sheet mode" CONTEXT lock. The shadcn `DialogContent` already has slide-in/out animations; the override classes only change position + max-width + rounded corners.

    **Step B — `CreateItemPopover.tsx`:** Same pattern as Step A. Extract the form body, wrap in Dialog on phone / Popover on desktop. The form's submit handler is unchanged. The Plan 50-01 `<StickyActionBar>` is NOT used here — the dialog already provides its own footer container; just put the Submit + Cancel buttons inside the dialog footer with the same styling shadcn dialogs use.

    **Step C — `InlineTitleEdit.tsx`:** Same pattern but with one twist — the desktop version positions itself absolutely over the pill being edited; on phone, instead of the absolute positioning, render a Dialog with a single `<input>` + Save/Cancel buttons. Pre-fill the input with the current title; Save calls the same mutation the inline edit currently calls.

    **Step D — `WeekView.tsx`:** Minor cleanup only.
    - Add a top guard: `if (window.innerWidth < 768) return null;` is the wrong pattern (re-renders on every state change). Instead, the `useCalendarViewMode` filter from Plan 50-02 already ensures WeekView is unreachable on phone. So the cleanup here is just: scan for any `min-w-[1024px]` or fixed-pixel widths that would crash if the phone-emulation devtools accidentally mounted WeekView, and either remove them or wrap in `md:` prefix. Read the file; if no fixed widths exist, this step is a no-op (just a one-line comment: `// WeekView is desktop-only — view router in Calendar.tsx filters it on phone (Plan 50-02)`).

    **Verify:**
    - `npx tsc -b` — zero new errors.
    - `npx vite build` — succeeds.
    - `grep -n "isMobile.*Dialog\|Dialog.*bottom-0" src/components/calendar/DayOverflowPopover.tsx src/components/calendar/CreateItemPopover.tsx src/components/calendar/InlineTitleEdit.tsx` — bottom-sheet branch present in all three.
    - Manual desktop check (>=768px): the three popovers still open as Popovers anchored to their triggers; no Dialog is mounted; behavior matches Phase 44.
    - Manual phone-emulation (375x667): the three popovers open as bottom sheets that slide up; tapping the backdrop dismisses; safe-area inset clears the home indicator.

    **Commit order (4 atomic commits expected):**
    - `feat(50-03): CalendarHeader phone layout (single row + 3-segment view pill)`
    - `feat(50-03): CalendarPill + DayView phone layout (28px min, full-width column, +New FAB)`
    - `feat(50-03): bottom-sheet branch for DayOverflowPopover + CreateItemPopover + InlineTitleEdit on phone`
    - `chore(50-03): WeekView responsive cleanup (no functional change)`
  </action>
  <verify>
    1. `npx tsc -b` — zero new errors.
    2. `npx vite build` — succeeds.
    3. `grep -l "isMobile" src/components/calendar/DayOverflowPopover.tsx src/components/calendar/CreateItemPopover.tsx src/components/calendar/InlineTitleEdit.tsx` — all three.
    4. `grep -n "fixed bottom-0\|bottom-0 left-0 right-0" src/components/calendar/DayOverflowPopover.tsx src/components/calendar/CreateItemPopover.tsx src/components/calendar/InlineTitleEdit.tsx` — bottom-sheet positioning present.
    5. Manual emulation pass: all three popovers correctly switch presentation between desktop (popover) and phone (bottom sheet).
  </verify>
  <done>
    Three popovers/edits switch to bottom-sheet on phone; WeekView guarded for accidental mounts; desktop unchanged in all four.
  </done>
</task>

</tasks>

<verification>
Phase-level check after both tasks:
- `npx tsc -b` — zero new errors.
- `npx vite build` — succeeds.
- All seven calendar files have either `useViewport` import (six of them) OR the desktop-only cleanup comment (WeekView).
- Manual desktop regression: open `/calendar` at >=768px — Phase 44 behavior intact (drag, inline edit, create-from-slot, day overflow popover all work as before; toggle still shows Month / Week / Day).
- Manual phone-emulation regression: all six interactive surfaces are tappable, tap-targets >=40px, popovers don't clip the viewport edge, FAB visible on DayView and triggers create flow.
</verification>

<success_criteria>
- CalendarHeader: single compact row + 3-segment pill toggle on phone; desktop unchanged.
- CalendarPill: 28px min, no tooltip on phone, source-color accent bar preserved.
- DayView: single column, taller hour rows, + New FAB with safe-area awareness; reuses existing create handler.
- DayOverflowPopover, CreateItemPopover: bottom-sheet on phone, Popover on desktop; same body content.
- InlineTitleEdit: bottom-sheet on phone, inline on desktop; same save mutation.
- WeekView: cleanup comment + any fixed-width fix; not reachable on phone (filtered in router).
- Feeds MOBILE-03 (header / pill / DayView / DayOverflowPopover / CreateItemPopover / InlineTitleEdit responsive).
</success_criteria>

<output>
After completion, create `.planning/phases/50-dashboard-mobile-ui-polish/50-03-SUMMARY.md` documenting:
- Files touched + line deltas (each component should add 20-50 lines for the isMobile branch).
- Commit hashes (4 atomic commits expected).
- Bundle size before/after.
- Decision log: any place a Dialog branch produced UX regressions on desktop and how it was scoped (the pattern should be branchless on desktop — note any deviation).
- Confirmation desktop Phase 44 behavior is intact (regression evidence: drag-and-drop a pill on desktop still works, inline edit on desktop still positions absolutely).
- Hand-off note for Plan 50-04: CalendarPill now has the isMobile guard; long-press handler from Plan 50-04 should attach inside the same isMobile branch (so desktop stays draggable, phone gets long-press → action sheet). The pill's tap handler currently opens the existing detail/edit affordance — long-press is ADDITIVE, not replacing the tap.
- Confirmation `.planning/` docs were added via `git add -f`.
</output>
