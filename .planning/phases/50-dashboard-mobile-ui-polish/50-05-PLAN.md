---
phase: 50-dashboard-mobile-ui-polish
plan: 05
type: execute
wave: 2
depends_on:
  - "50-01"
files_modified:
  - dashboard/src/pages/Overview.tsx
  - dashboard/src/pages/PendingTasks.tsx
  - dashboard/src/pages/Drafts.tsx
autonomous: true
requirements:
  - MOBILE-05

must_haves:
  truths:
    - "Overview at <768px collapses its 2-col metric grid to 1-col (grid-cols-1 md:grid-cols-2); each metric tile keeps icon + value + label visible without text wrap; on <375px (very narrow), the metric value text scales down one Tailwind step (text-2xl → text-xl) so 3-digit numbers don't wrap"
    - "PendingTasks at <768px: each PendingActionableCard's inline edit textarea is full-width (no max-w on phone); the 3-button action row (Approve / Edit / Reject) doesn't wrap at 320px (use flex-wrap-protected sizing — gap-2, min-w-0 on text spans, equal-width grid-cols-3 instead of flex on phone if needed); horizontal padding of the page reduces from p-6 to p-3 to give more card width"
    - "Drafts at <768px: when there is at least one primary action visible (e.g. 'Send all' / 'Regenerate' for a queue of drafts), it sits inside <StickyActionBar> from Plan 50-01 so it stays reachable while the user scrolls a long draft list; on desktop (>=md) the StickyActionBar renders inline (its built-in behavior — Plan 50-01 hand-off)"
    - "All three pages remain byte-identical on desktop (>=768px): the only changes are Tailwind responsive prefixes that activate below md"
    - "No new mutation, no new data fetch, no new layout primitive beyond Plan 50-01's StickyActionBar — this is a polish-only pass"
  artifacts:
    - path: "dashboard/src/pages/Overview.tsx"
      provides: "Mobile responsive: grid-cols-1 md:grid-cols-2 (or whatever the current desktop col-count is) on the metric grid; tile text scales down at <sm; touch targets >=44px on tile-as-button if applicable"
      contains: "grid-cols-1.*md:grid-cols\\|max-sm:"
    - path: "dashboard/src/pages/PendingTasks.tsx"
      provides: "Mobile responsive on PendingActionableCard sizing + page padding; inline-edit textarea full-width on phone; action row safely sized for 320px"
      contains: "p-3.*md:p-6\\|md:p-6.*p-3\\|grid-cols-3"
    - path: "dashboard/src/pages/Drafts.tsx"
      provides: "Mobile responsive: primary actions wrapped in <StickyActionBar>; card list uses single-column flow on phone"
      contains: "StickyActionBar"
  key_links:
    - from: "dashboard/src/pages/Drafts.tsx"
      to: "dashboard/src/components/ui/StickyActionBar.tsx"
      via: "<StickyActionBar><Button>Send all</Button>...</StickyActionBar> wraps the existing primary-action row"
      pattern: "StickyActionBar"
    - from: "dashboard/src/pages/PendingTasks.tsx (action row)"
      to: "PendingActionableCard internal action layout"
      via: "Plan 45-03 already established the card layout; Plan 50-05 only adjusts page-level padding + grid breakpoints, NOT card internals (cards already stack reasonably per CONTEXT)"
      pattern: "PendingActionableCard"
---

<objective>
Audit and polish the three daily-driver pages (Overview, PendingTasks, Drafts) so they don't crowd, wrap, or hide affordances on a phone. This is the smallest of the Phase 50 plans by file count (3 files) but reaches every page that isn't the calendar — the calendar is Plans 50-02..04.

Purpose: Plan 45 shipped PendingTasks recently and the cards already stack reasonably per CONTEXT. Overview has a metric grid that wraps awkwardly at <375px. Drafts is short (69 lines today) and just needs `<StickyActionBar>` for its primary actions. None of these need new components — Plan 50-01 already shipped StickyActionBar; the rest is Tailwind responsive prefixes.

Output: Three pages with phone-friendly responsive prefixes. No new files. No new dependencies. Desktop layout unchanged.
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-dashboard-mobile-ui-polish/50-CONTEXT.md
@docs/superpowers/specs/2026-04-20-dashboard-mobile-ui-design.md

# Plan 50-01 hand-off — StickyActionBar + useViewport ready
@.planning/phases/50-dashboard-mobile-ui-polish/50-01-SUMMARY.md
@dashboard/src/components/ui/StickyActionBar.tsx

# Pages this plan modifies
@dashboard/src/pages/Overview.tsx
@dashboard/src/pages/PendingTasks.tsx
@dashboard/src/pages/Drafts.tsx

# PendingActionableCard reference (Plan 45-03 established the card layout — this plan does NOT modify cards)
@dashboard/src/components/PendingActionableCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Overview metric grid responsive + PendingTasks page padding/grid responsive</name>
  <files>
    dashboard/src/pages/Overview.tsx
    dashboard/src/pages/PendingTasks.tsx
  </files>
  <action>
    Read each file FIRST. The changes below are pattern guidance — exact Tailwind class strings depend on what's already there.

    **Step A — `dashboard/src/pages/Overview.tsx` (271 lines):**

    1. Find the metric grid. It's likely a `<div className="grid grid-cols-2 gap-4 ...">` or `<div className="grid grid-cols-3 gap-4 ...">`. Add the mobile-first prefix:
       - `grid-cols-2` → `grid-cols-1 sm:grid-cols-2` (single column under 640px, two columns at 640px+).
       - `grid-cols-3` → `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`.
       - If gap is `gap-4` or larger on phone with cards already too tall, drop to `gap-3 sm:gap-4`.

    2. Inside each metric tile, find the value text (e.g., `text-2xl font-bold` or `text-3xl font-bold`). Add `text-xl sm:text-2xl` (or one step down) so a 3-digit value (e.g., "247 messages") doesn't wrap at <375px. Tailwind `max-sm:` is also fine for this — pick whichever the file already uses for consistency.

    3. If any metric tile is interactive (clickable Card → drilldown), confirm tap-target floor. Plan 50-01 bumped Button defaults to h-10; metric tiles using `<Card>` directly need a manual `min-h-[88px] md:min-h-0` (88px is two stacked text rows comfortably) or just rely on the existing card padding being enough.

    4. Page-level horizontal padding: if the outer wrapper is `p-6`, change to `p-3 md:p-6` so phone gets more card width. If it's `p-4`, leave it. AppLayout already wraps in `p-4 md:p-6` — read what's inside Overview to avoid double-padding.

    **Step B — `dashboard/src/pages/PendingTasks.tsx` (441 lines):**

    1. Page-level padding (per CONTEXT 'audit per-card horizontal crowding'): if Overview's wrapper now uses `p-3 md:p-6`, mirror it here for visual consistency.

    2. Find the action-button row inside PendingActionableCard rendering. Per Plan 45-03 SUMMARY, the buttons are Approve / Edit / Reject in a horizontal flex row with gap-2. CONTEXT says "ensures the 3-button row doesn't wrap at 320px". The buttons after Plan 50-01 are h-10 (default size, ~40px) and `px-4` — three of them with gap-2 = ~3*~80px + 2*8px = ~256px which fits in 320px - 16px*2 padding = 288px AT TIGHT. Test in devtools at 320px width.

       If they wrap: switch the row from `flex gap-2` to `grid grid-cols-3 gap-2` on phone (`grid-cols-3 md:flex md:gap-2`). The grid forces equal width; each button gets ~88-90px which fits 320px comfortably.

       This change is INSIDE PendingActionableCard if the rendering is in the card component, NOT the page. Re-read PendingActionableCard.tsx FIRST. If the change is in the card, that's a Plan 50-03/50-04 conflict zone — but PendingActionableCard is NOT in the calendar component list, so editing it here is safe (no cross-plan file ownership conflict per Phase 50 file inventory). Add `dashboard/src/components/PendingActionableCard.tsx` to this plan's files_modified IF you end up editing it.

    3. Find the inline-edit textarea (Plan 45-03 added it for the Edit action). It's likely already `w-full` per shadcn Textarea defaults. CONTEXT says "makes the inline edit textarea full-width on mobile" — confirm it has no `max-w-*` class wrapping it on phone. If it does, drop the constraint to `md:max-w-md` or similar.

    4. Sticky filter chips (the filter bar at the top of the page from Plan 43-02) — make sure the filter row scrolls horizontally rather than wraps on phone: add `overflow-x-auto whitespace-nowrap` to the filter row container if it's not already there.

    **Verify:**
    - `npx tsc -b` — zero new errors.
    - `npx vite build` — succeeds.
    - `grep -n "grid-cols-1.*sm:grid-cols-\\|max-sm:" src/pages/Overview.tsx` — at least one responsive grid prefix.
    - `grep -n "p-3.*md:p-6\\|md:p-6.*p-3" src/pages/Overview.tsx src/pages/PendingTasks.tsx` — page-level padding is responsive (or both unchanged if AppLayout already provides enough).
    - Manual desktop check (>=768px): Overview metric grid still 2-col (or 3-col) as before; PendingTasks card row + action row look identical to Phase 45 ship.
    - Manual phone-emulation (375x667): Overview metric grid stacks 1-col, no text wrap; PendingTasks card action row fits without wrapping.
    - Manual phone-emulation (320x568, smallest still-supported width — old iPhone SE): Overview values don't wrap, PendingTasks 3 buttons fit on one row.
  </action>
  <verify>
    1. `npx tsc -b` — zero new errors.
    2. `npx vite build` — succeeds.
    3. `grep -n "sm:grid-cols\\|md:grid-cols\\|grid-cols-1" src/pages/Overview.tsx` — responsive grid present.
    4. Manual at 320px width devtools: no horizontal scroll on Overview or PendingTasks (vertical scroll only).
    5. Manual at 768px+ devtools: layouts byte-identical to pre-edit Overview / PendingTasks.
  </verify>
  <done>
    Overview metric grid stacks on phone, values don't wrap at 320px; PendingTasks action row fits 320px width, page padding adjusts on phone, inline-edit textarea is full-width.
  </done>
</task>

<task type="auto">
  <name>Task 2: Drafts page primary-action StickyActionBar wrap + responsive cleanup</name>
  <files>
    dashboard/src/pages/Drafts.tsx
  </files>
  <action>
    Drafts.tsx is small (69 lines per current sizing). Read it FIRST to see what primary actions exist (CONTEXT calls out "Send all / Regenerate where applicable" — but those may not currently exist if the page is purely a list view).

    1. Identify the primary action(s). Possibilities:
       - A "Send all" button at the top or bottom of a draft list → wrap in `<StickyActionBar>`.
       - A "Regenerate" button per row → leave per-row (NOT a candidate for StickyActionBar; that's a row-level action).
       - No primary actions at all → skip this step. CONTEXT says "where applicable" — if not applicable, document in SUMMARY and move on.

    2. Wrap any page-level primary action button(s) in `<StickyActionBar>`:
       ```tsx
       import { StickyActionBar } from '@/components/ui/StickyActionBar';
       // ...
       <StickyActionBar>
         <Button onClick={handleSendAll}>Send all</Button>
         <Button onClick={handleRegenerate} variant="outline">Regenerate</Button>
       </StickyActionBar>
       ```
       StickyActionBar's behavior (Plan 50-01): on phone it pins to bottom with safe-area inset; on desktop it renders inline. The existing inline button placement on desktop is preserved either way — StickyActionBar renders identically to a `<div className="flex gap-2 mt-4">` on desktop.

    3. Card list responsive — if the draft cards are in a grid (e.g., `grid grid-cols-2`), collapse to `grid-cols-1 md:grid-cols-2` on phone. If they're already a `flex flex-col` stack, no change needed.

    4. If the page has any text inputs (search/filter), confirm they use the shadcn `<Input>` primitive (Plan 50-01 bumped it to text-base on phone). If raw `<input>` is used, swap to `@/components/ui/input`.

    5. Add a one-line content-bottom-padding so the StickyActionBar (which is fixed bottom on phone) doesn't cover the last card. The cleanest approach is `pb-24 md:pb-0` on the outer wrapper (24 = 96px = roughly StickyActionBar height + a comfortable gap). On desktop StickyActionBar renders inline so no padding is needed.

    **Verify:**
    - `npx tsc -b` — zero new errors.
    - `npx vite build` — succeeds.
    - `grep -n "StickyActionBar" src/pages/Drafts.tsx` — present (or documented as N/A in SUMMARY if no primary actions exist).
    - Manual desktop check: Drafts page renders identically to pre-edit (StickyActionBar inline on desktop is byte-equivalent to the prior inline button row).
    - Manual phone-emulation: scroll a draft list — Send all / Regenerate buttons stay pinned to bottom and clear the iOS home indicator.

    **Commit order (2 atomic commits expected):**
    - `feat(50-05): Overview + PendingTasks responsive prefixes (mobile-first grid + padding + 320px-safe action row)`
    - `feat(50-05): Drafts primary actions in StickyActionBar + content padding for fixed bar`
  </action>
  <verify>
    1. `npx tsc -b` — zero new errors.
    2. `npx vite build` — succeeds.
    3. Either `grep -n "StickyActionBar" src/pages/Drafts.tsx` returns a match OR Drafts has no page-level primary actions (document the latter case in the SUMMARY explicitly).
    4. Manual desktop: Drafts renders identically to pre-edit.
    5. Manual phone-emulation: any pinned action bar is reachable + safe-area-clear.
  </verify>
  <done>
    Drafts uses StickyActionBar where it has primary actions OR documents the N/A case; card list is single-column on phone; content has bottom padding so the fixed bar doesn't cover the last card.
  </done>
</task>

</tasks>

<verification>
Phase-level check after both tasks:
- `npx tsc -b` — zero new errors.
- `npx vite build` — succeeds.
- All three pages have either a responsive Tailwind prefix added OR an explicit "no change needed because X" note in the SUMMARY.
- Manual desktop regression on all three pages: byte-identical to pre-edit.
- Manual phone-emulation: Overview metric grid stacks; PendingTasks action row fits at 320px; Drafts primary actions are pinned (or documented as N/A).
</verification>

<success_criteria>
- Overview metric grid 1-col on phone, 2/3-col on desktop, no text wrap at 320px.
- PendingTasks action row fits at 320px without wrapping; inline-edit textarea full-width on phone; page padding tighter on phone.
- Drafts primary actions wrapped in StickyActionBar (or documented N/A); content padding clears the fixed bar; cards single-column on phone.
- Zero desktop regressions on any of the three pages.
- Feeds MOBILE-05 (Overview / PendingTasks / Drafts mobile audit; stacked columns, full-width textareas, sticky action bars where appropriate).
</success_criteria>

<output>
After completion, create `.planning/phases/50-dashboard-mobile-ui-polish/50-05-SUMMARY.md` documenting:
- Files touched + line deltas (each page should add 5-15 lines of class-string deltas).
- Commit hashes (2 atomic commits expected).
- Bundle size before/after (expected ~0kB delta — class string changes only).
- Decision log: whether Drafts had page-level primary actions; if not, explicit N/A documentation per CONTEXT.
- Decision log: whether PendingActionableCard.tsx had to be edited for the 320px action row fit; if yes, add it to files_modified retroactively.
- Confirmation desktop layout is byte-identical on all three pages (regression evidence: screenshot diff or grep evidence that the only added classes are mobile-first prefixes).
- Confirmation `.planning/` docs were added via `git add -f`.
</output>
