---
phase: 18-trip-memory
plan: 03
type: execute
wave: 3
depends_on: ["18-01", "18-02"]
files_modified:
  - src/groups/travelParser.ts
  - src/groups/travelHandler.ts
autonomous: true
requirements: [MEM-02]

must_haves:
  truths:
    - "A user @mentioning the bot with 'what did we decide about the hotel?' triggers history_search, not a live web search"
    - "The bot replies with a natural language answer synthesized from stored decisions and chat history"
    - "If no decisions or relevant messages exist, the bot says so rather than hallucinating"
    - "Existing travel search queryTypes (flights, hotels, restaurants, activities, car_rental, general) still work unchanged"
  artifacts:
    - path: "src/groups/travelParser.ts"
      provides: "history_search added to queryType enum"
      contains: "history_search"
    - path: "src/groups/travelHandler.ts"
      provides: "history_search dispatch branch + handleHistorySearch function"
      contains: "handleHistorySearch"
  key_links:
    - from: "src/groups/travelHandler.ts"
      to: "src/db/queries/tripMemory.ts"
      via: "imports getDecisionsByGroup, searchGroupMessages for recall"
      pattern: "import.*tripMemory"
    - from: "src/groups/travelHandler.ts"
      to: "src/ai/provider.ts"
      via: "imports generateText for synthesis"
      pattern: "import.*generateText.*provider"
    - from: "src/groups/travelParser.ts"
      to: "src/groups/travelHandler.ts"
      via: "TravelIntent type with history_search queryType flows to dispatch"
      pattern: "history_search"
---

<objective>
Add conversation recall so users can ask the bot what the group decided about a topic.

Purpose: Completes the MEM-02 requirement -- users @mention the bot with recall questions like "what did we decide about the hotel?" and get answers from stored trip decisions and chat history, without triggering a live travel search.
Output: Modified travelParser.ts (new queryType) and travelHandler.ts (new dispatch branch with Gemini synthesis).
</objective>

<execution_context>
@/home/yuval/.claude/get-shit-done/workflows/execute-plan.md
@/home/yuval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-trip-memory/18-RESEARCH.md
@.planning/phases/18-trip-memory/18-01-SUMMARY.md
@.planning/phases/18-trip-memory/18-02-SUMMARY.md
@src/groups/travelParser.ts
@src/groups/travelHandler.ts
@src/ai/provider.ts
@src/db/queries/tripMemory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add history_search queryType to travelParser.ts and update system prompt</name>
  <files>src/groups/travelParser.ts</files>
  <action>
    In `src/groups/travelParser.ts`, make two changes:

    1. Add `'history_search'` to the `queryType` enum in `TravelIntentSchema`. The current enum is: `z.enum(['flights', 'hotels', 'restaurants', 'activities', 'car_rental', 'general'])`. Add `'history_search'` as the last entry.

    2. Update the system instruction string in `parseTravelIntent` to teach Gemini when to emit `history_search`. Add a sentence after the existing instructions, before "Respond in JSON": "If the user is asking about a past decision, what was decided, or recalling something the group discussed (e.g., 'what hotel did we pick?', 'what did we decide about transport?', 'מה החלטנו על המלון?'), set queryType to 'history_search' and isVague to false. Do not set isVague for recall questions -- they are valid even without a destination."

    Do NOT change the Zod import (`zod/v3`) or the `zodToJsonSchema` call in this file. This file uses the v3 compat layer and changing it is out of scope. The new enum value works fine with the existing setup since it is just a string addition.

    After editing, regenerate the JSON schema by verifying the module still compiles. The `TRAVEL_INTENT_JSON_SCHEMA` is computed at module load from the updated schema, so no additional changes are needed.
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm travelParser.ts compiles. Verify the `history_search` string appears in the queryType enum definition.
  </verify>
  <done>travelParser.ts queryType enum includes 'history_search', system prompt instructs Gemini when to classify a message as history_search, file compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add history_search dispatch and handleHistorySearch to travelHandler.ts</name>
  <files>src/groups/travelHandler.ts</files>
  <action>
    In `src/groups/travelHandler.ts`, add two things: an import section update and a new `handleHistorySearch` function, plus a dispatch branch.

    **New imports** (add at the top with existing imports):
    - `import { getDecisionsByGroup, searchGroupMessages, getTripContext } from '../db/queries/tripMemory.js';`
    - `import { generateText } from '../ai/provider.js';` (this import may already exist -- check; if not, add it)

    **New function `handleHistorySearch`:**
    Add a private async function before `handleTravelMention`:

    `async function handleHistorySearch(groupJid: string, intent: TravelIntent, lang: 'he' | 'en'): Promise<string>`

    Logic:
    1. Load all trip decisions for this group: `getDecisionsByGroup(groupJid)`. Also load trip context: `getTripContext(groupJid)`.
    2. Determine search terms from the intent: use `intent.searchQuery` if available, otherwise extract keywords from `intent.destination` or `intent.preferences`, falling back to an empty string.
    3. If search terms are non-empty, run FTS5 search: `searchGroupMessages(groupJid, searchTerms, 15)`. Otherwise set ftsResults to empty array.
    4. Format decisions as a readable list: for each decision, format as "- [type]: value (confidence, date)".
    5. Format FTS results as a readable list: for each result, format as "- senderName: body (truncated to 200 chars)".
    6. Format trip context summary if available.
    7. Build a Gemini `generateText` call with:
       - systemPrompt: "You are a WhatsApp group trip assistant. Answer the user's question about past trip decisions based ONLY on the provided data. If no relevant decision or message exists, say so honestly -- do not make up information. Reply in {Hebrew if lang=he, English if lang=en}. Keep the answer concise (2-4 sentences)."
       - messages: a single user message containing: the user's question (from intent.searchQuery or the raw query), the formatted decisions list, the FTS search results, and the trip context summary.
    8. If generateText returns null or empty, return a fallback message: "I don't have any stored decisions about that topic." (or Hebrew equivalent based on lang).
    9. Return the Gemini response.

    **Dispatch branch in handleTravelMention:**
    After the `intent` is parsed and after the vague-check block (around line 208-210 in the current code), but BEFORE the "Clear travel request" search block (line 214), add a new conditional:

    ```
    if (intent.queryType === 'history_search') {
      const answer = await handleHistorySearch(groupJid, intent, lang);
      await sock.sendMessage(groupJid, { text: answer });
      return true;
    }
    ```

    This must be placed so that:
    - It runs AFTER the vague/clarification check (history_search should not be flagged as vague)
    - It runs BEFORE the search execution block (prevents a web search from being triggered)
    - It returns true (terminal -- no further pipeline processing)

    The "Searching..." indicator message that was already sent (line 163-164) is fine -- it will show briefly before the recall answer. This is acceptable behavior.
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm travelHandler.ts compiles. Verify that `handleHistorySearch` is defined and the dispatch branch exists before the search block. Start the bot, @mention it in a group with a recall question like "what did we decide about the hotel?" and observe: it should NOT trigger a web search. Instead it should respond with either a synthesized answer from stored data or a "no decisions found" message. Check logs for the history_search queryType being detected.
  </verify>
  <done>travelHandler.ts dispatches history_search queries to handleHistorySearch which queries tripDecisions + FTS5 + generates a natural language answer via Gemini, without triggering a live web search. Existing queryTypes still route to the search path.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Bot starts without errors
3. @mention with "what did we decide about the hotel?" triggers history_search (check logs for queryType)
4. Bot replies with a natural language answer (from stored data or "no decisions" fallback)
5. @mention with "find hotels in Barcelona" still triggers a normal web search (existing behavior preserved)
6. Reply-chain follow-ups to travel results still work
7. Rate limiting still applies to history_search queries (same rate limit path)
</verification>

<success_criteria>
- Recall questions produce natural language answers from stored decisions + chat history
- No live web search is triggered for history_search queries
- If no relevant decisions exist, the bot says so honestly
- Existing travel search, vague handling, reply chains, and rate limiting are unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/18-trip-memory/18-03-SUMMARY.md`
</output>
