# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-19)

**Core value:** The bot replies to WhatsApp messages in the user's authentic voice, so contacts can't tell the difference.
**Current focus:** Milestone v1.8 Task Approval & Context Enrichment — Phase 40 shipped; 2/5 phases complete; next is Phase 41 WhatsApp Approval UX.

## Current Position

Milestone: v1.8 Task Approval & Context Enrichment — **IN PROGRESS**
Phase: 40 Unified Detection Pipeline — **COMPLETE** (3/3 plans, 2026-04-19)
Plan: All plans complete. Next phase is 41.
Status: **Phase 40 COMPLETE (2026-04-19) — deployed to prod on main.** 3 plans shipped in 3 waves (40-01 detectionService wave 1, 40-02 router + messageHandler swap wave 2, 40-03 live verification wave 3 owner-approved). 9 atomic commits + 3 SUMMARY.md files. Final test counts: 74/74 vitest green for v1.8 (43 actionables queries + 11 backfill + 14 detection service + 6 router). Deployment: owner committed protobufjs override (3e09de3), merged feat/v1.8-task-approval into main no-ff, restarted PM2 (pid 2439675, restart count 436→437, clean). Live-DB smoke: backfill migrated 358 legacy rows to actionables with USER_JID fixup completed (0 placeholder rows remaining), 130 Google Tasks ids preserved. Post-restart unprompted verification (2 min window): 2 real-traffic detections landed in `actionables` as `pending_approval` — zero new writes to legacy `reminders(source='commitment')` or `todo_tasks` in the same window, confirming end-to-end dark-launch behavior. Owner walked through Google Tasks UI + WhatsApp self-chat — zero new Tasks entries, zero notifications. DETC-01 / DETC-02 / MIGR-02 flipped to Complete in REQUIREMENTS.md traceability.

One deviation from the planned design: `routeDetection` was extracted from `messageHandler.ts` into its own `src/actionables/detectionRouter.ts` module because the test couldn't load messageHandler without initializing the ElevenLabs voice client (fails without API key at import time). Same behavior, better testability; logged in 40-02-SUMMARY.md.

Detection notifications will stay silent until Phase 41 ships the approval UX. Accumulated `pending_approval` rows will be flushed through the debounced batched-preview flow at that time (see 40-CONTEXT.md "One-time digest when Phase 41 first ships").

Last activity: 2026-04-19 — Phase 40 closed; ready to start Phase 41 WhatsApp Approval UX (APPR-01..05, DETC-03).

Previous: 2026-04-19 — Phase 39 COMPLETE. 3 plans shipped in 2 waves (schema+migration, query layer, backfill). 54/54 vitest green, 356 legacy rows mapped cleanly into actionables with Google Tasks ids preserved on 128 and 308 USER_JID_PLACEHOLDER rows queued for the startup fixup that ran successfully on Phase 40 restart. ACT-01 / ACT-02 / MIGR-01 flipped to Complete.

_Previous milestone context preserved below._

---

Previous: 2026-04-17 — **Phase 38 COMPLETE.** Owner-approved live browser walkthrough: form fields PASS, post appears PASS (English; Hebrew waived as upstream pm-authority bug), validation errors inline PASS, replaces SSH PASS, localStorage persistence PASS. Final test counts: pm-authority pytest 77/77, whatsapp-bot linkedin vitest 103/103, dashboard tsc + vite clean (783.62 kB / 232.81 kB gzip). v1.7 milestone shipped: 6/6 phases (33-38), 14/14 requirements (LIN-01 through LIN-14).
Previous activity: 2026-04-17 — **Phase 37 COMPLETE.** Lesson-mode review fully in the dashboard (lesson selection + variant finalization + inline fal.ai image); all 3 SCs observed live by owner. LIN-11/12/13 shipped. v1.7 milestone: 5/6 phases complete. Previous: 2026-04-15 — **Phase 37 Plan 04 COMPLETE** (Wave 3: Queue Integration). Three atomic whatsapp-bot commits: `b957463` feat(37-04): status styles accentClass + card accent stripe + arrival flash props — `STATUS_STYLES` record type widened to include optional `accentClass`, `PENDING_VARIANT` recolored blue→indigo with label 'Variant to finalize' + `border-l-4 border-indigo-500` accentClass, `PENDING_LESSON_SELECTION` relabel 'Lesson to pick' + `border-l-4 border-purple-500` accentClass, `LinkedInPostCard` gains 2 new optional slot props (`accentStripeClass` string + `justArrivedFlash` boolean) threaded through `QueueCard` with 400ms transition-colors ceiling reused for both 300ms amber arrival flash AND 400ms emerald regen flash; `ea7bc30` feat(37-04): PendingActionEntryButton + useNewArrivalFlash hook — stub body (`return null`) replaced with real `Button asChild variant=outline size=sm` + react-router-dom `Link to=/linkedin/queue/posts/:id/{lesson|variant}` implementation, new 89-line `dashboard/src/hooks/useNewArrivalFlash.ts` with null-sentinel seed on first render (no flash storm on mount) + per-id 300ms setTimeout expiry + const `FLASH_STATUSES = new Set(['PENDING_LESSON_SELECTION', 'PENDING_VARIANT'])` filter (CONTEXT §Area 4 lock: only pending-action posts flash, NOT all statuses) + ZERO sonner imports; `5e3ece3` feat(37-04): queue integration — status strip counters + entry button routing + arrival flash — `StatusStripProps` gains `lessonsToPick` + `variantsToFinalize`, grid flexes from `lg:grid-cols-4` to `lg:grid-cols-3 xl:grid-cols-6` with 2 new mini-counter cards (purple + indigo) inserted between Pending and Approved, `LinkedInQueue` imports `PendingActionEntryButton` + `STATUS_STYLES` + `useNewArrivalFlash`, `LinkedInQueueRoute` subscribes `flashingIds = useNewArrivalFlash(patchedQueue)` after the optimistic-patch projection, `QueueFeed` branches `actionsSlot` on status (pending-action → `<PendingActionEntryButton post={post} />`, else Phase 36 `renderPostActions`) and passes `accentStripeClass={STATUS_STYLES[post.status]?.accentClass}` + `justArrivedFlash={flashingIds.has(post.id)}`. Barrel file `dashboard/src/components/linkedin/index.ts` NOT touched across all 3 task commits — `git diff --stat HEAD~3 -- dashboard/src/components/linkedin/index.ts` empty. `tsc -b` linkedin-subsystem clean (4 remaining errors are pre-existing baseline in KeywordRuleFormDialog + Overview, logged at deferred-items.md, unrelated to Phase 37). `vite build` clean (2069 modules, bundle 772.17 kB raw / 229.80 kB gzip — Δ +20.37 kB raw / +4.21 kB gzip vs Plan 37-01 baseline). StatusStrip: 4→6 counter cards. LinkedInPostCard: 5→7 slot props. Deviations: zero — all research_facts held as-planned (sanity check #9 confirmed `STATUS_STYLES.PENDING_LESSON_SELECTION` + `STATUS_STYLES.PENDING_VARIANT` keys still intact after 37-01's schema edits). LIN-11 / LIN-12 / LIN-13 remain complete (plan requirements front-matter re-asserts idempotent). Plan 37-05 live verification unblocked — full Phase 37 surface shippable once owner walks through it in the browser. _Previous: 2026-04-15 — **Phase 37 Plan 03 COMPLETE** (Wave 2 parallel: Variant Finalization Page). Three atomic whatsapp-bot commits: `b682e90` feat(37-03): useLinkedInPickVariant hook with SSE-driven slow path — 9-state discriminated union, 200 fast path + 202 → waiting_for_sse path (no polling, no 120s cap), `ackSlow()` callback pattern so the page owns terminal-state timing, ZERO references to `useLinkedInJob` (verified via `grep -c`); `40aa22b` feat(37-03): VariantCard + VariantImageSlot presentational components — 4-mode VariantImageSlot (idle/pending/ready/error) with live elapsed-seconds spinner that runs forever per CONTEXT §Area 3 Scenario B, VariantCard with focus-then-confirm visual ring + bilingual content (post-level Hebrew + per-variant English) + collapsible image prompt + GenerationMetadata strip, keyboard activatable via Enter/Space; `07d90f0` feat(37-03): full variant finalization page with SSE-driven image state — fetches post on mount via `/api/linkedin/posts/:id` with Zod guard, subscribes to `useLinkedInQueueStream` and re-seeds local `post` state on every `queue.updated`, watches `post.image?.url` AND `post.status` inside a waiting-for-sse useEffect and calls `ackSlow()` on terminal transition, then delays navigation back to `/linkedin/queue` by exactly 1500ms via a single `setTimeout` in each terminal useEffect (fast path + slow path — same 1500ms window for consistency), inline `modeFor(variantId)` helper computes `VariantImageMode` per variant based on focus + hasImage + hook state, renders error banner for failed/already_picked/validation_error/network states, page header with project/perspective/language/generated-at meta grid + collapsible source snippet, guards for PENDING_VARIANT status + `variants.length < 2` + schema drift + fetch error. `tsc -b` + `vite build` clean (bundle 771.56 kB raw / 229.64 kB gzip). The page is 332 lines, the hook is 125 lines, VariantCard is 105 lines, VariantImageSlot is 80 lines. Barrel file `dashboard/src/components/linkedin/index.ts` NOT touched — Plan 37-01 is the sole writer per the revised Wave-2 contract. `grep -c "useLinkedInJob" dashboard/src/hooks/useLinkedInPickVariant.ts` = 0 (locked invariant honored). LIN-12 and LIN-13 flipped to `[x]` Complete; live verification still deferred to Plan 37-05. Plan 37-03 ran in parallel with Plan 37-02 on file-disjoint paths with a shared git tree; commit interleave was clean (37-02 and 37-03 commits alternated without conflict). Deviations from plan: zero Rule 1-3 auto-fixes on the source side (the plan's embedded code compiled clean on first write). Pre-existing uncommitted modifications to six unrelated dashboard files (KeywordRuleFormDialog.tsx, ScheduleMessageDialog.tsx, button.tsx, useSettings.ts, ScheduledMessages.tsx, vite.config.ts) were explicitly NOT touched — all commits used file-level `git add` never `-A`. _Previous: 2026-04-15 — **Phase 37 Plan 01 COMPLETE** (Foundation: cross-repo schema additions + shared GenerationMetadata/StickyConfirmBar primitives + lesson/variant route scaffold). Cross-repo PostDTO additions shipped in 4 atomic commits across 2 repos: pm-authority `0f0474e` (feat: surface project_name + source_snippet + variant/lesson created_at on PostDTO); whatsapp-bot `796ea8a` (feat: mirror in proxy Zod) + `41777cd` (feat: dashboard strongly-typed variants + shared primitives + Wave-2/3/4 stubs) + `4ef1060` (feat: scaffold routes + stub pages). pm-authority pytest 68 → 70, whatsapp-bot linkedin vitest 95 → 98, dashboard tsc/vite clean. Two Rule 3 blocking auto-fixes: helper-fixture cascade in writes/stream tests + pm2 restart of pm-authority-http for the live integration test. The dashboard linkedin barrel is now Plan-37-01-only — 5 stub component files exist for Plans 37-02/03/04 to replace bodies of, never the barrel. Wave-2 (Plans 37-02 + 37-03) now unblocked to run in parallel on file-disjoint paths. _Previous: 2026-04-15 — **Phase 36 COMPLETE** (Plan 05: live verification + phase close)._ Owner walked through all 4 Phase 36 success criteria in the live PM2 browser stack against real pm-authority state and approved after two in-session fixes landed: `fcb619b` (drop spurious `status` from `JobAcceptedSchema`) and `ac9b47f` (image routes accept `?token=` query fallback for `<img>` tag auth). LIN-07 / LIN-08 / LIN-09 / LIN-10 all flipped to complete; ROADMAP.md Phase 36 bullet flipped `[x]` with 5/5 plans; Progress table row updated. v1.7 advanced to 4/6 phases shipped (Phase 33 + 34 + 35 + 36). Final test counts: pm-authority pytest 68/68, whatsapp-bot linkedin vitest 95/95 (+3 new image-auth cases from ac9b47f), dashboard tsc/vite clean, final bundle `index-DBlnMGHj.js` 750.94 kB / 225.41 kB gzip. whatsapp-bot PM2 restarted to pid 2017684; pm-authority-http NOT restarted (zero Python-side changes). Deferred: 4 pre-existing CommitmentDetectionService failures (out of scope, unrelated subsystem, logged at `deferred-items.md`). Plan 36-05 commits: `21de8b0` (deferred log), `fcb619b` (fix #1), `ac9b47f` (fix #2), plus phase-close metadata commit. Previous: 2026-04-15 — Phase 36 Plan 04 landed (wave 4: Image drop + PII gate). Four new dashboard files shipped: `dashboard/src/hooks/useLinkedInImageUpload.ts` (222 lines) — XHR-based multipart upload with `xhr.upload.onprogress` percent feedback (fetch cannot observe request-body progress), `validateImageClientSide(file)` exported for synchronous drop-handler use (MIME allowlist: png/jpeg/gif/webp + 10 MB cap), discriminated `ImageUploadError` union with 8 branches including a new `kind: 'aborted'` for silent-swallow of re-drop-aborts, single-slot XHR abort via `activeXhrRef`, Bearer-token auth + 401→clear+/login mirror of apiFetch. `dashboard/src/hooks/useLinkedInConfirmPii.ts` (155 lines) — thin POST /confirm-pii helper reusing Plan 36-02's `PostActionError` discriminated union so the same `actionErrorToToastText` router covers both edit/approve/reject AND confirm-pii error paths; optional `{note}` body. `dashboard/src/components/linkedin/LinkedInImageDropZone.tsx` (170 lines) — absolute-positioned overlay rendered via `LinkedInPostCard.thumbnailOverlay` (pre-wired by Plan 36-01 Task 6), drag counter pattern solves the React dragleave-fires-on-children quirk (increment/decrement a ref-count, only clear drag-over at 0), on drop runs sync validate → URL.createObjectURL preview → XHR upload → 1200ms preview hold after success before `URL.revokeObjectURL` (gives SSE ~3s to catch up), silent swallow of `kind: 'aborted'` errors to prevent double-toast on replace-during-upload. `dashboard/src/components/linkedin/LinkedInPiiGate.tsx` (56 lines) — amber outline "Mark PII Reviewed" button rendered via `piiGateSlot`, ShieldCheck + helper copy + Loader2 during in-flight confirm, `aria-label` for screen readers, component API-agnostic (calls `onConfirm` prop). `dashboard/src/pages/LinkedInQueue.tsx` rewired (+74/-1): new imports, `LinkedInQueuePageProps` extended with `renderThumbnailOverlay` + `renderPiiGate` render props, `QueueFeed` signature extended to thread both through to `LinkedInPostCard`, `LinkedInQueueRoute` gains `useLinkedInConfirmPii` hook call + `handleImageUploaded` (patches `{status, image}` from 2xx response) + `handleImageUploadError` + `handleConfirmPii` (optimistic DRAFT → await → reconcile with real status on success, `clearPatch` + toast on error), `renderThumbnailOverlay` + `renderPiiGate` factories (PII factory returns null unless `post.status === 'PENDING_PII_REVIEW'`). `dashboard/src/components/linkedin/postStatus.ts` widened: `LinkedInImageInfo.source: 'ai' | 'screenshot' | 'uploaded' | null` — mandatory for Task 6's image-field cast to type-check, achieves cross-repo parity with pm-authority Literal + whatsapp-bot Zod enum (both already included 'uploaded' since Plan 36-01). Task 1 was ZERO-change verification: pm-authority `ImageInfoDTO.source` already `Literal['ai', 'screenshot', 'uploaded'] | None`, whatsapp-bot `ImageInfoSchema.source` already `z.enum(['ai', 'screenshot', 'uploaded']).nullable()`, pytest 12/12 green before plan started. Commits: `ffd1415` (Task 2 useLinkedInImageUpload), `4451d60` (Task 3 useLinkedInConfirmPii), `7eeae67` (Task 4 LinkedInImageDropZone), `8783035` (Task 5 LinkedInPiiGate), `f5c5aea` (Task 6 wiring + postStatus.ts widen). `tsc -b` + `vite build` clean. Bundle delta vs 36-03 baseline: 743.67 → 750.89 kB raw (+7.22 kB), 223.35 → 225.37 kB gzip (+2.02 kB) — well within envelope. pm-authority pytest 12/12 green (test_http_upload_image.py + test_http_confirm_pii.py). Two auto-fixes applied: (1) Rule 1 Bug — added `kind: 'aborted'` discriminant to `ImageUploadError` + silent-swallow in drop handler to prevent double-toast on replace-during-upload re-drops; (2) Rule 3 Blocking — widened `LinkedInImageInfo.source` in `postStatus.ts` to include 'uploaded' (dashboard-local type mirror was the one missing link in the cross-repo Literal extension; plan flagged it as optional but tsc made it mandatory at Task 6 cast time). All 5 `LinkedInPostCard` slot props (actionsSlot, isRegenerating, justRegenerated, thumbnailOverlay, piiGateSlot) now FILLED — Plan 36-05 live verification is the only remaining work. PM2 restart NOT required (100% dashboard-side; whatsapp-bot proxy already ran Plan 36-01 Task 4's upload-image + confirm-pii routes). Previous: 2026-04-15 — Phase 36 Plan 03 landed (wave 3: Regenerate UX). Two new dashboard hooks shipped: `dashboard/src/hooks/useLinkedInJob.ts` (190 lines) is a generic 1500ms poller for `/api/linkedin/jobs/:id` — terminal-state stop on `succeeded|failed`, 2-min `POLL_MAX_MS=120_000` hard cap (more generous than CONTEXT §3's 60s — Claude CLI cold-starts can stretch past 90s), 40-failure burst safety cap that resets on every successful poll, ref-based interval cleanup (cleaner than the plan's `(pollOnce as unknown).._clean` closure trick), Bearer-token auth + 401→/login mirror of apiFetch, first poll fires immediately (no 1500ms cold gap). `dashboard/src/hooks/useLinkedInRegenerate.ts` (199 lines) orchestrates POST `/api/linkedin/posts/:id/regenerate` + job poll + 3 terminal callbacks (`onSucceeded`/`onFailed`/`onCapped`); single-active-job slot mirrors pm-authority's `semaphore(1)`; 409 with `error.code === 'REGEN_CAPPED'` routes to `onCapped` distinctly from other 409 STATE_VIOLATION errors; shape-drift fallback invokes `onSucceeded(postId, null)` when `job.result.post` fails Zod parse so SSE can catch up within ~3s; `optsRef.current` pattern in the terminal dispatcher avoids stale-closure bugs. `dashboard/src/pages/LinkedInQueue.tsx` rewired (+92/-3): added `useLinkedInRegenerate` hook with all 3 callbacks (`onSucceeded` calls `applyPatch` with new content/status/regeneration_count/regeneration_capped + `flashRegenSuccess(postId)` — explicitly NO toast per CONTEXT §3 lock; `onFailed` calls `clearPatch` + toast.error with sonner Retry action; `onCapped` toasts "Regeneration cap reached for this post (5/5)"); new `justRegenerated: Record<string, boolean>` state map driven by `flashRegenSuccess` which sets the flag and clears it via `setTimeout(...,400)` for the 400ms `bg-emerald-50 transition-colors` flash on `LinkedInPostCard.QueueCard` (pre-wired by Plan 36-01 Task 6); `isPostRegenerating` + `isPostJustRegenerated` predicate props threaded through `LinkedInQueuePage` → `QueueFeed` → `LinkedInPostCard`; `renderPostActions` closure now passes `isRegenerating={getRegenStatus(post.id)}` and replaces the Plan 36-02 no-op `onRegenerate` with `() => void handleRegenerate(post)`. Verify greps confirm `toast.success.*[Rr]egeneration` returns 0 in BOTH `LinkedInQueue.tsx` and `useLinkedInRegenerate.ts` — CONTEXT §3 NO-success-toast lock is honored. Commits: `c0d1c54` (Task 1 useLinkedInJob), `bd88a88` (Task 2 useLinkedInRegenerate), `9206d78` (Task 3 LinkedInQueue.tsx wiring). `tsc -b` + `vite build` clean. Bundle delta vs 36-02 baseline: 736.72 → 743.67 kB raw (+6.95 kB), 221.54 → 223.35 kB gzip (+1.81 kB) — well under the predicted 8-12 kB envelope. One auto-fix applied: Rule 3 Blocking — adapted both new hooks from plan-spec'd `credentials: 'include'` to `Authorization: Bearer ${localStorage.getItem('jwt')}` matching dashboard's existing apiFetch JWT pattern (cookie auth would have failed against whatsapp-bot's JWT-gated Fastify middleware); same root cause as Plan 36-02's deviation #1. Plan 36-05 live verification deferred. Previous: 2026-04-15 — Phase 36 Plan 02 landed (wave 2 start: approve/reject/edit). useLinkedInPostActions hook shipped at `dashboard/src/hooks/useLinkedInPostActions.ts` with a discriminated `PostActionError` union (state_violation / upstream_failure / internal_error / validation_error / network / unknown) and an `actionErrorToToastText` router mapping every error kind to CONTEXT §1 copy. `LinkedInPostActions.tsx` component renders a single-source-of-truth responsive action surface: on ≥md viewports a 4-button inline row (Approve emerald one-click / Reject outline / Edit outline / Regenerate blue outline), on <md a single `MoreHorizontal` DropdownMenu with 4 items. Both surfaces share `approveDisabled` / `rejectDisabled` / `editDisabled` / `regenDisabled` computed constants and a shared `AlertDialog` for destructive reject confirmation (mounted once, driven by `useState<boolean>`). `EditPostDialog.tsx` is a bilingual Tabs modal: Hebrew (rtl) + English (ltr) with a per-tab character counter that turns red at ≥3000 (warning only, not gating), explicit Save disabled when the active tab's `.trim() === ''`, and an inline red banner on save error (modal stays open). The Hebrew tab is hidden entirely when `post.content_he === null`. `LinkedInQueueRoute` rewritten with an optimistic-patch layer: `Record<string, Partial<LinkedInPost>>` projected through the SSE queue via `useMemo`, `handleApprove` / `handleReject` apply the patch before the POST and roll back on error, rejected posts fall out of the visible queue via a terminal `.filter(p => p.status !== 'REJECTED')`. Commits: `f968d4f` (Task 1 hook), `8160008` (Task 2 actions), `31f7f70` (Task 3 dialog), `6ad043d` (Task 4 wiring). `tsc -b` + `vite build` clean (bundle 736.72 kB / 221.54 kB gzip, ~+15 kB raw vs Plan 35-04 baseline as predicted). Two auto-fixes applied: (1) Rule 3 Blocking — adapted auth from `credentials: 'include'` to `Authorization: Bearer` matching dashboard's existing `apiFetch` JWT pattern; (2) Rule 2 Missing Critical — widened `actionErrorToToastText` action union to include `'regenerate' | 'upload' | 'confirm-pii'` so Plans 36-03 / 36-04 can reuse the exported router without casting. Plan 36-05 live verification deferred. Previous: 2026-04-14 — Phase 35 Plan 02 + Plan 03 landed in parallel (wave 2). Plan 35-02: server-side SSE stream route — `src/api/linkedin/routes/stream.ts` with `registerStreamRoutes(fastify)` now mounted in `src/api/routes/linkedin.ts` after the write-side routes. Endpoint `GET /api/linkedin/queue/stream?token=<jwt>` mirrors `/api/status/stream`'s EventSource-JWT pattern (manual `fastify.jwt.verify(token ?? '')` gate, no onRequest hook). Per-connection polling loop hits `callUpstream({ method:'GET', path:'/v1/posts', timeoutMs: 3000, responseSchema: z.array(PostSchema) })` every 3s, computes a sha1 over `[id, status, content[0:100], variants.length, lesson_candidates.length, image.url]` and emits `event: queue.updated\ndata: {"posts":[...]}` only when the hash changes. First poll fires immediately (no 3s hole on connect). 15s heartbeat `: ping\n\n` comment frames defend against middlebox idle drops. Upstream errors (timeout, connection_refused, SchemaMismatchError, 5xx) are `log.warn`-ed but the loop keeps ticking — the client sees last-known-good state plus continuing heartbeats until upstream recovers. Client disconnect via `request.raw.on('close', ...)` clears both intervals — zero leaked timers. Test file `src/api/linkedin/__tests__/stream.test.ts` uses a real `fastify.listen({port:0})` + `node:http.get` harness (not `fastify.inject()` — it buffers and can't observe multi-frame SSE) with a `maxMs` deadline + `res.destroy()` pattern that handles both 200 streams and 401 JSON. 11 new tests pin: 2x JWT 401 (missing + invalid), initial `queue.updated` on first successful poll, hash dedup over 5.5s window, change-re-emit on status transition, upstream recovery (ECONNREFUSED then success), 16s heartbeat, and 4 hashPosts unit tests (stability, status change, variants change, >100-char content is ignored). Full linkedin vitest: 84/84 passing (73 baseline + 11 new), test-file runtime ~34s due to real wall-clock SSE waits (accepted tradeoff per plan). `tsc --noEmit` clean for all linkedin files (pre-existing `cli/bot.ts` rootDir warning is out of scope). PM2 whatsapp-bot NOT restarted — live verification is deferred to Plan 35-04 which wires the client and can restart once. Plan 35-03: dashboard-side presentational primitives for /linkedin/queue (post card queue + published variants, sticky 4-mini-card status strip with DST-aware next-slot calculator, page shell with tabs + mock wrapper, status color map). 5 files created under dashboard/src/components/linkedin/ + 1 under dashboard/src/pages/; zero new deps; `tsc -b` + `vite build` clean. Files fully disjoint between 35-02 (`src/api/**`) and 35-03 (`dashboard/src/**`). Commits: 35-02 = `9300c8f` (feat: stream.ts) + `68b2e1c` (feat: wire plugin) + `562436c` (test: 11 vitest cases); 35-03 = 94ce743 + ba515d4 + 5684f22 + fbb8cc1 + f0e6f53 (feat(linkedin-ui): prefix).

Progress: [####......] 40% (v1.8: 2/5 phases complete — Phase 39 + 40 shipped 2026-04-19; Phases 41-43 pending)

**Previous:** Progress: [==========] 100% (v1.7: 6/6 phases complete — Phases 33 + 34 + 35 + 36 + 37 + 38 shipped 2026-04-17)

## Performance Metrics

**Velocity:**
- Total plans completed: 82 (v1.0: 9, v1.1: 13, v1.2: 4, v1.3: 9, v1.4: 12, v1.5: 12, v1.6: 12; v1.7 in progress: Phase 33 + 34 + 35 + 36 shipped + Phase 37 Plans 01/02/03/04 = 22 plans across 5 phases)
- v1.6 shipped in 1 day (12 plans, 6 phases)
- v1.4 shipped in 1 day (12 plans, 5 phases)
- v1.3 shipped in 1 day (9 plans, 5 phases)

**Cumulative (all milestones):**
- 7 milestones shipped (v1.0 through v1.6)
- 32 phases complete, 70 plans complete

## Accumulated Context

### Decisions

Full decision log in PROJECT.md Key Decisions table.

Recent decisions affecting v1.7:
- Owner repo is whatsapp-bot — roadmap lives here, pm-authority gets a new sidecar service as cross-repo side work
- pm-authority exposes a new FastAPI sidecar service binding 127.0.0.1 — binding is the security boundary, no bearer tokens or JWT (out of scope)
- whatsapp-bot never imports Python directly — only talks to pm-authority via HTTP over localhost through Fastify proxy routes
- All dashboard requests go to whatsapp-bot's own origin; `/api/linkedin/*` routes proxy to the FastAPI service with Zod request and response schemas
- Zod schemas maintained manually — OpenAPI codegen is out of scope for v1.7 (14 endpoints, codegen pipeline is overhead)
- Telegram bot is additive fallback — nothing removed, dashboard is strictly additive
- SSE infra already exists in whatsapp-bot for WhatsApp connection state — reused in Phase 35 for live queue updates
- pm-authority state.db remains source of truth — the FastAPI service is the single reader/mutator, dashboard never reaches around it
- Existing ReviewManager CRUD and generate_lesson_variants / handle_select_lesson_sync / post_variant_and_generate_image_sync functions power the mutate endpoints — no rewriting pm-authority internals

Plan 33-01 decisions (2026-04-12):
- Router-ownership boundary: main.py pre-mounts ALL six empty routers (posts, images, jobs, mutations_fast, mutations_slow, lesson_runs) in Plan 33-01 so waves 2-4 only append handlers to their own router module and never edit main.py
- ImageInfoDTO.filesystem_path uses Pydantic v2 Field(exclude=True) — internal-only, never appears on the wire
- ImageInfoDTO.pii_reviewed is a first-class wire field derived from post.status != 'PENDING_PII_REVIEW'
- PostDTO.id is str (UUID) matching real pm-authority schema; created_at from sequences.created_at; updated_at always None in v1 (no column)
- StartLessonRunRequest body = {source_sequence_id, chosen_lesson, perspective?, language?} matching generate_lesson_variants signature; brand-new project ingestion deferred to Phase 35
- jobs.start_gc_task/stop_gc_task lifespan shim with stable signature so Plan 33-03 fills in the JobTracker body without touching main.py
- state.db pragmas applied at every connection open: WAL + busy_timeout=5000 + foreign_keys (not just once at startup)
- 10-code ErrorCode taxonomy: VALIDATION_ERROR, NOT_FOUND, STATE_VIOLATION, REGEN_CAPPED, LESSON_ALREADY_PICKED, VARIANT_ALREADY_PICKED, UNPROCESSABLE, INTERNAL_ERROR, UPSTREAM_FAILURE, UNAVAILABLE — maps 1:1 to HTTP codes via status_for()
- PM2 entry `pm-authority-http` on 127.0.0.1:8765 workers=1 (single-worker mandatory for in-memory JobTracker)

Plan 33-02 decisions (2026-04-13):
- services/http/dto_mapper.py is the SINGLE source of truth for SQLite→PostDTO assembly — every later plan that returns a PostDTO after a mutation MUST import build_post_dto from here rather than duplicate the mapping logic
- Path-jail for image endpoints: resolve raw image_path → relative_to(/home/yuval/pm-authority/data/) → fail-soft 404 for every failure mode (missing, invalid, escape attempt, non-file). Uniform NOT_FOUND shape keeps dashboard error handling simple; no 403 and no 500
- Lesson-candidate image lives on posts.image_path (not on the candidate row) — endpoint 404s unless the candidate is selected AND the post has a rendered image_path
- Superseded variants (post_variants.selected = -1) filtered out at the mapping layer, NOT the SQL layer, so future audit features can still fetch them if needed
- NON_TERMINAL_STATUSES tuple in dto_mapper.py is the canonical default list filter — excludes PUBLISHED and REJECTED
- TestClient fixture must rewrite get_conn.__wrapped__.__defaults__ (not just DEFAULT_DB_PATH module attr) because @contextmanager captures the default db_path at function-definition time
- pii_reviewed is computed at DTO-build time as (post.status != 'PENDING_PII_REVIEW'), not stored in the DB — anything past the PII gate is considered reviewed

Plan 33-03 decisions (2026-04-13):
- JobTracker.semaphore is a public asyncio.Semaphore(1) on the tracker instance — Plan 33-04 workers acquire it around any Claude-CLI / fal.ai subprocess to serialize global slow work; single-slot matches pm-authority's single-user design
- services/http/state_guard.py is the SINGLE source of truth for post status transitions — ALLOWED_TRANSITIONS dict covers approve/reject/edit/regenerate/replace_image/pick_variant/pick_lesson; Plan 33-04 imports check_transition directly, no duplication
- State guard is status-string based (not enum-based) — posts.status is TEXT in pm-authority and the wire format is a string; enum conversion is pure overhead
- Whitespace-only edit is 422 UNPROCESSABLE (handler check), empty-string edit is 400 VALIDATION_ERROR (Pydantic min_length=1 + global handler); matches CONTEXT.md §4 taxonomy
- Fast mutation handlers import dto_mapper.build_post_dto LAZILY inside the handler body — lets the module load cleanly even while the parallel Plan 33-02 is in flight; pytest.importorskip in the test file mirrors the same contract
- routers/jobs.py start_gc_task is idempotent: if app.state.job_tracker is already set (test fixture injects one) it only calls .start_gc() rather than rebuilding; matches the shim signature Plan 33-01 committed to
- get_tracker(request) is a plain helper, not a FastAPI Depends — keeps the APIError-raising style uniform with every other error site in the service

Plan 36-02 decisions (2026-04-15):
- Bearer-token auth in useLinkedInPostActions: plan specified `credentials: 'include'` but dashboard uses JWT bearer tokens from localStorage (see `dashboard/src/api/client.ts`). Adapted to match `apiFetch`'s behavior — `Authorization: Bearer ${localStorage.getItem('jwt')}` header + 401 → clear token + redirect to `/login`. Cookie auth would have failed against whatsapp-bot's JWT-gated Fastify middleware end-to-end.
- `actionErrorToToastText` action union widened to `'approve' | 'reject' | 'edit' | 'regenerate' | 'upload' | 'confirm-pii'` at Plan 36-02 time so Plans 36-03 and 36-04 can reuse the exported router without casting. New kinds fall through the default case; those plans can add branches locally if they need custom copy.
- Edit button placed in the 4-button action row next to Approve/Reject/Regenerate — CONTEXT §1 lists only 3 primary buttons but never specs where the Edit trigger lives. Action row is the most discoverable placement. User sees this in Plan 36-05 and can push back.
- `renderPostActions` render prop (NOT HOC / context / global mutation store) on `LinkedInQueuePage` — keeps the component presentational and the 35-03 mock-data default export still works without `renderPostActions` (passes `actionsSlot={undefined}` to `LinkedInPostCard` which 36-01 made a no-op branch).
- Optimistic patch persists through the POST response per CONTEXT §1 lock "local wins until the POST response resolves" — the patch is not auto-cleared on success; SSE eventually delivers a matching post structurally within ~3s. Only error paths explicitly `clearPatch(postId)`.
- Single source of truth for button disable predicates in `LinkedInPostActions` — `approveDisabled`/`rejectDisabled`/`editDisabled`/`regenDisabled` + matching tooltip strings computed once at the top, consumed by both the desktop inline row and the mobile dropdown. Adding a 5th button later means updating one block, not two.
- Destructive reject `AlertDialog` mounted ONCE in the component body, driven by `useState<boolean>` (`rejectOpen`) — both the desktop Reject button and the mobile dropdown Reject item call `openReject()`. Same confirmation copy shows regardless of entry surface.

Plan 36-04 decisions (2026-04-15):
- Task 1 required ZERO file changes. Both pm-authority `ImageInfoDTO.source` (`Literal['ai', 'screenshot', 'uploaded'] | None`) and whatsapp-bot `ImageInfoSchema.source` (`z.enum(['ai', 'screenshot', 'uploaded']).nullable()`) already accepted 'uploaded' from Plan 36-01. The dashboard-local type mirror in `postStatus.ts` was the one missing link — widened in Task 6. pytest 12/12 green before plan started.
- Bearer-token auth in both new hooks (useLinkedInImageUpload + useLinkedInConfirmPii): same cookie-vs-JWT rationale as Plans 36-02/36-03. XHR version uses `xhr.setRequestHeader('Authorization', 'Bearer ...')` right after `xhr.open`; fetch version uses the standard headers object. Both mirror apiFetch's 401 → clear JWT + redirect to /login.
- Added a `kind: 'aborted'` discriminant to `ImageUploadError` (plan had lumped aborts under 'unknown'). The drop zone detects this branch and returns early without calling `onError` — prevents a spurious "upload aborted" toast when the user drops a second image while the first is still uploading (the hook aborts the first XHR on the way in). Pure correctness fix.
- 1200 ms preview hold after upload success before `URL.revokeObjectURL`. Gives SSE its ~3s budget to deliver the refreshed post and re-render the underlying Thumbnail. The plan raised a potential stale-bytes issue if the browser caches the old `/image` URL response; v1 relies on the object-URL hold + the status pill change (DRAFT → amber PENDING_PII_REVIEW) as visual confirmation. Cache-bust `<Thumbnail cacheBust>` prop workaround deferred to Plan 36-05 if verification surfaces stale bytes.
- Widened `LinkedInImageInfo.source` in `postStatus.ts` from `'ai' | 'screenshot' | null` to `'ai' | 'screenshot' | 'uploaded' | null` — mandatory, not optional. Task 6's `handleImageUploaded` patch casts the response `DashboardPost.image` (loose `source: z.string()`) onto `LinkedInPost['image']`; without widening the target type, tsc errored at the assignment. The plan flagged this as optional ("if that wasn't done, do it now") — tsc made it mandatory.
- Reused the `'confirm-pii'` branch of `actionErrorToToastText` (Plan 36-02 widened the action union exactly for this). Currently falls through to the default case (`err.message || 'Action failed.'`) — acceptable for v1; dedicated copy can be added in Plan 36-05 if verification reveals gaps.
- Silent single-upload-per-hook semantics via `activeXhrRef`: when a user drops a second file while the first is still uploading, the hook aborts the first XHR. Matches the rest of the phase's one-action-at-a-time design (regen semaphore, optimistic patch per post).

Plan 35-03 decisions (2026-04-14):
- Duplicate PostSchema types into `dashboard/src/components/linkedin/postStatus.ts` rather than aliasing @/ to the whatsapp-bot src tree — keeps dashboard Vite bundle self-contained, avoids cross-bundle Zod-runtime sharing, and Plan 35-04 will Zod-parse SSE payloads at the proxy boundary to catch drift at runtime
- Native `Intl.DateTimeFormat({ timeZone: 'Asia/Jerusalem' })` + iterative 4-pass offset correction for nextPublishSlot — date-fns-tz is not in dashboard deps, and the approach is pure, unit-testable, and DST-correct. Smoke-tested against Oct 25 2026 autumn fallback (returns Tue Oct 27 06:30 IST with the right UTC offset)
- Single `LinkedInPostCard` with `variant: 'queue' | 'published'` prop instead of two sibling components — shared Thumbnail/ContentPreview subcomponents avoid duplication; parent selects layout via one prop
- Default export of `dashboard/src/pages/LinkedInQueue.tsx` is `LinkedInQueueMockPage` (feeds hand-crafted mock data to `LinkedInQueuePage`). Named export `LinkedInQueuePage` is the public presentational API that Plan 35-04 will import into a real-data wrapper — allows 35-03 to be verified standalone without any data fetching
- `StatusStrip` receives `pendingCount`/`approvedCount` via props derived from the queue list itself (CONTEXT §2) — zero extra API calls; only the 60s `setInterval` re-render drives countdown label updates
- Content budget 240 chars total for bilingual posts, 55/45 Hebrew/English split, Hebrew rendered first with `dir="rtl" lang="he"`, "—" separator, then English with `dir="ltr" lang="en"` — all inside a single LTR `line-clamp-3` container so the browser handles mixed-direction text naturally
- Thumbnail falls back to a neutral gray div with `FileText` icon (not hidden) on image error or when `image.url === null` — keeps layout stable
- `MetricsRow` renders "Metrics pending — available ~72h after publish" italic placeholder with `Clock` icon when `post.analytics === null`, otherwise a 4-stat row with Eye/MessageSquare/Repeat/Heart lucide icons and `1.2k` style formatting (`n >= 1000 ? (n/1000).toFixed(1)+'k' : String(n)`)
- Zod NOT installed in dashboard in this plan — pure presentational layer needs no runtime validation; Plan 35-04 installs it for the SSE payload parser + drift detection
- Did NOT touch `dashboard/src/components/layout/AppLayout.tsx` or `dashboard/src/router.tsx` — Plan 35-04 owns navigation + route registration (35-03 scope discipline)
- First commit (94ce743) inadvertently swept in 3 pre-existing index-staged `.planning/` docs (REQUIREMENTS.md, ROADMAP.md, STATE.md) that were already staged by a prior operation before 35-03 started. These are planning docs always committable; subsequent task commits were scoped clean. Logged as a staging hygiene note, not a scope violation

Plan 35-01 decisions (2026-04-14):
- Helper pattern over JOIN: `_fetch_latest_analytics(conn, post_id)` is a separate SELECT with `ORDER BY fetched_at DESC LIMIT 1`, matching `build_post_dto`'s existing per-table query style (5+ separate queries). Wire contract is identical to what a `LEFT JOIN (SELECT ... LIMIT 1)` would produce — same fields, same null semantics, same newest-row-wins tiebreak. CONTEXT §3 "Option A / LEFT JOIN" was decision-shorthand for "embed in PostDTO" not "literal SQL JOIN"
- Zod `PostSchema.analytics: PostAnalyticsSchema.nullable().optional()` — `.optional()` bridges the older-pm-authority-build deploy-window case (field absent), `.nullable()` covers the live-default (no post_analytics row → explicit `null`). Combination is load-bearing; tested against all three wire shapes
- Every `PostAnalyticsSchema` sub-field is `z.number().int().nullable()` — mirrors Pydantic `Optional[int]` because LinkedIn often returns partial metrics on a given fetch
- Test file placed at `tests/test_http_analytics_embed.py` (flat), NOT at `tests/services/http/test_analytics_embed.py` as the plan specified — pm-authority's test directory is flat and all other HTTP tests follow `tests/test_http_*.py` convention
- Test fixture SCHEMAs in 4 existing pm-authority HTTP test files (`test_http_reads.py`, `test_http_fast_mutations.py`, `test_http_slow_mutations.py`, `test_http_end_to_end.py`) updated to include `CREATE TABLE post_analytics` — adding an unconditional SELECT in `build_post_dto` caused 15 tests to crash with "no such table". Chose production-schema-matching fixtures over a fail-soft try/except in the helper to preserve signal on real schema drift
- `seeded` fixture returns `tuple[sqlite3.Connection, str]` rather than stashing `post_id` on the connection object — Python 3.14's `sqlite3.Connection` has no `__dict__` and rejects dynamic attributes (the plan's example pattern would have crashed)
- PM2 unblocker required Rule 3 auto-fix: `better-sqlite3` binary was compiled against Node v22 (NODE_MODULE_VERSION 127) but PM2 runs under Node v20.20.0 (v115). Fixed via `PATH=~/.nvm/versions/node/v20.20.0/bin:$PATH npm rebuild better-sqlite3 --build-from-source`. First rebuild attempt without `--build-from-source` was a no-op because npm reused the prebuilt binary. After rebuild, PM2 restart succeeded cleanly, `/api/linkedin/health` returns 401 (JWT-gated) instead of 404 (stale SPA fallback), confirming Phase 34 routes are live on the listening socket
- pm-authority-http restarted AFTER the schema change (not before) to pick up `PostAnalyticsDTO`; pm2 module loading means schema changes don't hot-reload. Live curl verified `"analytics": null` on both PUBLISHED posts afterwards

Plan 34-04 decisions (2026-04-13):
- Live integration test uses `fastify.inject()` + real undici global `fetch` against live pm-authority on 127.0.0.1:8765 — no mocks in this file. `vi.unstubAllGlobals()` at module top defends against stubbed-fetch leakage from reads/writes test files when vitest runs them in the same worker
- Portability skip gate: `beforeAll` probes `/v1/health` with 500ms `AbortSignal.timeout`; every test short-circuits with `console.warn('[integration.test] skipping: pm-authority unreachable')` and passes trivially when upstream is down. Explicit skip pattern (not `it.skipIf`) produces a clearer log signal AND keeps tests reported as passed — the portability contract is "integration test never breaks the main suite"
- `fastify.inject()` chosen over real-HTTP-through-PM2 curl because (a) no JWT minting, (b) no need to restart the live PM2 whatsapp-bot (which would briefly disconnect Baileys), (c) the exercised code path is identical — `callUpstream` fetches pm-authority the same way regardless of inbound transport, (d) ~100x faster. The only thing `inject()` skips is the outer Fastify listen loop, which is stock code we don't own
- Data-agnostic multi-status filter assertion: every returned row's status must match its filter, sum-of-non-terminal-counts ≤ unfiltered.length (matches pm-authority's `NON_TERMINAL_STATUSES` default excluding PUBLISHED/REJECTED). Works with any DB state — no hardcoded counts
- Optional Test 5 (edit round-trip with revert) DROPPED: live DB has 2 posts (1 APPROVED state-guard-blocks-edit + 1 PENDING_VARIANT might-interact-with-live-variant-gen), neither is a safe target. Plan explicitly allowed dropping this. Write routes already pinned by 25 mocked-fetch tests in writes.test.ts with full pass-through coverage
- DID NOT restart live PM2 whatsapp-bot: `pm2` CLI not on agent PATH, restart would briefly drop active Baileys session, integration test already proves wire behavior in-process. Phase 35 kicks off with a deliberate `npx pm2 restart whatsapp-bot` to pick up Plans 01-04 source changes before wiring dashboard UI
- Added Test 4 (jobs 404) beyond the plan's numbered tests — different upstream router (routers/jobs.py) with in-memory JobTracker backend vs SQLite, gives two independent pass-through proofs instead of one
- Added Test 6 (single post GET by real discovered UUID) — proves `PostSchema.parse` on single-post fetch in addition to list. Cleanly skips if DB is empty
- LIN-02 flipped to `[x]` Complete with evidence "63 unit vitest + 6 live integration tests green; live pm-authority verified via fastify.inject against 127.0.0.1:8765". Every bullet of LIN-02's success sentence (dashboard fetches LinkedIn post data via Fastify proxy routes forwarding to pm-authority with typed Zod schemas and error pass-through) is pinned by a green test

Plan 34-03 decisions (2026-04-13):
- Mixed 200/202 pick-variant implemented via Plan 34-01's `validateStatuses:[200]` opt-out — client PostSchema-validates only the fast branch; the 202 branch is validated inline in the route with `JobAcceptedSchema.safeParse` and a `SchemaMismatchError` thrown on drift. Keeps `callUpstream` one-schema-per-call; branching logic lives only in the one route that needs it.
- `validateBody<T>(schema, body, reply)` helper writes a `VALIDATION_ERROR` envelope shaped identically to pm-authority's (`error.code`/`message`/`details.issues`) and returns `null` on failure so routes bail out with `if (body === null) return`. Makes the wire shape of proxy-local validation errors indistinguishable from upstream ones — the dashboard's future error discriminator needs no special case for "who validated first".
- `EditRequestSchema` and every other request schema kept `.strict()` per Plan 34-01's convention — extra fields on body-validated routes become 400 VALIDATION_ERROR (pinned by a dedicated test). Dashboard bugs that smuggle unexpected fields surface at the proxy boundary rather than at pm-authority.
- Timeout tiers `FAST_MUTATION_TIMEOUT_MS=5000` and `SLOW_MUTATION_TIMEOUT_MS=10000` as top-of-file module constants — matches Plan 34-02's `JSON_READ_TIMEOUT_MS`/`IMAGE_STREAM_TIMEOUT_MS` pattern and Plan 34-01's tier hierarchy (1s health / 3s reads / 5s fast / 10s slow / 30s images).
- `SchemaMismatchError` imported directly from `../client.js`, not via `../errors.js` re-export — shorter, tighter dependency graph, matches reads.ts.
- No live smoke test this session — 25 mocked-fetch tests cover every branch including schema-mismatch on both pick-variant branches (impossible to reliably trigger against real pm-authority), and Plan 34-04 is the explicit owner of live PM2 end-to-end. Running curl now would duplicate work for zero information gain.
- Auth-gate test block exercises both Fastify type shapes — `/edit` (body + path param) and `/lesson-runs` (body only, no params) — each asserting the 401 short-circuit happens before any upstream fetch.
- LIN-02 deliberately REVERTED to `[ ]` / "In Progress (34-01/02/03 complete, awaiting 34-04 live integration)". A prior agent had prematurely marked it `[x]` after 34-02, but LIN-02's SC is "User can open the dashboard and it fetches LinkedIn post data" — that only happens after Plan 34-04 rolls the new routes into the live PM2 `whatsapp-bot` process. No false "done" signals.

Plan 34-02 decisions (2026-04-13):
- Sub-file layout `src/api/linkedin/routes/reads.ts` rather than extending `src/api/routes/linkedin.ts` directly — keeps the plugin file small and gives Plan 34-03 a symmetric `writes.ts` slot; plugin file becomes a pure "wire things together" layer
- `registerReadRoutes` is a plain async function, not a Fastify plugin — called inline from the existing linkedinRoutes plugin so all routes share the same JWT decorator and lifecycle; Plan 34-03 follows the same pattern
- Timeout tiers as module constants (`JSON_READ_TIMEOUT_MS=3000`, `IMAGE_STREAM_TIMEOUT_MS=30000`) at the top of reads.ts — makes the contract visible at-a-glance and easy for Plan 34-04's live integration test to reference
- `sendBinaryStream(reply, upstream)` helper centralizes the `Readable.fromWeb` bridge + header forwarding — Route 3 (post image) and Route 4 (lesson-candidate image) share one implementation to prevent drift
- `Readable` imported at module top (`import { Readable } from 'node:stream'`), NOT lazily inside the handler — avoids re-import on every request; the plan example showed a dynamic import but there's no functional reason for it
- Path params `encodeURIComponent`-escaped even for image routes where Fastify's router already strips literal slashes — defense-in-depth against a future router change; test 13 pins the behavior using `%25` (the only unsafe character Fastify passes through unchanged)
- `PostArraySchema = z.array(PostSchema)` hoisted to a module constant rather than constructed per-request — minor but consistent with Plan 34-03's likely bulk schemas
- Auth-gate test lives in a second `describe` block with its own `buildTestServer` using a rejecting `authenticate` decorator — keeps the happy-path suite order-independent and lets us assert `fetchMock` is NEVER called when auth fails (proves 401 short-circuits before upstream work)
- Live smoke test ran via an in-repo tsx one-shot script + `fastify.inject` against the real PM2 pm-authority, NOT by restarting the live PM2 whatsapp-bot — per plan directive, Plan 34-04 owns live PM2 verification

Plan 34-01 decisions (2026-04-13):
- Proxy uses native global fetch (Node 20 / undici) + per-call AbortSignal.timeout — no axios, no shared timeout defaults; every callUpstream call picks a tier deliberately (1s health / 3s reads / 5s fast mutations / 10s slow 202 / 30s image stream)
- SchemaMismatchError is a distinct class from UpstreamError — a Zod mismatch on a 2xx is OUR bug (whatsapp-bot drifted from pm-authority), not an upstream failure; routes map it to HTTP 500 INTERNAL_ERROR, not to the upstream error envelope
- Response schemas are permissive (no .strict) so pm-authority can add fields without breaking the proxy; request body schemas ARE .strict() to reject dashboard bugs early
- Snake_case wire format preserved end-to-end — schemas match Pydantic 1:1 so the dashboard can later import the same Zod module for typed fetching without any re-mapping layer
- callUpstream has a validateStatuses opt-out for mixed 200 Post / 202 JobAccepted responses (regenerate / pick-lesson / lesson-run) — no union-typed response, just passthrough the 202 body unchecked
- /api/linkedin/health ALWAYS returns HTTP 200 with a discriminated-union body (Phase 34 SC#4) — any upstream error (refused / timeout / 5xx / schema-mismatch) folds into {upstream:'unavailable', reason:<taxonomy>} so the dashboard banner has a reliable degraded signal instead of a spinning request or a 503
- Single routes/linkedin.ts file shared by Plans 34-02 and 34-03 — not split into subfiles; keeps server.ts registration stable and Plans 02/03 just append handlers
- z.iso.datetime({offset:true}) is the correct Zod v4 API for timestamps (accepts both '...Z' and '...+00:00' suffixes — live pm-authority emits '...Z'); z.record(z.string(), z.unknown()) is the Zod v4 two-arg form

Legacy decisions from v1.6 (see phase 27-32 archive):
- DB schema is unconditional root blocker — Phase 27 must complete before any other phase starts
- Cancel state must be DB-persisted (cancelRequestedAt column), never in-memory — survives PM2 reloads
- Voice/AI content resolves at fire time, not schedule time — no pre-generation
- Cron strings (not ms intervals) stored for recurrence — DST-safe via node-cron Asia/Jerusalem
- Promise.race timeout on every Baileys send (15s) and every TTS/Gemini call (30s)
- p-queue concurrency:1 for TTS to respect ElevenLabs limits
- Plain text FK for scheduledMessageId (no drizzle references()) — consistent with project convention
- Hand-written migrations after 0010 — FTS5 virtual tables incompatible with drizzle-kit
- [Phase 36-review-actions-write]: Bearer-token auth in useLinkedInJob + useLinkedInRegenerate (Rule 3 fix mirroring Plan 36-02): plan said credentials:'include' but dashboard uses JWT bearer from localStorage. Both new hooks now match apiFetch's pattern with 401→clear+redirect to /login.
- [Phase 36-review-actions-write]: Single activeJob slot in useLinkedInRegenerate (not per-post map) mirrors pm-authority's semaphore(1). isRegenerating(postId) predicate keeps per-post mental model for caller while only one regen runs at a time.
- [Phase 36-review-actions-write]: POLL_MAX_MS=120_000 in useLinkedInJob (more generous than CONTEXT §3's 60s). Claude CLI cold-starts can stretch past 90s; v1 ergonomic priority is 'don't give up too early'. Tighten once we have telemetry.
- [Phase 36-review-actions-write]: Plan 36-03 honors CONTEXT §3 NO-success-toast lock for regenerate. grep -c 'toast.success.*[Rr]egeneration' returns 0 in both LinkedInQueue.tsx and useLinkedInRegenerate.ts. Success path is purely visual: optimistic content patch + 400ms bg-emerald-50 flash via separate justRegenerated state map + setTimeout cleanup.
- [Phase 37-lesson-mode-ux]: Plan 37-02: locked-modal pattern (triple-suppressed Radix Dialog) + POST/job-poll mutation hook + auto-navigate page useEffect; reusable template for future pick-* mutations
- [Phase 37-lesson-mode-ux]: Plan 37-03: useLinkedInPickVariant deliberately bypasses useLinkedInJob (which has POLL_MAX_MS=120_000 hard cap). Uses an `ackSlow()` callback pattern so the page — not the hook — owns the terminal-state observation via useLinkedInQueueStream. The page watches post.image.url AND post.status, calls ackSlow() on transition, then delays navigation by exactly 1500ms via one setTimeout per terminal useEffect so the fal.ai image is visually observable on the focused variant before the router navigates back to the queue. Pattern honors CONTEXT §Area 3 Scenario B "no client-side timeout; trust backend/SSE to surface completion or failure" — the spinner in VariantImageSlot's pending mode literally runs forever until SSE delivers the terminal state.
- [Phase 38]: RadioGroup created using radix-ui monorepo (not @radix-ui/react-radio-group), matching codebase import pattern

### Pending Todos

(None)

### Blockers/Concerns

- Baileys v7.0.0-rc.9 stale-socket bug (issue #2132) — Promise.race mitigation required regardless of fix
- ElevenLabs plan tier determines p-queue concurrency ceiling — currently :1 (conservative), verify if higher is needed
- Phase 38 (new lesson run form) is the highest-risk dependency in v1.7 — depends on the long-running generator pipeline working cleanly over HTTP; kept as the final phase so any issues surface late without blocking the rest of the queue UX
- pm-authority FastAPI service adds a new side process to yuval-server — needs process supervision (PM2 or systemd) to stay up alongside whatsapp-bot

## Session Continuity

Last session: 2026-04-17
Stopped at: Phase 37 COMPLETE (5/5 plans, all 3 SCs live-verified by owner). Next: Phase 38 Plan 01 (New Lesson Run Form). Previous: — Queue Integration (purple/indigo pills + 4px accent stripes + entry buttons routing to Wave-2 pages + 2 new status-strip mini-counters + 300ms amber arrival flash via new useNewArrivalFlash hook). 3 atomic whatsapp-bot commits: `b957463` (postStatus.ts STATUS_STYLES accentClass + LinkedInPostCard 2 new props) + `ea7bc30` (PendingActionEntryButton real body + useNewArrivalFlash hook) + `5e3ece3` (StatusStrip counters + LinkedInQueue integration). Barrel `dashboard/src/components/linkedin/index.ts` NOT touched across all 3 commits (verified via `git diff --stat HEAD~3 -- index.ts` empty). Vite build clean (2069 modules, 772.17 kB raw / 229.80 kB gzip, Δ +20.37 kB raw / +4.21 kB gzip vs Plan 37-01 baseline). LinkedIn-subsystem tsc clean (4 remaining errors are pre-existing baseline in unrelated KeywordRuleFormDialog + Overview files, logged at deferred-items.md). Zero deviations — all research_facts held as-planned; sanity check #9 confirmed STATUS_STYLES keys still intact after 37-01's edits. LIN-11/12/13 remain complete. **Resume with Plan 37-05 (verification checkpoint)** — the full Phase 37 surface (foundation + lesson selection page + variant finalization page + queue integration) is shippable and awaiting owner walkthrough in the live browser. Plan 37-04 SUMMARY at `.planning/phases/37-lesson-mode-ux/37-04-SUMMARY.md`. _Previous stopped-at: Phase 37 Plan 01 COMPLETE — Foundation (cross-repo schema additions + shared GenerationMetadata/StickyConfirmBar primitives + lesson/variant route scaffold). 4 atomic commits across 2 repos: pm-authority `0f0474e` + whatsapp-bot `796ea8a` + `41777cd` + `4ef1060`. pm-authority pytest 70/70 (was 68, +2 new source_snippet tests), whatsapp-bot linkedin vitest 98/98 (was 95, +3 new Plan 37-01 PostSchema field pins), dashboard tsc -b + vite build clean. pm-authority-http restarted to pid 2025950, whatsapp-bot restarted to pid 2026685. The dashboard linkedin barrel is now Plan-37-01-only — 5 stub component files (LessonCandidateCard, LessonGenerationModal, VariantCard, VariantImageSlot, PendingActionEntryButton) exist for Plans 37-02/03/04 to replace bodies of, never the barrel itself. **Resume with Plans 37-02 + 37-03 in parallel.** Plan 37-02 fills LessonCandidateCard.tsx + LessonGenerationModal.tsx + LinkedInLessonSelection.tsx; Plan 37-03 fills VariantCard.tsx + VariantImageSlot.tsx + LinkedInVariantFinalization.tsx. Both consume the new shared primitives + DashboardVariant/DashboardLessonCandidate types and the existing pick-lesson / pick-variant proxy hooks. Plan 37-01 SUMMARY at `.planning/phases/37-lesson-mode-ux/37-01-SUMMARY.md`.

_Previous: 2026-04-14 — Phase 35 Plan 03 COMPLETE._ Dashboard-side presentational primitives for `/linkedin/queue` shipped in Wave 2 parallel with 35-02 (file-disjoint by design). 5 task commits under the `feat(linkedin-ui):` prefix landing 6 new files: `dashboard/src/components/linkedin/postStatus.ts` (LinkedInPost type mirror + STATUS_STYLES map + isPending/isApproved/isNonTerminal predicates), `.../nextPublishSlot.ts` (pure DST-aware Tue/Wed/Thu 06:30 Jerusalem calculator using native Intl.DateTimeFormat with iterative offset correction — smoke-verified against 5 inputs including Oct 25 autumn fallback), `.../LinkedInPostCard.tsx` (queue + published variant card with 96×96/48×48 thumbnail, Hebrew-above-English content rendering, metrics row or 'Metrics pending' placeholder), `.../StatusStrip.tsx` (sticky 4-mini-card strip with 60s setInterval countdown and amber degraded banner branch), `.../index.ts` (barrel), `dashboard/src/pages/LinkedInQueue.tsx` (named LinkedInQueuePage + default LinkedInQueueMockPage for standalone verification). `cd dashboard && npx tsc -b` clean; `npm run build` produces 627 kB bundle cleanly. Scope discipline verified — no src/api/** touched, no AppLayout.tsx/router.tsx touched (Plan 35-04 owns those). Plan 35-02 also completed in parallel in the same execution window (SSE stream route — src/api/linkedin/routes/stream.ts + wiring in src/api/routes/linkedin.ts — commits 9300c8f/68b2e1c/562436c). Phase 35 progress: 3/4 plans done.

_Previous: Phase 35 Plan 01 COMPLETE. Cross-repo PostDTO.analytics embed + PM2 whatsapp-bot unblocker shipped._ Live wire verified end-to-end: pm-authority emits `"analytics": null` on both PUBLISHED posts (live post_analytics table has 0 rows), whatsapp-bot `PostSchema.parse` accepts absent/null/populated/partial-null shapes, Phase 34 routes now reachable on port 3000 with `/api/linkedin/health` → 401 (JWT guard). Commits: pm-authority `baeb9b6`, whatsapp-bot `f9b8e23`. Tests: pm-authority HTTP 56/56, whatsapp-bot linkedin vitest 73/73, live integration 6/6. better-sqlite3 rebuilt from source against Node v20.20.0 to fix NODE_MODULE_VERSION ABI mismatch (was compiled against v22/v127, PM2 runs under v20/v115). Full plan-01 summary at `.planning/phases/35-linkedin-queue-read-side-ui/35-01-SUMMARY.md`. Resume with `/gsd:execute-phase 35` to kick off Plan 35-02 (status strip with next publish slot). Downstream plans can consume `post.analytics` as a first-class field — no second round-trip needed for metrics.

_Previous session (2026-04-13):_ Phase 34 COMPLETE (4/4 plans shipped). Plan 34-04 added `src/api/linkedin/__tests__/integration.test.ts` — a live wire-level vitest file that boots a real Fastify instance with `linkedinRoutes` (stubbed `authenticate` decorator) and exercises the full proxy stack against the real PM2-running pm-authority on 127.0.0.1:8765 via `fastify.inject()` + real `undici` global `fetch`. Six tests pin: (1) health upstream:'ok' via real `ProxyHealthResponseSchema` parse with live version + db_ready=true, (2) posts list → `z.array(PostSchema).parse(body)` round-trips cleanly on real data (2 live posts: 1 APPROVED + 1 PENDING_VARIANT both parse without issue), (3) bogus post id → 404 NOT_FOUND envelope verbatim with `post_id` echoed in `details`, (4) bogus job id → 404 NOT_FOUND envelope verbatim (different upstream router — `routers/jobs.py` with in-memory JobTracker backend vs SQLite, gives two independent pass-through proofs), (5) multi-status filter: data-agnostic assertions — every returned row matches its filter, sum-of-non-terminal-counts ≤ unfiltered.length (matches pm-authority's `NON_TERMINAL_STATUSES` default), multi-status union `?status=DRAFT&status=APPROVED` ≤ total, (6) single post GET by real discovered UUID → `PostSchema.parse` succeeds with matching id/sequence_id/status (skips cleanly if DB empty). Portability skip gate: `beforeAll` probes `/v1/health` with 500ms `AbortSignal.timeout`; every test starts with `if (!pmAuthorityReachable) { console.warn('[integration.test] skipping: ...'); return; }` so when upstream is down, all 6 tests emit a grep-able warning and pass trivially. Verified both ways: 6/6 live in ~192ms (real upstream), 6/6 skip-pass in ~28ms when `PM_AUTHORITY_BASE_URL=http://127.0.0.1:9999`. `vi.unstubAllGlobals()` at module top defends against stubbed-fetch leakage from reads/writes test files. Full linkedin vitest suite: 69/69 passing (18 client + 7 health + 13 reads + 25 writes + 6 integration). TypeScript clean. Phase 34 SC#1-SC#4 now end-to-end verified on the live wire (not just in unit tests). LIN-02 flipped to `[x]` Complete with traceability-table evidence. PM2 `whatsapp-bot` process was NOT restarted this session — `pm2` CLI not on agent PATH (installed as a node module), restart would briefly disconnect active Baileys, integration test already proves wire behavior in-process. The running whatsapp-bot still serves pre-Phase-34 code on its listening socket. whatsapp-bot commit: 43b18f3 (test 34-04: integration.test.ts + skip gate). Plan 34-04 SUMMARY.md lives at `.planning/phases/34-fastify-proxy-layer/34-04-SUMMARY.md`. Optional edit round-trip test DROPPED because the 2 live posts are in unsuitable states (APPROVED blocks edit via state guard, PENDING_VARIANT might interact with live variant generation) — write routes already pinned by 25 mocked-fetch tests with full pass-through coverage.
Resume with: `/gsd:execute-phase 35` to run Plan 35-04 (the final plan). Plan 35-04 wires live data: installs zod in dashboard deps, creates `useLinkedInQueueStream` hook (SSE client with Zod payload parse + drift detection), creates `useLinkedInPublishedHistory` hook, replaces `LinkedInQueueMockPage` default export in `dashboard/src/pages/LinkedInQueue.tsx` with a real-data wrapper that passes the hook outputs into the named `LinkedInQueuePage` export, registers the route in `dashboard/src/router.tsx`, adds the nav entry in `dashboard/src/components/layout/AppLayout.tsx`, and runs a live browser checkpoint. FIRST ACTION for the next agent still stands: verify whatsapp-bot PM2 is serving Phase 34 routes (it was unblocked in Plan 35-01; skip `pm2 restart` unless Plan 35-02's stream route requires a reload — check 35-02-SUMMARY.md for the definitive answer). Phase 35 wires the React `/linkedin/queue` page (LIN-03/04/05/06): list view by status, status strip with next publish slot, recent-published tab, SSE auto-refresh. The full typed proxy surface (5 GET + 8 POST routes + /health + Zod schemas) is ready to consume from `dashboard/src/*` — the dashboard can `import { PostSchema, JobSchema, ... }` directly from `src/api/linkedin/schemas.ts` for fully-typed fetching since snake_case wire format is preserved end-to-end.
